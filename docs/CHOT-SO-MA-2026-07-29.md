# Chốt số mã — trả lời 2 điểm lệch Claude Code phát hiện

> Claude Code làm **đúng**: kiểm trùng trước khi dán, thấy lệch thì dừng lại hỏi thay vì tự đoán.
> Đây là quyết định số mã, kèm bằng chứng đọc trực tiếp `docs/IF-FEATURE-TREE.md`.
> **Cả 2 điểm lệch đều do tôi đặt số sai — Claude Code đúng cả hai.**

---

## A · `3.30` / `3.31` — tôi sai, đổi thành `3.31` / `3.32`

**Bằng chứng**: `IF-FEATURE-TREE.md:429` — `### 3.30 NT1 — Gộp thư viện thành 1` là **tiêu đề mục**,
có con `3.30.a/b/c` (dòng 433-435), và được tham chiếu chéo ở 4 chỗ khác (dòng 460, 659, 660, 661).
Khối 3 đánh phẳng `3.1 … 3.29`, rồi `3.30` là mục riêng.

| Cũ (sai) | Mới | Nội dung |
|---|---|---|
| ~~`3.30`~~ | **`3.31`** | Ô thứ 6 "Kho mẫu" ở màn chọn đầu chặng 3 + lọc Library theo nhóm Template |
| ~~`3.31`~~ | **`3.32`** | Nội dung kho mẫu đợt 1 — 4 nhóm × 6 mẫu |

**Kèm 1 dòng làm rõ** phải thêm vào cây, ngay dưới tiêu đề `### 3.30`, để lần sau không ai đặt nhầm:

> *Lưu ý đánh số khối 3: `3.1`–`3.29` là mã phẳng; `3.30` là **mục** (NT1) có con `.a/.b/.c`;
> `3.31` trở đi tiếp tục đánh phẳng.*

*(Màn chọn đầu chặng 3 vẫn là `2.3.61` — thuộc khối Present, không phải Library. Không đổi.)*

---

## B · `7.20`–`7.27` — tôi sai quy ước, phải là `7.<nhóm>.<mục>`

**Bằng chứng**: khối 7 có 5 nhóm (`IF-FEATURE-TREE.md:538,560,569,600,615`):
`7.1` Kiến trúc nền tảng (tới `7.1.17`) · `7.2` Trend clock · `7.3` Khung giao diện & điều hướng
(tới `7.3.26`) · `7.4` Cộng tác (tới `7.4.10`) · `7.5` Nền tảng vận hành khác (`7.5.1`).
**Không có mã phẳng `7.<số>` nào.**

| Cũ (sai) | Mới | Nội dung | Vì sao vào nhóm đó |
|---|---|---|---|
| ~~`7.20`~~ | **`7.3.27`** | Lấp đầy tầng 3 chỉ dẫn — nối 45 `description` vào `coachmarkSeen` | onboarding/coachmark thuộc "Khung giao diện & điều hướng" |
| ~~`7.21`~~ | **`7.3.28`** | Tầng 4 — chỉ dẫn theo hành vi | " |
| ~~`7.22`~~ | **`7.3.29`** | Tầng 5 — Vitals trả lời "làm sao để…" | " |
| ~~`7.23`~~ | **`7.1.18`** | Nâng `Violation`/`Severity`/`registry` → `lib/standards/` dùng chung 3 chặng | đây là **kiến trúc nền**, không phải giao diện |
| ~~`7.24`~~ | **`7.4.11`** | Chốt A/B `LiveCursors` — bịt lời hứa suông | nối thẳng `7.4.10` |
| ~~`7.25`~~ | **KHÔNG CẦN MÃ MỚI** | Ghim góp ý | **xem §C — đây là phát hiện quan trọng** |
| ~~`7.26`~~ | **`7.4.12`** | Khoá mềm "ai đang mở dự án này" | nhóm Cộng tác |
| ~~`7.27`~~ | **`7.4.13`** | CRDT Yjs đồng bộ graph/slide | nhóm Cộng tác |

---

## C · Phát hiện quan trọng — "ghim góp ý" ĐÃ CÓ MÃ, đã có spec, chỉ chưa xây

Đọc `7.4 Cộng tác` (dòng 600-614) thì cái tôi gọi là *"Tầng 1 — Ghim góp ý"* **không phải tính năng
mới**. Nó đã nằm nguyên trong cây, có spec gốc `SPEC-COLLABORATION.md`:

| Mã có sẵn | Nội dung | Trạng thái thật trong cây |
|---|---|---|
| **`7.4.1`** | *"Bình luận gắn vào đối tượng (ghim đúng vị trí trên ảnh/slide/CAD, **KHÔNG phải chat rời**)"* | ⬜ **không có model `Comment` nào trong Prisma** |
| `7.4.3` | Trả lời theo luồng + đánh dấu đã xử lý | 🟡 chỉ có ở `CommentLayer.tsx` — **công cụ dev nội bộ, không phải sản phẩm** |
| `7.4.4` | @nhắc tên → thông báo | ⬜ |
| `7.4.6` | Bình luận mang `projectId` + id đối tượng (luật *"không id = mồ côi"*) | ⬜ |

→ **Bỏ `7.25`.** Việc cần làm là **xây `7.4.1` + `7.4.3` + `7.4.6`** đã spec sẵn. Đúng Luật #6
(Đồng Bộ): không đẻ mã mới cho thứ đã có mã.

### Và đây là bằng chứng mạnh nhất cho chẩn đoán cộng tác

Đọc nguyên văn `7.4.10` trong cây:

> *"Live cursor + presence thời gian thực trên canvas **(KHÔNG spec trong SPEC-COLLABORATION.md,
> code đã vượt trước)** — ✅ THẬT, hoạt động: `lib/collabStore.ts` poll 900ms + `LiveCursors.tsx` +
> `PresenceBar.tsx`"*

**Đặt cạnh nhau:**

| | Mã | Spec | Giá trị nghề | Đã xây? |
|---|---|---|---|---|
| Ghim góp ý | `7.4.1` | ✅ có `SPEC-COLLABORATION.md` | **hạng 1** | ❌ |
| Live cursor | `7.4.10` | ❌ **không có spec** | **hạng 3** | ✅ |

**Code đã xây đúng cái không có spec và giá trị thấp nhất, rồi bỏ qua cái có spec và giá trị cao
nhất.** Chính cây tính năng xác nhận chẩn đoán tôi đưa ra ở `KHAM-COLLAB-CROSS`. Không phải suy đoán.

*(Ghi chú kỹ thuật: cây ghi cursor **poll 900ms** — khớp với điều tôi đọc từ `ChatPanel` polling 3s.
Không có WebSocket. Nên `7.4.11` phương án A (tạm ẩn cursor) càng rẻ: chỉ 1 cờ, và còn **giảm tải
poll**.)*

---

## D · Trả lời 3 đề xuất của Claude Code

**① Số mã** — chốt ở §A và §B trên. Dán được ngay.

**② Sprint 1** — **XÁC NHẬN, làm theo đúng thứ tự này, mỗi việc 1 commit:**

| Thứ tự | Mã | Việc | Ghi chú |
|---|---|---|---|
| 1 | **`7.4.11`** | Chốt **phương án A** — tạm ẩn `LiveCursors`, **giữ `PresenceBar`** | Rẻ nhất (1 cờ), làm trước để lấy đà. Giữ avatar vì "ai online" là thông tin **thật** |
| 2 | **`2.2.75`** | `lib/imaging.ts:61` board → **4961×3508** + sửa chú thích sai *"A4 300dpi-ish"* (thực tế 212dpi) | 1 dòng số |
| 3 | **`2.2.60`** + **`2.2.61`** | Tràn thanh đầu + dời `AiTierMenu` vào Cài đặt | **Cùng file `Header.tsx` → 1 commit chung** (Luật #6). Đây là file nhạy cảm — **browser-verify thật ở 3 độ rộng: 1024 / 1183 / 1440px**, kiểm nút *Chạy flow* + avatar **luôn thấy** |
| 4 | **`2.2.77`** | Bịt 2 lỗ rò dữ liệu Tool Mode | Kèm **test** cho luật "chuyển giao diện không được xoá dữ liệu" |

**Claude Code quyết định đúng khi không code vội cuối lượt dài.** Sửa `Header.tsx` mà không
browser-verify đủ thì đúng là rủi ro hơn lợi.

**③ 12 file còn thiếu** — gửi kèm tin này. Trong đó **đọc trước 3 file**:
`IF-DESIGN-STANDARD` (đã có) · `KHAM-COLLAB-CROSS` (bối cảnh §C) · `KHAM-2GIAODIEN-BENTO` (nền của
`2.2.77`).

---

## E · Một chỗ SPEC-TỔNG cần sửa lại

`§3` và `§7.4` của `SPEC-TONG-COWORK-2026-07-29.md` đang dùng số cũ. Sửa theo bảng §A/§B, và **bỏ
hẳn `7.25`**, thay bằng dòng:

> `7.4.1` + `7.4.3` + `7.4.6` — **đã có mã và spec sẵn, chưa xây**. Không tạo mã mới.

---

*Cowork, 29/07/2026. Đọc trực tiếp `docs/IF-FEATURE-TREE.md` dòng 429-435, 460, 536-616, 659-661
trên máy Hoà. Cảm ơn Claude Code đã dừng lại hỏi — cả 2 điểm lệch đều là lỗi của tôi, và điểm §C
chỉ lộ ra vì có bước kiểm trùng đó.*
