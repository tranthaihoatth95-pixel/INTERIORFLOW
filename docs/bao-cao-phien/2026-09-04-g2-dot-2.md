# G2 · HÀNH TRÌNH NGHỀ — ĐỢT 2 (04/09)

**Việc:** nâng cổng G2 bằng cách **thêm hành trình vào bộ chạy đã có**, không dựng bộ mới.
`scripts/nghiem-thu-g2-hanh-trinh.mjs`: `HANH_TRINH` **4 → 10 mục**, khung `chayMot()` **không đổi
một dòng logic**.

**Kết quả một dòng:** `ĐẠT 9/10 · hiệu chuẩn ĐẠT` · **cột KẾT QUẢ ĐÃ LƯU 4/22 → 7/22** ·
**2 lỗi CHẶN** tìm được, cả hai nằm trong vùng cấm ghi nên **khai, không vá**.

---

## ⓪ TIỀN ĐỀ

- **⓪a — khung tái dùng được: XÁC NHẬN.** `HANH_TRINH = [J16, J17, J19, J20]`, khung sở hữu bất
  biến 5 bước, đủ 8 hàm phiếu nêu (`moPhienTrinhDuyet` · `dongPhien` · `dangNhap` ·
  `docKhoSheets` · `moMatVe` · `veMotDuong` · `chup` · `demHang`).
- **⓪b — mốc: LỆCH, đã xử đúng luật.** HEAD ở `f43de304`, **lệch 192 commit** sau
  `origin/integration/2026-09-04`; cây **sạch**; `git merge-base --is-ancestor` **rc=0** ⇒
  `git merge --ff-only` ⇒ HEAD nay đúng `5cb4db6c` (`git rev-list --count 5cb4db6c..HEAD` = **0**).
  Không rebase, không `--force`.

---

## ① CỘT ĐÃ LƯU ĐI TỪ 4/22 LÊN **7/22** — bằng chứng đọc từ đâu

| Mã | Trạng thái | Sự thật đọc TỪ NƠI LƯU THẬT |
|---|---|---|
| **J07** | ✅ PASS | ① hàng `ProjectFile` tên **`ban-ve.sao-luu.idf`** — không tin cái tên, **tải ruột về parse**: 1404 byte, **2 thực thể** dựng lại được ② IndexedDB `userId::/cad-editor::projectId` sau khi khôi phục. **Xoá SẠCH hồ sơ trình duyệt giữa hai phiên** ⇒ sự thật chỉ có thể đến từ máy chủ |
| **J12** | ✅ PASS | IndexedDB `userId::/present-editor::projectId` → **7 phần tử / 1 trang**, đếm qua `deck.slides[].elements[]` |
| **J06** | ✅ PASS | IndexedDB `userId::/cad-editor::projectId` → **1 → 2 thực thể**, và **toàn bộ ID của lần trước còn nguyên** (đo danh tính, không đo số đếm) |
| **J18** | ✅ PASS *(tầng cơ chế)* | **SQL thật** trên `Flow`: tab A 200 · tab B **409** · CSDL giữ đúng bản của A (`rev 1`), `graphJson` chứa `"a"` và không chứa `"b"` |
| **J22** | ✅ PASS | Không có cột này để đầy — nó không sinh gì. Sự thật là **phản hồi máy chủ**: `503` + `code PROVIDER_NOT_CONFIGURED` + câu báo nói được việc phải làm |
| **J04** | ❌ FAIL | SQL: `Project` **20 → 20**, `Flow` **12 → 12** |

🔴 **Vì sao tăng 3 chứ không tăng 6, nói thẳng:** J04 **đỏ thật**; J18 mới xong **nửa cơ chế**;
J22 **không có cột ĐÃ LƯU** để mà đầy. Ba chữ "chưa" đó là số thật, không làm tròn lên.

📌 **Một chỗ ma trận ghi sai từ đầu, nay sửa:** cột ĐÃ LƯU của J07 ghi *"`Flow` version"*. Đo tại
nguồn: `grep "api/flows"` trong `components/cad/` + `lib/cad/` = **0**. Bản vẽ 2D **không đi qua
`Flow`**; đường thật là `lib/cad/luu-len-may-chu.ts` → `POST /api/project-files` (nhịp 30s), và
lưới đỡ khôi phục ở `components/cad/CadSheets.tsx:458` (hồ sơ mới + IDB rỗng ⇒ kéo bản sao máy chủ
về). Chính lưới đỡ đó là thứ J07 dựng ra để bắt.

---

## ② HIỆU CHUẨN TỪNG HÀNH TRÌNH

Danh sách hiệu chuẩn nay **sinh từ chính khai báo hành trình** (`hieuChuanMo`) — quên khai là bộ
tự bêu tên. Lượt cuối: **9/10 có khai**, `J20` chưa (bộ in *"mọi chữ PASS của nó chỉ là lời khai"*).

| Mã | Thế giới hỏng chặn gì | Đỏ vì? |
|---|---|---|
| J07 | `POST /api/project-files` | ✅ **khẳng định** — *"sau 34s KHÔNG có hàng ProjectFile"* |
| J12 | `IDBObjectStore.put` | ✅ khẳng định — *"IndexedDB KHÔNG có bản ghi"* |
| J06 | `IDBObjectStore.put` | ✅ khẳng định — *"phiên 1 không ghi được gì"* |
| J18 | ép `PUT /api/flows/*` luôn trả 200 | ✅ khẳng định — *"tab sau KHÔNG bị chặn ⇒ ghi đè âm thầm"* |
| J22 | ép `POST /api/jobs` trả 200 kèm job bịa | ✅ khẳng định — *"vẫn trả 200 ⇒ app chạy giả"* |
| J04 | `POST /api/flows` | ⚠️ **THOÁI HOÁ — không kết luận được** |
| J16 · J17 · J19 | (giữ nguyên bản cũ) | ✅ khẳng định |
| J20 | **chưa khai** | — |

🔴 **Ba chỗ phải sửa trong chính phép hiệu chuẩn — cả ba là bẫy chung, ghi lại:**

1. **ĐỎ GIẢ VÌ CHẶN SAI ĐƯỜNG.** Bản đầu của J07 chặn `IDBObjectStore.put` ⇒ `CadSheets` không
   chốt được cờ hydrate, canvas không mount, bộ ngã ở `waitForSelector` sau 60s. Khung **bắt
   đúng** và in *"HIỆU CHUẨN KHÔNG KẾT LUẬN: ngã vì hạ tầng"* thay vì cho qua. ⇒ Thế giới hỏng
   phải cắt **đúng đường mà hành trình đó khẳng định**; với J07 là đường máy chủ.
2. **HIỆU CHUẨN THOÁI HOÁ** *(mới, tôi thêm vào khung)*. Hành trình đã đỏ ở **thế giới lành** thì
   hiệu chuẩn của nó chứng minh **số không** — nó đỏ ở cả hai thế giới. Bộ nay tự in
   `⚠️ HIỆU CHUẨN THOÁI HOÁ cho J04`. Không có dòng này thì chữ *"hiệu chuẩn ĐẠT"* ở cuối bảng
   che mất đúng chỗ nó không đo được.
3. **CUỘC ĐUA DO CHÍNH BỘ ĐO TẠO RA.** J18 bản đầu mở tab ở `/projects/[id]/render`; mount
   `FlowCanvas` là `persistNow()` tự PUT và **đẩy `rev`** ⇒ tab A nhận 409 trước cả khi ca tranh
   ghi được dựng. Đứng ở route trung tính `/files` ⇒ hai lệnh PUT của bộ đo là **hai người viết
   duy nhất** ⇒ tất định.

---

## ③ HAI LỖI CHẶN — mỗi lỗi kèm `tệp:dòng`

Cả hai **cùng một họ: nút làm một nửa rồi đứng im.** Và cả hai **không máy nào trong repo bắt
được** — `tsc` rc=0, `npm test` rc=0, `soi:cong-cu-chet` rc=0 và còn báo `H3 · TAY CẦM RỖNG — 0`.
Đúng giới hạn máy đó tự khai: *"chứng minh CÓ ĐƯỜNG MOUNT, KHÔNG chứng minh BẤM VÀO CÓ VIỆC XẢY RA"*.

### D-J04a 🔴 — nút *"Tạo dự án mới"* trên Home không tạo dự án

- `components/home/XuongHome.tsx:184` nút `nut-chinh` *"Tạo dự án mới"* → `onClick={onMo}`
- `components/home/XuongHome.tsx:456-460` `moVat` → `hienVat.href` rỗng ⇒ rơi xuống `onEnter()`
- `components/home/HomeScreen.tsx:575-583` `onEnter` → `setStageDone(true)` + `toProjectRender()`
  — **đi tới dự án ĐANG MỞ**, không tạo dự án nào

**Đo trên app thật** (hồ sơ sạch, đã đóng lớp che `WelcomeIntro`): đúng **một** nút khớp
`/Tạo dự án/i`; bấm xong URL `/` **không đổi** · `Project` **16 → 16** · `Flow` **12 → 12** ·
lệnh gọi API khác GET **chỉ có `POST /api/cursors`** (nhịp presence) — **không một lời gọi
`/api/flows`**.

➕ Kèm theo, cùng chỗ: `XuongHome.tsx:184,187,190` — **ba nút** *"Tạo dự án mới"* ·
*"Mở dự án có sẵn"* · *"Nhập từ tệp · dwg · pdf · ảnh"* dùng **CHUNG một `onClick={onMo}`**.
Ba nhãn khác nhau, một hành vi.

### D-J04b 🔴 — cửa "dự án chưa có bản vẽ": tạo Flow thật rồi kẹt vĩnh viễn

- `components/studio/ProjectScopeEmptyState.tsx:64-77` `handleCreate` kết bằng `goToStage(routeId)`
- `:17-22` `goToStage` → `router.push(stageRoutePath(id, stage))` = **ĐÚNG URL đang đứng** ⇒ Next
  không dựng lại ⇒ `useProjectScopeSync` không chạy lại ⇒ `status` vẫn `missing` ⇒ cửa vẫn render
  và cờ `busy` **không bao giờ được gỡ**
- `:79-93` `handleAttachOrphan` kết y hệt ⇒ dính cùng bệnh

**Đo trên app thật, dự án mới tinh, CẢ HAI chặng:**

```
/cad     · cửa hiện: CÓ · MÁY CHỦ: Flow 0 → 1 · MÀN: kẹt "Đang tạo…" = CÓ, canvas = 0
         · SAU KHI TẢI LẠI: cửa còn = không · canvas = 1
/present · cửa hiện: CÓ · MÁY CHỦ: Flow 0 → 1 · MÀN: kẹt "Đang tạo…" = CÓ, canvas = 0
         · SAU KHI TẢI LẠI: cửa còn = không
```

Chờ 20s không đổi. Không có phản hồi 4xx/5xx nào, không lỗi console — nên đây **không phải lỗi
mạng**, đúng là đường điều hướng không đi tới đâu.

📌 **Vì sao xếp là lỗi chặn chứ không phải phiền toái**: nút chết thì người dùng biết mà đi đường
khác; nút này **ghi dữ liệu xong rồi đứng im** ⇒ người dùng đọc ra là *app treo*, còn cái `Flow`
vừa sinh thì vẫn nằm đó. Cùng họ với ca WorkHub *"nút nói dối việc nó vừa làm"* (`00-CHOT` 04/09).

### Hai chỗ nghi ngờ nhất mà phiếu chỉ đích danh — nói thẳng kết quả

- **J18 SẠCH ở tầng cơ chế.** Máy chủ chặn đúng (409), CSDL giữ đúng bản của người ghi trước,
  **không ghi đè âm thầm**. Client cũng **không nuốt 409 im lặng**: `lib/store.ts:1224-1228` bắt
  409 và gọi `setNotice(...)`.
- **NHƯNG tầng người dùng vẫn UNVERIFIED, và tôi khai rõ vì sao phép đo của tôi không đủ tư
  cách kết luận**: tab B trong bộ đo gửi `fetch` PUT **thô**, tức **đi vòng qua chính bộ xử 409
  của client** ⇒ "màn không hiện gì" là hệ quả của cách đo, **không** phải bằng chứng app im lặng.
  Ba dữ kiện **đọc được từ mã** thì đáng đưa cho mắt Hoà: notice chỉ render ở
  `components/FlowCanvas.tsx:884` (route canvas) · **tự tắt sau 4,5s** (`:437-440`) · và mang
  **màu emerald** — cảnh báo mất-việc đang mặc áo màu thành-công.
- **J22 SẠCH.** Môi trường kiểm **thật sự không có `FAL_KEY`** (`.env` chỉ có `DATABASE_URL` ·
  `NEXT_PUBLIC_DEMO` · `AUTH_SECRET` · `INTEGRATION_ENC_KEY`) nên đây là ca thật, không mô phỏng.

---

## ⑦b CHƯA CHẮC / CHƯA KIỂM

- **Bộ đo ĐI VÒNG D-J04b bằng một `reload()`** (`quaCuaDuAnRong()`). Nó có ghi rõ tại chỗ rằng đó
  là **liều thuốc giấu bệnh cho bộ đo**, không phải bằng chứng cửa đó chạy được. J07 · J12 · J06 ·
  J16 · J17 đều đi qua đường này ⇒ **PASS của chúng đúng cho bất biến lưu-tải, KHÔNG nói gì về
  chất lượng đường vào.**
- **J18 tầng người dùng: chưa đo được** (lý do ở trên). Con số 409 là thật; câu *"người dùng thấy
  gì"* thì chưa.
- **J20 không khai thế giới hỏng** ⇒ theo đúng luật của chính bộ này, PASS của J20 là **lời khai**,
  không phải phép đo hai chiều.
- **J07 chỉ chứng minh đường khôi phục-từ-máy-chủ trong ca IDB RỖNG.** Ca IDB **có dữ liệu cũ hơn**
  bản trên máy chủ thì ai thắng — chưa đo.
- **Chưa chạm Electron đóng gói** (J19 vẫn trích thân hàm `snapshotBeforeUpgrade`, không chạy app
  đóng gói) · **chưa thử trình đọc màn hình** · **chưa đo bằng bàn phím**.
- **Số dự án/flow trong CSDL kiểm bị các lượt chạy làm phình** (mỗi hành trình một dự án riêng,
  find-or-create theo tên) — không ảnh hưởng khẳng định nào vì mọi phép đo đều so **trước/sau
  trong cùng một lượt**, nhưng con số tuyệt đối trong log **không** dùng để suy ra gì khác.
- **Không đọc được ảnh bằng mắt.** 18 ảnh + 1 PDF đã ghi vào
  `docs/delivery/anh-duyet-mat/g2-hanh-trinh/`; tôi chỉ **đo** chúng (J20 đo mực: mọi điểm ảnh =
  255 ⇒ FAIL), **không nhìn**.

## ⑦c HẠN DÙNG KẾT LUẬN

- **Chỉ đúng với Chromium 1194 + Linux + `next dev`.** Không suy sang Safari/Firefox, không suy
  sang bản `next build`, không suy sang Electron đóng gói.
- **Chỉ đúng với hồ sơ trình duyệt SẠCH.** Ca hồ sơ cũ / nhiều tab / nhiều thiết bị chưa chạm.
- **Nhịp 30s của sao lưu máy chủ là hằng số trong mã** (`CadSheets.tsx`). Ai đổi nhịp đó là J07
  phải đổi thời gian chờ, nếu không nó sẽ đỏ vì *chưa tới giờ* chứ không vì *không lưu được*.
- **D-J04a/D-J04b đo lúc `components/home/**` và `components/studio/**` đang do lane DESIGN giữ.**
  Lane đó sửa xong thì phải **đo lại**, đừng chép lại kết luận này.
- Hai lỗi trên **chưa được vá** ⇒ khẳng định về chúng còn hiệu lực cho tới khi có người sửa.

---

## ④ MÃ SẢN PHẨM SỬA: **0 dòng**

Không đụng một dòng mã sản phẩm nào. Hai lỗi tìm được đều rơi vào **vùng cấm ghi** của phiếu
(`components/home/**` · `components/studio/**`) ⇒ khai vào đây và vào `JOURNEY-MATRIX §1.7`,
không tự vá. Toàn bộ thay đổi nằm ở: bộ chạy · ma trận · báo cáo · ảnh bằng chứng.

## ⑤ MÁY KIỂM (chạy tách, đọc rc riêng từng lệnh)

```
npx tsc --noEmit          rc=0
npm test                  rc=0   (0 fail)
npm run soi:frontier      rc=0   🔴 0 LỆCH · 👁 1 qua mắt · ✅ 77 xong-máy · ⬜ 56 chờ
npm run soi:cong-cu-chet  rc=0   H1 40 · H2 0 · H3 0 · H4 1  (giữ nguyên mốc)
```
