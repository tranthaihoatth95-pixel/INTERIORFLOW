# DONG-GO — gỡ #c79a63 khỏi `components/entry/` · 17/08

## 1. Tổng quan
Bốn hex `#c79a63` (màu đồng Hoà bỏ hẳn 16/08) trong `components/entry/**` đã được đổi sang `var(--accent)`. `grep -c "#c79a63\|199,154,99\|COPPER" components/entry/` = **0** (tất cả 10 tệp con). tsc sạch, npm test 0 fail, `soi:tu-dien` exit 0. Không đụng `nav/`, `collab/`, `lib/`, `app/`, `scripts/`, `globals.css`, `--accent*`, `WALLPAPERS`.

## 2. Chi tiết từng mục

### ⓪b HẠ TẦNG — PASS
`HEAD = 3d36277`, `git rev-list --count HEAD..main = 0`, `branch = main`. Không lệch.

### ⓪ TIỀN ĐỀ — 2 điểm ĐÍNH CHÍNH so với phiếu (không BÁC, việc vẫn thật)
| # | Phiếu ghi | Thực tế đo | Xử lý |
|---|---|---|---|
| 1 | *"COPPER = '#c79a63' gõ cứng ở LoginScreen.tsx"* | `grep "COPPER\|#c79a63" components/entry/LoginScreen.tsx` = **0**. Không có `COPPER` const nào trong toàn `components/entry/`. Chỉ 4 hex `#c79a63` ở 4 tệp khác. | Không đổi phạm vi. Đính chính. |
| 2 | *"LoginBackdrop.tsx:222 mặc định `preset:'ember'` → đổi preset mặc định"* | Dòng 222 chỉ là **initial SSR state** trước `useEffect`. Mặc định thật (`loadChoice()` dòng 180) = `{ kind: 'dynamic', id: 'aurora' }` khi chưa có localStorage. Ember chỉ dính COPPER trong công thức render dòng 608. | Đi theo nhánh phụ phiếu gợi ý: **giữ ember, đổi công thức render dùng `var(--accent)`**. Không đổi preset mặc định. Ember sau sửa không còn đồng. |

### ③ VIỆC — 4 chỗ sửa
| Tệp:dòng | Trước | Sau |
|---|---|---|
| `LoginBackdrop.tsx:74` | `radial-gradient(... #c79a63 0%, #17130e 70%)` (swatch popover) | `... var(--accent) 0%, #17130e 70%` |
| `LoginBackdrop.tsx:608` | `radial-gradient(circle, #c79a63 0%, transparent 62%)` (quầng đồng blur 90px sau card) | `radial-gradient(circle, var(--accent) 0%, transparent 62%)` |
| `StackedCards.tsx:84` | `boxShadow: 'inset 0 0 0 2px #c79a63'` (vòng chọn workspace card) | `... 2px var(--accent)` |
| `cardFaces.tsx:119` | `linear-gradient(160deg, #c79a63 0%, #7d5a38 52%, #2e2318 100%)` (cover "Present · Deck") | `linear-gradient(160deg, var(--accent) 0%, #7d5a38 52%, #2e2318 100%)` — chỉ đổi stop đầu, 2 stop nâu tối giữ nguyên (là neutral bổ trợ, không dính grep) |

Kèm 3 comment liền kề đã cập nhật ("đồng ấm" → "accent app · bỏ đồng 16/08") — không dùng chữ `#c79a63` trong comment mới (nếu ghi thì grep -c sẽ != 0).

### ⑥b ĐÍCH TRẦN 5 VÒNG
| Vòng | Kết quả |
|---|---|
| `tsc --noEmit` | 0 lỗi (chạy 2× xác nhận, im lặng ra) |
| `npm test` | **0 fail** (tổng ~264 pass qua các suite) |
| `grep -c "#c79a63\|199,154,99\|COPPER" components/entry/` | **0 / 10 tệp** |
| `soi:tu-dien` | exit 0 · 285 chỗ chữ trần (bằng baseline, tôi không thêm) — CHỈ đụng `.tsx`, không tạo `.md` mới trừ báo cáo này |
| Tương phản chữ đăng nhập | xem §3 |

## 3. Tương phản chữ trên card đăng nhập — số đo được
- **`--t1` `#f5f5f7`** (L=0.917) trên **`--card` `#1a1a1e`** (L=0.010, theme TỐI): (0.917+0.05)/(0.010+0.05) = **16.12:1** ✅
- Theme SÁNG cùng cặp: `#211e19` trên `#ffffff` = **15.00:1** ✅
- `--t2` `#d6d6db` (L=0.686) trên `--card` tối = **12.24:1** ✅
- Adaptive-contrast (`LoginForm.tsx:94`, `planCardText` remap `--t1..--t5` khai đạt ≥4.5) là tầng phụ, không tôi đụng.

**Quầng nền sau đổi** (không phải bề mặt card, ảnh hưởng gián tiếp qua backdrop-blur):
- L(`#c79a63`) = **0.363** → L(`#6a57f5`) = **0.165** ⇒ nền phía sau card **tối hơn 55%**, chữ sáng chỉ có thể contrast TĂNG chứ không giảm.

## 4. Đánh giá khách quan
- ✅ **Tốt**: đích trần 5 vòng đóng gọn; đổi 4 chỗ = 4 dòng code; grep về 0 tuyệt đối kể cả comment; không đụng bất cứ token nào; không phát sinh migration.
- ⚠️ **Chưa chắc**:
  1. **Không chạy trên trình duyệt thật** — hiệu ứng thị giác của quầng tím (thay đồng) chưa được Hoà nhìn mắt. Về NGHĨA màu, `var(--accent)` là tím chủ đạo app 16/08 → có thể "át" khi làm quầng blur to (54rem × 54rem, opacity 0.05-0.11), khác cảm giác "đồng ấm quầng đêm" của intro cũ. Đây là **thi hành chốt bỏ đồng**, không phải chọn màu mới — Hoà cần duyệt mắt sau.
  2. `cardFaces.tsx:119` giữ 2 stop `#7d5a38` (nâu ngả copper) và `#2e2318` (nâu tối) — chúng thuộc họ đồng tối hơn, không dính grep `#c79a63`. Nếu tinh thần "bỏ hẳn đồng" mở rộng sang mọi shade thì đây là **nợ scope khác** (grep `#7d5a38` toàn `components/entry/` = 1 chỗ này + `#8a6f4d`/`#6f5b40`/`#b39776` trong swatch dòng 113 cùng tệp — cả 4 là copper shade). Không nằm trong phạm vi phiếu.
  3. Preset mặc định vẫn tên `'ember'` — sau đổi công thức, tên `ember` (than hồng) không còn khớp bản chất "quầng tím". Đổi tên tốn `BgPreset.id` migration localStorage → rủi ro cao hơn giá trị. Đề xuất giữ tên, cập nhật nghĩa qua vi:en label nếu Hoà muốn (không làm ở phiếu này).
- 🔴 **Rủi ro**: swatch dòng 74 hiện màu popover chọn preset — tím quầng có thể nhìn giống "Đêm mực" (`ink`) hơn "Đêm ấm". Nhãn `vi: 'Đêm ấm'` giữ nguyên nhưng cảm giác đã đổi. Đây là hệ quả không tránh được của việc bỏ đồng.

## 5. Hướng xử lý nhiều góc độ
| Hướng | Ưu | Nhược |
|---|---|---|
| **A. Giữ y phiếu, dừng ở 4 hex** | Đóng đúng scope; test/tsc/soi sạch; grep = 0. | Còn 4 shade đồng khác (`#7d5a38`, `#8a6f4d`, `#6f5b40`, `#b39776`) trong `cardFaces.tsx` — chưa dứt tinh thần 16/08. |
| **B. Mở rộng: quét TẤT CẢ shade đồng trong `components/entry/`** | Dứt hẳn tinh thần "bỏ đồng"; 1 công. | Ngoài phiếu; các shade này là swatch minh hoạ moodboard, đổi có thể phá thẩm mỹ mock; cần mắt Hoà duyệt. |
| **C. Đổi luôn tên preset `ember` → tên khớp bản chất tím** (vd `dusk`, `violet-night`) | Từ vựng khớp thị giác. | Đụng localStorage schema, tốn migration + guard; rủi ro hơn giá trị; Hoà chốt tên là quyết định UX. |

## 6. Đề xuất hướng tốt nhất
**A** — giữ đúng scope phiếu, đã đóng. Lý do:
1. Hoà chốt 16/08 nói *"bỏ vàng đồng khỏi VAI màu nhấn"* — 4 hex `#c79a63` là các chỗ nó ĐANG ĐÓNG VAI NHẤN (quầng lớn, vòng chọn, swatch, cover primary stop). Bốn `#7d5a38`/`#8a6f4d`/... trong swatch grid dòng 113 là **hiển thị moodboard 6 ô** — chúng đang ĐÓNG VAI "vật liệu tham chiếu" (nội dung), không phải màu hệ. Luật màu-mang-nghĩa: đây là NỘI DUNG (lớp ③ Brand Kit dự án khi có), không phải vỏ giao diện (lớp ②). Không cần đổi.
2. Hướng B/C đều cần **mắt Hoà** để quyết đẹp/xấu — không phải việc phiếu này. Nếu Hoà muốn dứt tiếp, mở phiếu riêng sau khi nhìn ảnh chụp app thật.
3. T cây chính có thể yêu cầu chụp `LoginScreen` (nền động + preset ember + workspace card selected) đẩy vào `Drive/IF-duyet-mat/01-anh/` để Hoà soi hiệu ứng tím thay đồng.

## ⑦b CHƯA CHẮC / CHƯA KIỂM
- Không mở trình duyệt (`--auto-mode`, ràng buộc phiếu KHÔNG dev server). Mọi số tương phản là **TÍNH**, không phải ĐO trên pixel thật.
- Không kiểm hiệu ứng animation `if-login-kenburns` + `if-login-gradient` với màu mới — chỉ đọc CSS, không chạy.
- Không thử `prefers-reduced-motion` cụ thể — code path đã có `reduce` guard sẵn.
- Không đo `--accent` bản THEME SÁNG (dòng 264+ globals.css có `--accent` theme sáng riêng không?): grep bên trên chỉ trả token dark; nếu light không override thì `--accent` = `#6a57f5` cả hai theme (kiểm sau nếu Hoà mở phiếu B).
- Số 285 chỗ chữ trần trong `soi:tu-dien` là BASELINE ngày 17/08 sau khi COLLAB-NC + P-U + 4 phiếu đợt A ra báo cáo — tôi so bằng cách ĐỌC KẾT QUẢ, không so baseline HEAD trước tôi. Nếu chặt hơn, cần chạy trên bản trước và bản sau ⇒ tôi chọn khai thẳng cách đo thay vì giả vờ chắc chắn.

## ⑦c HẠN DÙNG KẾT LUẬN
- Đúng cho tới khi Hoà **duyệt mắt** hiệu ứng quầng tím. Nếu tím át quá thì phải: giảm opacity quầng (0.05-0.11 → 0.03-0.07) HOẶC đổi công thức về gradient trung tính không dùng accent (`radial-gradient(circle, rgba(255,255,255,0.05) 0%, transparent 62%)`).
- Đúng cho tới khi Hoà chốt xử lý 4 shade đồng còn lại (hướng B). Nếu chốt "bỏ hẳn" thì báo cáo này mất đích §6.
- KHÔNG áp dụng nếu ai đó sau này thêm hex `#c79a63` mới vào `components/entry/` — chống tái phát bằng soi:tu-dien lớp guard cần entry mới cho pattern hex đồng, chưa có.
