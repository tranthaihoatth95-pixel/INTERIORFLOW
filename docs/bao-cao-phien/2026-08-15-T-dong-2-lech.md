# T · BÁO CÁO PHIÊN — ĐÓNG 2 LỆCH ĐỎ + NGHIỆM THU HÀNG ĐỢI RENDER (15/08)

Vai: T (điều phối tổng thể, `docs/HOP-DONG-PHOI-HOP-T.md`). Phiên này KHÔNG phóng sub-agent —
việc là dọn tồn của phiên trước, phóng agent chỉ tốn thêm vòng bàn giao.

## 1 · TỔNG QUAN
Đầu phiên `soi:frontier` báo **2 lệch đỏ** kiểu "code có rồi mà sổ ghi chưa"
(`mirror-doi-xung-chuan-net`, `render-queue-live`). Cả hai là code thật nhưng phiên trước đứt
giữa chừng: **tsc toàn repo đang đỏ** vì một dòng import sai, và bảng hàng đợi render chưa ai mở
mắt xem. Đã sửa lỗi chặn, nghiệm thu bảng hàng đợi trên app thật, flip sổ đúng hiện trạng,
commit 4 cụm. **Kết phiên: soi:frontier 0 lệch · tsc 0 · test 0 fail.**

## 2 · CHI TIẾT

| Mục | Bằng chứng |
|---|---|
| **Lỗi chặn cả repo** | `components/ui/ToolbarChip.tsx:22` import `{ Tooltip }` trong khi `Tooltip.tsx:95` là **default export** → TS2614, tsc đỏ toàn repo. Sửa 1 dòng → `npx tsc --noEmit` exit 0, 0 dòng lỗi |
| **Test** | `npm test` exit 0 · 7.711 assertion ok · 0 fail |
| **mirror-completion** | Code committed `f423652` (`mirrorCompleteShapes` PCA + union-find, 187 dòng). Chạy lại test đầu-cuối: **74 pass · 0 fail**, gồm ca "2 chân hội tụ kích thước, tâm KHÔNG dịch" và "vòng khác vai trò không bị đụng" |
| **Hàng đợi render — verify app thật** | server 3000, `/projects/…/render`, **1440×900**. 4 job diễn tập (0 credit, `window.__ifRenderQueue`): 3 xong → thumbnail **đổi sang ảnh kết quả**, 1 lỗi đỏ kèm lý do đọc được; tổng "3/4 xong"; ETA hiện **chỉ sau** khi có job xong ("0/4 · —" → "1/4 · còn ~26 giây"); `cancelAll` trả đủ 3 `cancelled`; hàng đợi rỗng → panel **tự ẩn hẳn**; "Huỷ tất cả" mờ khi hết việc |
| **Điểm mount** | `HomeScreen.tsx:720` — NGOÀI `<AppShell>`, trong nhánh chặng 3D ⇒ sống chung cả 2 mode Node/3D, loạt view không đứt khi gạt mode |
| **Màn khoá** | `⌃⌘Q` trên app thật: đồng hồ + "Đã khoá · Untitled flow" + dòng trong ngày *"Chậm mà đúng, còn hơn nhanh mà phải sửa lại."* + thẻ đăng nhập lật vào. Chạy đúng |
| **4 máy soi** | `soi:tu-dien` 0 lệch · `soi:contract` 21 có dây / 0 lệch · `soi:hinh-hoc` 10/1010 ngoài thang (nợ cũ) · `soi:thao-tac` **2 lệch — nợ cũ nguyên si** |
| **2 lệch thao-tac KHÔNG phải mới** | focus-visible **31 file** · hex inline **193×** — trùng khít số đã ghi sổ 13/08 ở entry `he-luat-thao-tac`. `grep` 3 file mới trong log lệch = **0 hit** ⇒ phiên này không đẻ lệch nào |
| **Commit** | `13e4c23` hàng đợi render · `1e5b408` màn khoá · `bdd6a96` ToolbarChip + fix tsc · (+1 cụm docs/sổ) |

## 3 · TỔNG KẾT VẤN ĐỀ
Không có bug kiến trúc nào ở đây. Đây là **bệnh bàn giao**: phiên trước làm xong 3 mảnh việc tốt
(hàng đợi render, màn khoá, nền ToolbarChip) nhưng dừng trước bước chốt — không commit, không
flip sổ, và để lại một import sai đủ làm tsc đỏ. Máy soi bắt được đúng 2/3 mảnh (mảnh thứ ba,
ToolbarChip, không có entry nên không ai canh). Cơ chế sổ frontier **đã làm đúng việc của nó** —
đó là lý do 2 lệch không trôi sang tuần sau.

## 4 · ĐÁNH GIÁ KHÁCH QUAN
**Tốt**
- Hàng đợi render viết đúng luật: tiến trình là số thật đi nguyên chuỗi `onProgress`, ETA không
  bịa, huỷ thật bằng `AbortController`, có cửa nghiệm thu 0 credit — nhờ cửa này tôi mở mắt được
  toàn bộ 4 trạng thái mà không đốt một job AI nào.
- Cả hai mảnh đều **tự khai giới hạn ngay trong code** (huỷ dừng ở ranh giới node; không thêm
  icon mail vì chưa có hệ thật) thay vì im lặng.

**Chưa / rủi ro**
- Đường job **THẬT** (`kind:'node'` → `runNode`) mới verify bằng đọc mã + tsc, **chưa chạy sống**
  vì tốn credit. Đây là khoảng cách thật giữa "chạy được với job diễn tập" và "chạy được với
  render thật" — đã ghi vào registry, không tô hồng.
- `ToolbarChip` là **component mồ côi**: đã commit nhưng chưa ai import. Đúng kiểu lệch #4 trong
  §6 hợp đồng T ("UI về đích, code 0 dòng" — ở đây là biến thể ngược: code có, đích chưa tới).
  Nó chỉ an toàn vì L1 còn nguyên trong hàng đợi.
- Nợ nghiệm thu mắt tăng lên **64 mục** trong khi mới **1 mục** qua mắt Hoà. Đây là con số đáng
  lo nhất của phiên, hơn cả hai lệch vừa đóng — băng thông duyệt mắt của Hoà là tài nguyên khan
  hiếm nhất, đúng cảnh báo đỏ đã ghi ở `DOI-CHIEU-3-TRUONG-PHAI`.
- `AGENTS.md` (untracked) là bản sao CLAUDE.md do công cụ ngoài sinh, có chỗ sai
  (`.Codex/launch.json` thay vì `.claude/launch.json`). **Tôi không commit** — không phải sản
  phẩm phiên này, và một bản sao lệch của luật nền là mầm loạn thông tin.

## 5 · HƯỚNG XỬ LÝ — NHIỀU GÓC
**Hướng A — đi tiếp L1 (lắp ToolbarChip vào 3 thanh công cụ).**
Ưu: đóng nốt mảnh mồ côi, gỡ lệch nặng nhất của hiến pháp giao diện, đúng ưu tiên Hoà nêu 15/08
("dứt điểm giao diện"). Nhược: đụng 3 component ruột của cả 3 chặng, rủi ro hồi quy thị giác cao,
và **kết quả phải qua mắt Hoà** — đẩy tiếp vào chỗ đang tắc.

**Hướng B — mở Lô duyệt mắt (48 mục / 7 trạm, ~20 phút).**
Ưu: tấn công thẳng nút thắt thật (64 nợ mắt / 1 đã duyệt); một lượt của Hoà flip được cả loạt.
Nhược: tốn đúng thứ Hoà thiếu là thời gian; không đẻ thêm dòng code nào.

**Hướng C — vá findings DF2 (F3–F9).**
Ưu: đó là lỗi trên đường dogfood thật, có cái mất dữ liệu (F4 mất slide, F5 phình 190MB) — loại
lỗi ăn vào niềm tin. Nhược: 7 finding rời rạc, không dồn vào một giá trị nhìn thấy được.

## 6 · ĐỀ XUẤT
**Chọn B trước, rồi A** — và cụ thể là: Hoà đi Lô duyệt mắt #1 (`docs/duyet-mat/LO-1-2026-08-13.md`),
song song tôi lắp ToolbarChip vào 3 thanh công cụ để đúng luật "mỗi đợt ≥1 việc giàu cốt lõi +
≥1 việc nhìn-thấy-được".

Vì sao B trước A: hai lệch vừa đóng đều là **xong-máy**, và sổ đang có 64 mục như thế đối lại
đúng 1 mục qua mắt. Làm thêm hướng A mà không giải nút mắt thì chỉ nâng 64 thành 65 — đó chính
là kiểu lệch "cái gì cũng có, không cái nào trọn" mà §6 hợp đồng bắt tôi phải cảnh báo. B là
việc **chỉ Hoà làm được**, A là việc tôi làm được — nên chạy song song, không xếp hàng.

Không chọn C lúc này: F3–F9 quan trọng nhưng thuộc đường dogfood, mà dogfood chỉ có giá trị khi
người thật đi lại — để chung đợt với "vòng người dùng thật" thay vì vá lẻ bây giờ.

## 7 · CHỜ HOÀ
1. **Đi Lô duyệt mắt #1** — trả lời "Trạm N ok/lệch" là tôi flip loạt.
2. **Bảng hàng đợi render**: `npm run dev` → mở `/projects/…/render` (**cửa sổ ≥1440 rộng**, hẹp
   hơn app rơi vào "Tổng quan — chỉ đọc" và panel không mount) → dán `__ifRenderQueue.demo(3)`
   vào console là thấy chạy (0 credit). Xem xong cho một chữ ok/lệch.
   *(T đã dừng server mình mở, đúng luật §0aa một-server-một-thư-mục.)*
3. **Icon thông báo/mail ở màn khoá** — chưa làm vì app chưa có hệ thật; Hoà xác nhận nó nối vào
   đâu thì tôi mới gắn.
4. **`AGENTS.md`** — giữ untracked hay muốn tôi dọn cho khớp CLAUDE.md?
