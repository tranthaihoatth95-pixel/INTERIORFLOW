# BÀN `04` · DESIGN · thẩm mỹ và bố cục

> Ngồi vào bàn này thì đọc hết trang này **trước khi gõ**. Rời bàn thì cập nhật mục **ĐANG DỞ**.
> Kiến thức nằm ở bàn, không nằm ở người ngồi — xem `docs/control/ban/README.md`.

## VAI
ngôn ngữ thị giác · lưới · tỉ lệ · thứ bậc

## CẤM
- lấy GU-PROFILE làm chuẩn — nó là DNA số 1, KHÔNG phải chuẩn
- kết luận bố cục từ số CSS mà chưa nhìn ảnh app thật

## BÀI HỌC ĐÃ TRẢ GIÁ — đọc để không trả lại
Thứ tự Hoà chốt: chuẩn ngành → điều hơn chuẩn → HỌC GU SAU CÙNG.
Đo 30/08: thang chữ IF có BỐN tỉ lệ (1,167·1,143·1,25·1,400) — thang mô-đun đúng nghĩa có MỘT.

## NGHIỆM THU — xong ở bàn này nghĩa là
qua `if-design-review` sáu cổng · trên ảnh app thật · cả hai theme

## CHỐT CỦA HOÀ — 30/08, hệ màu
5 bộ, mỗi bộ = **hình nền + bảng màu đi liền**, ứng 5 phong cách/cá tính nghệ thuật.
Người dùng chọn bộ, rồi tinh chỉnh **trong range design system cho phép** → combo cá nhân hoá mà
**vẫn không phạm luật chung**.
⇒ Token nền phải **dẫn xuất từ bộ**, không cố định cứng — base tự mang gu thì mọi wallpaper khoác
lên đều bị nhuộm. Chi tiết: `docs/control/IF-CHUAN-NEN.md` §4.

## ĐANG DỞ
- ✅ **30/08 — xong phần khai báo + range + cổng.** `docs/control/IF-HE-5-BO-MAU.md` trả cả ba câu
  ①②③; mã ở `lib/wallpaper/mau-bo.ts` + `mau-bo.test.ts` (51 pass, 8 ca đột biến).
  ⚠️ **ĐỌC TRƯỚC KHI NHẬN VIỆC MÀU:** năm bộ **đã tồn tại từ 16/08** ở `lib/wallpaper/sets.ts` —
  control plane từng ghi "chưa có". Đừng dựng lại; đọc `sets.ts` trước.
- 🔴 **CÒN NỢ, theo thứ tự:** ① Hoà quyết khoảng trống gu ẤM (`IF-HE-5-BO-MAU.md` §3b — bốn vùng
  cấm màu nghĩa bịt kín dải 350°–57°, tức champagne/đồng của `GU-PROFILE` §1 **không có chỗ hợp
  lệ**; ba đường A/B/C, mặc định khuyến nghị B) ② nối `--accent` + dải trung tính **dẫn xuất từ bộ
  đang chọn** vào `globals.css` — nay vẫn là hằng số ③ dựng mặt chỉnh 6 token ④ soi app thật.
*(người rời bàn cập nhật mục này — để trống nghĩa là bàn sạch, không phải nghĩa là chưa ai ghi)*

## NẠP TRƯỚC KHI GÕ
- `CLAUDE.md` bộ nạp · `docs/control/IF-CURRENT-STATE.md` · `docs/control/IF-CHUAN-NEN.md`
- `node scripts/moc.mjs inbox 04` — phiếu đang chờ

---

<!-- MÁY GIỮ · phieu-ca.mjs · CẤM SỬA TAY -->

## VIỆC ĐANG MỞ — máy sinh 2026-08-31 14:07

Đang ngồi: **chưa ai khai giữ lane này**

✅ Không phiếu nào đang mở.

_2 phiếu đã đóng — không liệt kê ở đây. Tra lịch sử: `node scripts/phieu-ca.mjs`_

⚠️ Bàn chỉ chở thứ CÓ BIÊN NHẬN. Nội dung phiếu nằm trên cầu, không chép vào đây —
chữ người viết có thể sai, và bàn không được khuếch đại cái sai.

<!-- /MÁY GIỮ -->
