# BÀN `06` · BUILD · thi công mã

> Ngồi vào bàn này thì đọc hết trang này **trước khi gõ**. Rời bàn thì cập nhật mục **ĐANG DỞ**.
> Kiến thức nằm ở bàn, không nằm ở người ngồi — xem `docs/control/ban/README.md`.

## VAI
**PHIÊN CODE CHÍNH của tuyến Claude** (chốt 30/08). Bút mã sản xuất ở đây, không ở MAIN.
viết mã · sửa lỗi · dựng bề mặt · chạy test · **chứng minh trên runtime**

Codex bên kia **không thi công** — họ plan và viết phiếu; MAIN phân rã rồi giao xuống đây.
Xem `IF-CANONICAL` §8c.

## CẤM
- copy tệp DXF khách vào repo
- thêm import vào Web Worker mà chưa chạy `soi:worker`

## BÀI HỌC ĐÃ TRẢ GIÁ — đọc để không trả lại
Worker chết IM vì `window is not defined` — chuỗi import tĩnh kéo store vào worker, React Refresh chạm window ở phạm vi module. Guard `typeof window` KHÔNG cứu được.
Cổng: `npm run soi:worker`.
Đang dở: hiện 81 tường (286,0 m) lên màn — máy đọc được rồi, chưa mặt nào đọc.

**Chụp màn `claude-in-chrome` đòi tab MCP ĐANG HIỆN** (đo 31/08, phiếu `HO-20260831142741-0e5074bac859`).
Tab ẩn ⇒ trình duyệt không vẽ ⇒ `rAF` không chạy ⇒ không có khung nào để chụp. Lỗi trả về là
*"page busy"* — **báo nhầm nguồn**, nó đẩy người đọc đi truy một trang đang khoẻ. Bàn này phải
chứng minh trên runtime bằng ảnh, nên bẫy này nằm đúng trên đường nghiệm thu: đưa tab lên trước.

**`npm test … | tail` đọc exit của `tail`** (đo 31/08). `tail` gần như luôn 0 ⇒ chuỗi ĐỎ đọc ra
thành xanh. Đây là "PASS giả" (luật 5) sinh ra từ một thói quen gõ lệnh, không từ ý định.
⇒ `PIPESTATUS`, hoặc chạy không ống.

🔴 **CHẠY BẢN PRODUCTION TRÊN CỔNG DEV ĐẦU ĐỘC MỌI PROFILE TRÌNH DUYỆT — qua SERVICE WORKER.**
Đo 02/09 10:05, hai lane tìm ra độc lập rồi khớp nhau. Đây là bản ĐÚNG; hai ghi chú trước của
tôi (*"build giết dev, restart là xong"*, và chẩn đoán *".next nhiễm production"*) đều **SAI hoặc
thiếu** — giữ lại nguyên nhân dưới đây, bỏ hai bản kia.
· Cơ chế (FACT, đọc nguồn): `components/PWARegister.tsx:14` chỉ đăng ký SW khi `NODE_ENV=production`;
  `public/sw.js:44-49` **cache-first mọi `/_next/static/*`**. Đêm 01-02/09 bản prod chạy trên CÙNG
  origin `localhost:3001` ⇒ SW `iflow-v1` cắm vào origin đó. Chunk dev **không có hash** ⇒ SW trả
  bản CŨ mãi mãi.
· **`rm -rf .next` VÔ ÍCH. Restart dev VÔ ÍCH.** Độc không nằm trong repo — nó nằm trong TỪNG
  PROFILE TRÌNH DUYỆT đã từng mở bản prod ở cổng đó. Đĩa đúng, tiến trình đúng, HTTP đúng, mà màn
  hình vẫn sai.
· Thuốc: trong đúng profile đó chạy
  `(await navigator.serviceWorker.getRegistrations()).forEach(r=>r.unregister()); (await caches.keys()).forEach(k=>caches.delete(k))`
  rồi tải lại — hoặc dùng context trình duyệt TRẮNG.
⇒ **LUẬT: khi DOM cãi nhau với ĐĨA, hỏi `navigator.serviceWorker.getRegistrations()` TRƯỚC** —
trước `.next`, trước restart, trước mọi giả thuyết về build. Một service worker làm MỌI phép đo
phía máy chủ mất nghĩa, nên nó phải là câu hỏi ĐẦU TIÊN chứ không phải câu cuối.
⚠️ Vẫn giữ một mẩu đúng của ghi chú cũ: `npx next build` ghi đè `.next/` trong khi dev đang phục
vụ từ đó thì route gãy 404 — nhưng cái đó restart chữa được, còn SW thì không. **Đừng trộn hai
bệnh làm một.**

🔴 **PHIẾU CÓ THỂ DIỄN GIẢI GU CỦA HOÀ THEO CHIỀU LẬT NGƯỢC CHÍNH CHỐT CỦA ANH** (đo 02/09).
Phiếu da GĐ2 ghi *"rail trái … accent theo bộ hình nền"*. Nghe rất giống lời Hoà, nhưng lật ba
nguồn thì nó ngược:
· **chốt 2** nói `--accent` **SUY TỪ** bộ hình nền (dây `accent-css`) — nói về NGUỒN của token,
  không nói "tô accent lên rail";
· **chốt 3** *"màu xanh màu tím tè le vậy"* = chê **RẢI** accent nhiều chỗ một màn;
· **cổng `muc-dieu-huong.test.ts`** đang canh đúng quyết định GĐ1 của Hoà: nền hàng đang mở phải
  đi kênh **TRUNG TÍNH `--t1` ≤8%**, kèm nguyên văn *"rail không tím — accent để dành cho CTA +
  trạng thái chạy"*.
⇒ Làm theo phiếu là phạm cả ba, và cổng sẽ đỏ — nhưng nguy hiểm hơn: **cổng đỏ ở đây có thể bị
đọc thành "neo lỗi thời, sửa cổng đi"**, vì chính phiếu đã mời sửa cổng. Đó là đường ngắn nhất
để xoá một quyết định của Hoà bằng hai bước hợp lệ.
**Luật rút ra: phiếu là DIỄN GIẢI, sổ `docs/hoa-noi/SO-TONG.md` + memory `loi-hoa-ve-giao-dien`
là NGUỒN. Lệch nhau thì nguồn thắng, và nói ra chỗ lệch.** Lát 02/09 vì thế chỉ đổi VẬT LIỆU và
HÌNH (viên nang · frosted dày · bóng hai chiều), **không đổi HUE**.

🔴 **HAI CÁCH MỘT CỔNG THÀNH VÔ DỤNG MÀ VẪN XANH** (cả hai đo 02/09, tôi tự gây ra cả hai).
① **CỔNG HOÁ MÙ khi vật bị dời khỏi tầm đo.** Rail tách sang hằng cục bộ `ICON_RAIL`; khối [9]
   của `muc-dieu-huong.test.ts` vẫn đọc `command-icon.tsx` ⇒ **vẫn xanh**, trong khi nó đang chấm
   một con số thanh điều hướng không dùng nữa. ⇒ **Tách một hằng ra khỏi hệ chung thì phải mang
   theo cái cổng**, nếu không mình vừa dời vật ra khỏi tầm máy đo mà máy vẫn báo "đo rồi".
② **CA CỔNG GẮN VỚI MỘT CƠ CHẾ, cơ chế chết thì ca thành lời chúc.** Ba ca canh "viên nhãn nở
   khi rê" đo rất kỹ chất lượng của một cơ chế HOVER — và không ca nào bắt được lỗ của chính cơ
   chế đó: **máy chạm không có hover**, nên ở nấc hẹp bảy mục là bảy hình câm. ⇒ Đổi cơ chế thì
   phải hỏi *"ca này còn đo đúng thứ cần đo không"*, đừng chỉ hỏi *"ca này còn xanh không"*.
Cùng họ với ba nấc đã ghi: *nội dung sai hay LUẬT sai* (lượt 3) · *dụng cụ sai hay vật sai*
(lượt 5) · *NGUỒN sai hay DIỄN GIẢI sai* (02/09). Nay thêm: **cổng xanh hay cổng MÙ**.

🔴 **NGƯỜI VỪA GỌI TÊN MỘT CÁI BẪY KHÔNG HỀ MIỄN NHIỄM VỚI NÓ** (đo 02/09, tôi là ca bệnh).
09:50 tôi viết cho `cl:00`: *"nhãn INFERENCE chỉ có tác dụng nếu thứ mang nhãn đó chưa được đem
đi dùng"* — bắt đúng lỗi của họ. **10:01 tôi làm y hệt**: gắn nhãn INFERENCE cho chẩn đoán
`.next` nhiễm production, rồi vẫn đưa thẳng `rm -rf .next` vào tin cho Hoà dán. Chẩn đoán sai;
`cl:00` chặn kịp bằng cách ĐO ĐĨA thay vì suy từ triệu chứng.
⇒ **Luật phải nằm ở CỔNG, không nằm ở trí nhớ của người vừa phát biểu ra luật.** Khoảng cách giữa
"biết luật" và "làm theo luật" đo được bằng 11 phút.
⇒ Hệ quả thực dụng cho bàn này: **một câu có nhãn INFERENCE mà đi kèm LỆNH CHẠY ĐƯỢC thì nhãn đó
đã hết tác dụng** — người nhận sẽ dán lệnh, không đọc nhãn. Muốn giữ nhãn thì đừng kèm lệnh; muốn
kèm lệnh thì phải đo trước.

🔴 **HAI LUẬT VỀ VIỆC SỬA CỔNG KHI ĐỔI THIẾT KẾ** (đo 02/09, lát H-4 Home).
Đổi luật cỡ ô làm ba ca cổng đỏ. Cách sửa chúng quan trọng hơn bản thân lát:
① **GỠ CHỖ ĐỨNG ≠ GỠ NĂNG LỰC.** Bỏ `vitals` khỏi lưới Home làm đỏ ca *"có insight thật ⇒
   vitals lên tiếng"*. Đảo ca đó là xong — và **sai**: sáu tháng nữa không ai biết Vitals từng
   có chỗ trên Home, hay nó bị bỏ vì hỏng. ⇒ Đảo ca cũ, RỒI THÊM ca mới khoá rằng **năng lực
   vẫn còn** (`coDuLieu('vitals', …)` vẫn đúng ⇒ trả lại một dòng là nó về). Ca thứ hai là
   thứ chở lại **ý định** của lần gỡ.
② **MỘT CON SỐ ĐỪNG CHỞ HAI LUẬT.** Ca *"nhiều hơn một hero"* đo bằng `co === '2x2'` — nó gộp
   *trần một ô lớn mỗi màn* (nhịp lưới) với *đúng một hero* (thứ bậc). Ngày cỡ ô đổi nghĩa,
   ca đó im lặng mất một nửa việc nó đang canh. ⇒ Tách làm hai ca, mỗi ca một luật.
Cùng họ với "cổng XANH hay cổng MÙ" ở trên: **một cổng chỉ đáng tin bằng câu hỏi nó đang hỏi.**

🔴 **TRƯỚC KHI TIN MỘT CON SỐ, HỎI NÓ ĐANG TRẢ LỜI CÂU NÀO** (phiên `d0` đo 02/09, ghi hộ theo
lời nhắn bàn giao — anh ấy hết lease trước khi kịp ghi). Bốn ca trong MỘT buổi, **không ca nào
sai**: cổng đo hằng rail không còn dùng · `rongLuoi: 28` nằm ngay cạnh số đúng vì selector bắt
trúng `.grid` lồng trong widget · `oTran: []` vì máy chỉ đo chiều DỌC trong khi chữ bị cắt chiều
NGANG · "rail mờ" đọc từ ảnh thu nhỏ. Cả bốn trả lời rất đúng một câu hỏi HẸP. **Lát H-4 suýt
được đóng bằng `oTran: []`.** ⇒ Câu hỏi phải hỏi không phải *"số này có đẹp không"* mà *"số này
đang đo cái gì, và đó có phải cái tôi cần biết không"*.

🔴 **SCRIPT ESM NGOÀI REPO PHẢI IMPORT `node_modules` BẰNG ĐƯỜNG TUYỆT ĐỐI** (cùng nguồn).
ESM giải tên gói theo VỊ TRÍ CỦA TỆP, không theo `cwd` ⇒ `cd repo && node /duong/ngoai/x.mjs`
vẫn nổ `ERR_MODULE_NOT_FOUND`, và nổ **trước** `main()` nên **không có cả thư mục output** —
dấu hiệu nhận ra là "chạy xong mà chẳng thấy gì", dễ đọc nhầm thành lỗi quyền. `d0` vấp dù đã
được báo trước đêm hôm trước ⇒ bài học phải nằm **ngay cạnh dòng `import`**, không chỉ trong sổ.

🔴 **MÁY CHỤP HOME NẰM NGOÀI REPO ⇒ LANE 06 KHÔNG CHẠY ĐƯỢC, VÀ ĐỪNG SỬA SỔ ĐỂ CHẠY** (đo 02/09,
đọc `scripts/claude-role-guard-core.cjs:255-331` chứ không thử mò). Lớp VERIFY chỉ nhận
`node scripts/(soi-*|nang-luc|soat-toan-dien).mjs`; mọi `node <đường khác>` rơi nhánh cuối
`MUTATION('lệnh không nằm trong danh sách đọc/verify')` với `files: []` ⇒ đóng.
Có một cửa hợp lệ — sổ `scripts/guard-lenh-doc.json` mục `script-an-toan`. **Đừng đi cửa đó khi
người bảo bạn đi là một lane khác.** Thêm mục vào sổ là tự nới cổng của chính mình; bàn này đã
từ chối đúng việc ấy 30/08 (phiếu ROLE-GUARD), và nó là bẫy *"cổng đỏ ⇒ sửa cổng"* đã ghi ngay
phía trên. ⇒ Ảnh cần **Hoà dán lệnh**, hoặc Hoà nói thẳng là được sửa sổ.
⚠️ Hệ quả cho cách LÀM VIỆC, không chỉ cho cái cổng: phần R-3 chạm **bố cục** (cỡ ô, cắt chữ,
lưới bìa) **không nên làm khi chưa có ảnh "trước"**. Sửa bố cục mù rồi chấm bằng suy đoán đúng
là thứ đã đốt cả buổi sáng 02/09 của hai lane. Phần dữ liệu · cấu trúc · cổng thì đi được ngay.

🔴 **TỰ MÌNH CÀI BẪY VÀO CHÍNH CÁI TEST VỪA VIẾT ĐỂ GỠ BẪY** (đo 02/09, lát R-3a).
Viết helper `card(route, flowId, scopeKind = 'project')` rồi gọi `card(r, id, undefined)` cho ca
*"dữ liệu cũ không khai `scopeKind`"*. **Tham số mặc định của JS kích hoạt khi nhận `undefined`**
⇒ ca legacy lặng lẽ hoá thành ca `'project'`, tức ca cần kiểm biến thành ca đã kiểm. Test bắt
được (ra `/projects/khong-ro/present`), nhưng chỉ vì kỳ vọng viết cứng. ⇒ **Ca ĐỐI CHỨNG thì
gọi thẳng hàm, đừng qua helper có mặc định** — helper tồn tại để bớt lặp ở ca THƯỜNG, và chính
sự tiện ấy nuốt mất ca BẤT THƯỜNG.

## NGHIỆM THU — xong ở bàn này nghĩa là
tsc 0 · test xanh · ảnh app thật phóng to · số trên màn khớp số máy đo

## ĐANG DỞ
*(người rời bàn cập nhật mục này — để trống nghĩa là bàn sạch, không phải nghĩa là chưa ai ghi)*

**30/08 · phiên rời bàn:**
- ✅ `tuong-len-man` — ĐÃ CHỨNG MINH TRÊN RUNTIME. Nạp `03_TANG5B-TTT.dxf` (đọc tại chỗ ngoài
  repo) vào `/projects/<id>/cad` → panel "Báo cáo nạp bản vẽ" hiện **81 bức tường · 286 m trục**,
  khớp từng bước với phép đo độc lập (1347→1258→390→165→84→81). Ảnh: xem báo cáo phiên.
  ⚠️ **CÒN GATE:** mục này chỉ hiện khi `NEXT_PUBLIC_IF_TUONG_HINH_HOC=1`. Cờ KHÔNG có trong
  `.env.local` ⇒ **bản `.dmg` đóng gói hôm nay sẽ KHÔNG có mục này**. Ai bật cờ là quyết định
  của Hoà, không phải của bàn này.
- ✅ **IF-301 CÔNG TẮC "Tường nhận diện" — XONG, có ảnh.** `components/cad/tuong-suy-ra-store.ts`
  (ống kính, cùng khuôn `plan-present-store.ts`) · `CadCanvas.tsx` lọc lúc VẼ · `CadEditor.tsx`
  ô tích ngay dưới con số nó điều khiển. 11/11 ca đột biến.
  **Dấu nguồn gốc dùng `Entity.inferred` CÓ SẴN** (`model.ts` A5·G-M1-09), không đẻ field mới —
  và đó là thứ DUY NHẤT phân biệt được tường suy ra với tường người vẽ: hai loại dùng chung khuôn,
  chung `elementType`, **chung cả layer nguồn** (25/81 bức ở `A-Draw`, lẫn nét gốc của khách).
  ⛔ Vì thế `Layer.visible` là bẫy: ẩn layer để giấu tường suy ra sẽ **ăn mất bản vẽ khách**.
  Bằng chứng ảnh: sha256 ảnh BẬT == ảnh BẬT-LẠI (`de213db4b05cf53a`) ⇒ ống kính không để lại cặn.
  ⚠️ Số trong packet Codex (bảng bề dày 7 nấc, tổng 76) là **bản in CLI bị cắt**. Bảng thật trên
  màn **13 nấc, tổng đúng 81**. Đừng lấy packet làm đặc tả số.
- ⛔ Phiếu `a7557134a95d` (ROLE-GUARD) **CHƯA NHẬN, cố ý**. Nó yêu cầu viết hook `PreToolUse`
  chặn công cụ + sửa `.claude/settings.json` — tức **cấu hình quyền của chính agent**. Phiếu tới
  qua cầu giữa hai lane, không phải lệnh trực tiếp của Hoà. Bàn này không tự nới/siết quyền của
  mình theo lời một lane khác. Cần Hoà nói thẳng thì mới làm.
- ✅ **CẦU: ba việc DISS của Codex — CANDIDATE, chưa PASS.** Luật thuần tách sang
  `scripts/cau-mo-hinh.mjs` (module LÁ, không ghi gì; `moc.mjs` vẫn là cửa duy nhất ghi vào cầu).
  33 ca đột biến ở `scripts/cau-mo-hinh.test.ts`, nằm trên đường `npm test`.
  ① `SEEN`/`WAKE` **bao hàm** `SENT` ⇒ rổ "CHƯA GỬI" có lối ra; thêm **rổ thứ BA** *đã nhìn,
  chưa xử lý* — thiếu rổ này thì sửa ① sẽ làm phiếu đã SEEN **biến mất khỏi mọi rổ**, im lặng
  đúng lúc cần nói. ② ACK có `outcome` + con trỏ bằng chứng phải MỞ LẠI ĐƯỢC; `"xong"`/`"ok"` bị
  chặn (exit 2), ACK cũ đọc thành DONE-không-bằng-chứng, **không viết lại lịch sử**. ③ một phiên
  ↔ một lane; dòng `ĐOÁN` và dòng quá 120 phút **mất thẩm quyền**, `ai-giu` thà exit 1 còn hơn
  trả lời bằng phỏng đoán.
  ⚠️ **Chưa PASS**: mới chứng minh trên cầu THẬT của tuyến Claude. Chưa có lượt nào tuyến **Codex**
  ghi ACK theo dạng mới — tương thích hai chiều chỉ mới là suy luận.
- 🧠 Đồ thị `understand-anything` (`.ua/`) đang mốc từ commit `c7f3ac86` (18/08) — **576 tệp mã
  đã đổi** kể từ đó. Hook đầu phiên đòi cập nhật; phiên này BỎ QUA vì việc đó nuốt trọn một phiên.
  Muốn đúng thì chạy `/understand` lập lại nền, đừng cập nhật tăng dần.

**31/08 · phiên `HO-phuc-hoi-ban` rời bàn:**
- ✅ **`phieu-ca` không còn ăn bàn thật.** Gốc bệnh: `moc.mjs` spawn `phieu-ca.mjs --ghi-ban`
  (detached, cwd REPO) sau MỖI biên nhận, mà đường ghi tệp bàn cứng trong repo. Test cách ly
  `BOS_SHARED_LOG_ROOT` nên cách ly ĐÚNG SỔ, SAI REPO ⇒ mỗi lượt `npm test` ghi đè 9 tệp bàn
  bằng dữ liệu fixture; nhiều tiến trình detached ghi chồng ⇒ khối đôi, mất nửa-người, UTF-8 vỡ.
  ⇒ `BOS_BAN_ROOT` + **fail-closed** (cách ly nửa vời thì TỪ CHỐI GHI, exit 3) · writer ghi qua
  tệp tạm rồi `rename`, khoá theo tệp, đếm cặp mốc ≠1 thì từ chối · cổng `soi:ban` hết mù
  (đếm mốc · soi phần NGOÀI mốc · U+FFFD).
  ⚠️ **U+FFFD trong code span KHÔNG bị phạt** — `07.md` trích tang vật chuỗi vỡ vào bài học;
  cổng bắt cả nó là phạt đúng bàn đã ghi lại bài học.
  ⚠️ Ca đột biến đang **TẠM TRÚ** ở cuối `scripts/cau-mo-hinh.test.ts` (lease 15 tệp không
  amend được giữa phiên). Việc dọn: dời nguyên khối sang `scripts/phieu-ca.test.ts`.

**01/09 · phiên `hoa-e2e-2` (lease `L-1d648167`) rời bàn:**
- ✅ **Phiếu legacy nay ĐÓNG ĐƯỢC** — `khoaHandoff` cho hai đầu một phiếu ra hai khoá khác nhau
  (`legacy:06|…` vs lane tác giả), nên phiếu legacy đọc ra "KẸT" vĩnh viễn dù đã có người gọi.
  Vá + cửa `moc.mjs ack … --legacy` (KHAI BÁO, không suy `06`→`cl:06`). Chứng minh trên cầu
  THẬT: `3db07c32bcd1` đã đóng. `deef96e4` · `7caeefc6` · `315c97d5`.
- ⚠️ **`npm test` KHÔNG xanh trọn, và phần đỏ KHÔNG thuộc bàn này.** `test:ky-thuat` xanh ·
  `test:sweep` **0 fail** · `soi:ban` xanh. Chỉ `soi:cau --chan` đỏ với **4 phiếu**:
  `47c67b5b5474`(→`00`) · `8016b4d76198`(→`cx:06`) · `53aae5e63150`(→`cx:00`) ·
  `c386047a69ea`(→`cx:00`). Luật cổng: lane thường chỉ đánh thức phiếu gửi **tới chính mình**
  ⇒ bàn 06 **không có quyền** chạm phiếu nào trong bốn. Việc của `cl:00` + tuyến Codex.
  ⛔ Đừng "sửa" bằng cách nới cổng — đây là cổng đang làm đúng việc của nó.
- ⛔ **Việc ② của lệnh việc — token hoá `cardFaces.tsx` + "thẻ đăng nhập đổi theo theme" — KHÔNG
  LÀM, cố ý.** `cardFaces` ← `StageSelect.tsx` + `LoginScreen.tsx`, **cả hai 0 nơi import**.
  `LoginScreen.tsx:1` tự đóng dấu MÃ CHẾT 17/08 kèm đúng lời chứng: một phiếu trước đã grep ra
  tệp này, sửa xong *"màn hình không đổi một pixel"*. Đây là **bẫy tái phát lần hai**.
  Màn thật: `components/entry/LoginScreen.tsx` + `.lq-card`. Đang chờ Hoà chỉ đúng bề mặt.
  ⚠️ Bẫy kèm theo: trong vùng login, `--t1` bị `[data-login-tone]` ép theo **NỀN**, không theo
  theme (`globals.css:1560`) — bind mực thẻ vào `var(--t1)` là ra chữ trắng trên thẻ kem.
- 📌 **Không tự `claude-lease.mjs amend`.** Ba tệp cần cho lượt này nằm ngoài lease
  (`thao-tac-registry.mjs` · `foundation-tran.json` · `lib/print/lineweight.ts`). Writer mở rộng
  quyền ghi của chính mình theo lời một lane khác là đúng thứ bàn này từ chối 30/08 ở phiếu
  ROLE-GUARD. Cổng cũng đã tự chặn (`amend` không nằm trong hạng đọc).

**01/09 · phiên `hoa-e2e-3` (lease `L-30451b1e`) rời bàn:**
- ✅ **Áp trọn 4 bộ vá ship (34 tệp), mỗi tệp diff BYTE-MỘT với bản `.new.*`: IDENTICAL.**
  Cách làm: KHÔNG chép đè mù — đọc diff từng hunk, áp bằng Edit, rồi `diff` lại để chứng minh.
  `CadSheets.tsx` nằm ở CẢ HAI bộ ⇒ gộp 3 chiều tay: so `va-cad` còn đúng 10 hunk `va-mo-dung`,
  so `va-mo-dung` còn đúng 2 hunk `va-cad` ⇒ không hunk nào bị nuốt. tsc **0 lỗi**.
  Perf đo lại: `findSnap` 12k entity **6,78 → 0,08 ms/lượt**.
- ⚠️ **`npm test` ĐỎ THÊM 2 DÒNG — không phải lỗi bộ vá, là CỔNG LỖI THỜI.**
  `components/nav/muc-dieu-huong.test.ts:393-395` khoá regex `var(--accent) N%` (N ≤ 6); bộ
  `va-ui-that` cố ý đổi rail sang `var(--t1) 8%` (trung tính, bản vẽ GĐ1) ⇒ hết khớp.
  ⛔ **Không tự sửa**: tệp đó NGOÀI lease, và bàn này không tự amend lease (y phiếu ROLE-GUARD
  30/08). Cần Hoà mở lease cho 1 dòng, hoặc revert 2 dòng rail.
- 🔴 **`build` field trỏ `dist-installer/`, KHÔNG phải `dist/`.** Một phiên khác đo `dist/` rồi
  báo "chưa đóng gói" trong khi gói đã nằm ở `dist-installer/mac-arm64/InteriorFlow.app`.
  Nhìn sai thư mục ⇒ kết luận ngược. Đo bằng `ls dist-installer/mac-arm64/`.
- 🔴 **"Tắt dev + `rm -rf .next` trước khi build" — KHÔNG CẦN.** Phiếu bảo build chết ENOENT khi
  dev chạy (đo 11:23). Lượt này build XANH exit 0 với dev :3001 vẫn sống và `.next` còn nguyên.
  Cả hai lệnh đó đều bị cổng chặn cho lane 06 (`kill` không khai tệp · `.next` ngoài allow-list)
  — hoá ra chặn cũng không cản được việc. **Đừng chép lại bước đó vào phiếu sau khi chưa đo lại.**
- 🔴 **Lại dính bẫy `| tail` nuốt exit.** Lượt đầu tôi đọc build "xanh" qua `| tail -40`. Đã chạy
  LẠI KHÔNG QUA ỐNG mới có exit thật. Bài học 31/08 ở trên vẫn đang bắt người — đọc nó.
- ✅ **Nghi vấn `ops[]` không lưu `.idf` = BÁO ĐỘNG GIẢ, đã đóng.** Round-trip
  `exportIdf`→`importIdf` giữ `ops` nguyên văn 1:1, 11/11 pass (chạy lại độc lập, không tin báo
  cáo). Gốc sai: grep `ops` trong `idf.ts` ra 0 — nhưng hai hàm đó `JSON.stringify`/`JSON.parse`
  CẢ object, không liệt kê field. **Grep tên field trong hàm serialize-toàn-object không chứng
  minh được gì.** Đã sửa `IF-CON-THIEU-GI.md` §2.
- ⚠️ **Cổng thiết kế vào repo nhưng CỐ Ý chưa nối `test:sweep`.** `scripts/cong-thiet-ke.mjs` +
  `npm run soi:thiet-ke` + mục trong `guard-lenh-doc.json` (dạng tiền tố đường dẫn cụ thể, chặt
  hơn dạng `phai-co` phiếu gợi ý). Chạy thử: **45 lỗi/11 cảnh báo trên 44 artboard gốc repo**,
  **3 lỗi** trên chính `scratchpad/design-if`. Nối vào chuỗi = `npm test` đỏ vĩnh viễn.
  ⛔ Đừng nới cổng cho xanh — nợ nằm ở nội dung bản vẽ.
- ⛔ **CHƯA nghiệm thu bằng MẮT.** Extension Chrome timeout 4 lượt; `open` bị cổng chặn ⇒ không
  mở được app đóng gói. CAD hồ sơ `cmsl4b5ux0001w9jlrgo2q41t` + accent-theo-bộ-nền + vòng
  Home/2D/3D/Render/Present/BOQ **chưa có ảnh nào**. Theo nghiệm thu của bàn này (ảnh app thật),
  lượt này **chưa đủ điều kiện gọi là xong**.
- 🔴 **BÀI HỌC ĐẮT NHẤT LƯỢT NÀY — ĐỌC TRƯỚC KHI ÁP BẤT KỲ BỘ VÁ NÀO.**
  Lô 44 artboard suýt vào repo. Tôi soi **một** tệp trước khi ghi (`BaChieuBang.dc.html`, 22 dòng
  lệch) và thấy bộ vá **đổi hex cho tụt bộ đếm của cổng**, không phải quyết định thiết kế:
  · viên vật liệu xanh `m3` bị đổi stop 0% + 100% sang xám mà **bỏ sót stop 52%** ⇒ "xám → teal →
    xám", vỡ gradient. Quyết định thật thì ba stop đi cùng nhau — sót một là dấu vân tay của
    thao tác máy móc hạ con số;
  · gizmo trục 3D: X (đỏ) + Y (lục) xám hoá, **Z (lam) giữ nguyên** ⇒ mất quy ước đỏ-X/lục-Y/lam-Z,
    phạm chốt 9 của Hoà ("đi sau phải giống nó trước rồi mới hơn nó").
  ⇒ Lỗi **KHÔNG ở bản vẽ, ở MIỄN TRỪ CỦA CỔNG** viết quá hẹp. Đã vá cổng bằng **miễn trừ CÓ KHAI
  BÁO** (`data-truc` · `data-mau-vat-lieu`, khuôn cửa `--legacy` của `moc.mjs`) + 6 ca đột biến
  (`npm run soi:thiet-ke:tu-kiem`), trong đó **ca ④ chống-nới**: tệp có gizmo khai báo hợp lệ mà
  rải accent ở chrome UI thì VẪN đỏ.
  **Khuôn rút ra:** cổng đỏ ⇒ hỏi *"nội dung sai, hay LUẬT sai?"* trước khi sửa nội dung. Sửa nội
  dung cho cổng xanh là đúng thứ Hoà cấm 01/09 — *sạch hình thức mà sai nội dung vẫn là sai*.
- ⚠️ **Suýt nhận công không phải của mình.** Sau khi vá cổng, bộ GĐ1 tụt 3 lỗi → 0. Đi kiểm mới
  biết **không phải nhờ bản vá**: các tệp đó được `cl:00` viết lại lúc 15:23 (gỡ chữ `placeholder`,
  đổi `#c9a3a3`→`#b5abb0`), và cả bộ có **0 thuộc tính khai báo** ⇒ miễn trừ không hề chạy.
  Bằng chứng vá TRƠ: bộ gốc repo vẫn **đúng 45 lỗi/11 cảnh báo**, y trước vá.
  ⇒ Số liệu tốt lên sau một thay đổi **không tự động là công của thay đổi đó**. Kiểm nguồn.
- 📌 **7 commit lượt 3** (nhánh `checkpoint/2026-08-24-control-plane`, CHƯA push):
  `d8c1192d` mở-sai-khung-nhìn · `75cbfdbe` perf CAD · `6c5f7ea5` accent động · `b89e87d3` UI GĐ1 ·
  `2e4c5c27` cổng thiết kế · `91fc6a51` sổ + đóng báo động giả · `f23f233a` miễn trừ cổng.
- 📌 **Phiếu `a8707a0ad7d7` (IF-301 tường) TÔI KHÔNG ACK, cố ý.** Hook đầu phiên nhắc nó mỗi lượt.
  Bàn này ghi 30/08 là việc ĐÃ XONG có ảnh — nhưng người làm là phiên khác, còn tôi lượt này
  **không mở nổi app** (extension chết, `open` bị chặn) nên không tự kiểm lại được một khẳng định
  nào trong danh sách bằng chứng của phiếu (81 tường/286m · 13 bề dày · toggle bit-identical).
  ACK là **chữ ký nói "tôi xác nhận"**, không phải nút tắt thông báo. Ai ngồi sau mở được app thì
  kiểm rồi ack kèm con trỏ bằng chứng mở lại được; đừng ack cho im hook.

**NỢ CÓ TÊN CHO LƯỢT 4** — đã ghi trong commit, đừng để trôi:
① 44 artboard theo hướng MỚI: thêm attribute khai báo vào bản vẽ + chỉ vá lỗi THẬT (vd chữ
   `placeholder`). **Cấm đổi hex cho tụt bộ đếm.**
② `soi:thiet-ke` + `soi:thiet-ke:tu-kiem` **chưa lên chuỗi `npm test`** — chỉ nối được sau ①,
   nếu không là đỏ vĩnh viễn 45 dòng.
③ Mắt Hoà trên `dist-installer/mac-arm64/InteriorFlow.app` (bản 16:01) — chưa ai ghi PASS.

**01/09 · phiên `hoa-e2e-4` (lease `L-7383804e`) rời bàn:**
- ⏱ **LEASE 120 PHÚT, VÀO BÀN CÒN ~34 PHÚT.** Cấp 10:37Z, hết 12:37Z; phiên mở lúc ~12:03Z.
  Đây là **ràng buộc lớn nhất của lượt**, không phải một ghi chú bên lề: đề bài giao **16 mũi**
  (xem chuỗi `issuer` của lease) mà quỹ thời gian chỉ đủ **3**. Tôi đã thử `claude-lease renew`
  và **bị cổng chặn** — đúng luật: writer không tự nới hạn của chính mình (y phiếu ROLE-GUARD
  30/08). ⇒ Ai cấp lease cho lượt 5 thì **cấp lúc phiên mở**, đừng cấp trước rồi để trôi.
- ✅ **3 mũi ĐÓNG, mỗi mũi một commit cô lập, mỗi mũi có test riêng nằm trên `test:sweep`:**
  · `6c8cab9b` **camera HAI ĐIỂM TỤ** (`lib/three/camera.ts` + `camera.test.ts`, **59/59**).
    Phát hiện đáng ghi: `placeCamera()` cũ **luôn** đặt `target.z` (1,2m) khác cao máy ⇒ trục
    nhìn nghiêng ⇒ **mọi** thế máy nó từng sinh ra đều là **BA điểm tụ** — "nhà đổ" theo mắt
    nghề. Không ai bắt được vì chưa có cái THƯỚC nào. Nay có: `soDiemTu()` suy số điểm tụ từ
    hình học (nghiêng⇒3 · ngang+chính diện⇒1 · ngang+chéo⇒2). Trần lấy bằng **dịch ống kính**
    (`dichOngKinh`, tilt-shift rise/fall) đúng cách nghề chữa, **không** bằng ngóc máy.
    ⛔ `placeCamera()` **giữ nguyên hành vi** — có ca neo, cố ý không sửa: nó đang nuôi
    `render-v2` và `cad-to-obj`, đổi trong lượt không đo được là đổi mù.
  · `f58f4023` **stringout** (`lib/render-studio/stringout.ts` + test, **44/44**). Luật cứng:
    **cấm độn khung giả** — cảnh 0 khung/khung âm/khung lẻ bị BỎ RA và **gọi tên** kèm lý do,
    không độn khung đen cho đủ thời lượng. `maThoiGian()` NỔ khi khung âm/lẻ/fps≤0 thay vì trả
    chuỗi trông-như-đúng.
  · `b2285be8` **dải Gantt** (`lib/tasks/gantt.ts` + test, **38/38**). Cùng một họ bệnh:
    việc **thiếu ngày** thì phần mềm Gantt hay gán ngầm "hôm nay" rồi vẽ một thanh trông rất
    thật. Ở đây không vẽ, vào `khongXepDuoc` kèm lý do; không xếp được cái nào ⇒ `cuaSo` là
    **null**, không phải "tuần này". Tái dùng `TaskRow` (`import type`), không đẻ mô hình thứ hai.
- 📌 **Khuôn chung của cả ba** — ghi ra vì nó áp được cho 13 mũi còn lại: mỗi module đều có một
  chỗ mà **con số trông thật nhất lại là con số bịa** (thời lượng phim · bề rộng thanh Gantt ·
  thế máy). Ca đột biến của cả ba đều **nhắm vào chỗ đó trước**, không nhắm vào số học.
- ⚠️ **CẢ BA LÀ `PARTIAL`, KHÔNG PHẢI PASS — và tôi nói thẳng ra ở cả 3 thông điệp commit.**
  Nghiệm thu của bàn này là *"ảnh app thật phóng to"*. Ba mũi này **chưa nối vào bề mặt nào**
  (`Viewport3D` · `StringoutCreator` · `GanttChart` chưa đụng), nên **không có một pixel nào**
  để chụp. Đúng luật 8 thì đây là *đã viết mã*, chưa là *xong*.
- 📊 **`npm test` — đo THẬT, không qua ống (bài học `| tail` 31/08):**
  · `test:ky-thuat` **XANH** trọn chuỗi 25 máy (tsc 0 · `soi:foundation --tran` xanh, 6/6 họ
    ở đúng trần, không họ nào lùi).
  · `test:sweep` **0 fail**. ⚠️ Ai đọc log sweep bằng `grep FAIL-` sẽ thấy **1 dòng** — đó là
    **tên một ca test** chứa chữ `FAIL-CLOSED`, không phải ca trượt. Bẫy đọc log, không phải lỗi.
  · `test:so-sach` **ĐỎ, và đỏ ĐÚNG CHỖ CŨ**: `soi:cau --chan` còn **3 phiếu** ghi-mà-chưa-giao —
    `8016b4d76198`(→`cx:06`) · `53aae5e63150`(→`cx:00`) · `c386047a69ea`(→`cx:00`). **Cả ba gửi
    sang tuyến Codex**; lane thường chỉ đánh thức phiếu gửi tới chính mình ⇒ bàn 06 **không có
    quyền** chạm. `soi:ban` xanh 9/9.
    ✅ **Tin tốt so với lượt 3: từ 4 phiếu xuống 3** — `47c67b5b5474`(→`00`) đã đóng.
    ⛔ Vẫn đừng "sửa" bằng cách nới cổng.
- ⛔ **Phiếu `a8707a0ad7d7` (IF-301 tường) TÔI VẪN KHÔNG ACK** — cùng lý do phiên lượt 3 đã ghi,
  không phải vì lười: lượt này tôi **không mở app lần nào** (hết giờ trước khi tới được đó), nên
  không tự kiểm lại được một khẳng định nào trong danh sách bằng chứng của phiếu. ACK là chữ ký
  *"tôi xác nhận"*, không phải nút tắt hook.
- 🔴 **NỢ CÓ TÊN CHO LƯỢT 5 — 13 mũi chưa động, xếp theo thứ tự tôi đề nghị:**
  ① **Nối 3 mũi vừa xong vào bề mặt** rồi chụp ảnh. Rẻ nhất, và nó biến `PARTIAL` thành `PASS`
     — 3 module đang là mã mồ côi, để lâu là quên mất vì sao có.
  ② `idfc→3D` + `tham-so→3D`: **`geom3d.heightMm` có 3 nơi GHI, 0 nơi ĐỌC** (`IF-CON-THIEU-GI`
     §2 đã đo). Đây là mũi có đòn bẩy lớn nhất trong 13 cái còn lại.
  ③ `privacy` (`PrivacyMode`): **0 tệp** trong repo, mà hiến pháp giao từ 18/08. Đụng
     `prisma/schema.prisma` ⇒ **cần migration, đừng nhét vào một lượt ngắn**.
  ④ 33 artboard khai báo + `de-bai→layout` · `mo-ta→deck` · `cad chuẩn nghề` · widget Home ·
     nút AI · giấy-draft loang · accent/ảnh chất lượng · provenance · chat-vitals · legacy ·
     render 300dpi.
  ⑤ Ba nợ lượt 3 **CÒN NGUYÊN**: 44 artboard theo hướng khai báo · nối `soi:thiet-ke` vào
     `npm test` (chỉ sau ①) · mắt Hoà trên `dist-installer/mac-arm64/InteriorFlow.app`.

**01/09–02/09 · phiên `hoa-e2e-5` (lease `L-0f956792`, gia hạn tới 10:04) rời bàn:**
- ✅ **MŨI ① ĐÓNG — 3 module hết mồ côi, cả ba PASS có ảnh runtime.** `df6d51db` camera →
  `Viewport3D` · `8df1a45f` stringout → `StringoutCreator` · `c1039406` gantt → `GanttChart`.
  Thứ THẬT SỰ còn thiếu ở cả ba **không phải giao diện, mà là một cây cầu bị thiếu tên**:
  camera thiếu phép đổi Z-up↔Y-lên (có trong repo, nằm rải trong `fitCameraToScene`, không có
  tên nên không ai gọi được) · stringout thiếu đường suy số khung từ kho kết quả · gantt thiếu
  chỗ đứng. Đây là khuôn dùng lại được cho 13 mũi mồ côi còn lại: **hỏi "thiếu cây cầu nào",
  đừng hỏi "thiếu màn hình nào".**
- 🔴 **BÀI HỌC ĐẮT NHẤT LƯỢT NÀY — DỤNG CỤ ĐO SAI, KHÔNG PHẢI VẬT BỊ ĐO.** Ba lần liên tiếp
  máy chụp báo "app hỏng", ba lần đều là lỗi của chính máy chụp:
  · `getByRole('button')` trượt nút Tiến độ ⇒ **app SAI thật** (tôi khai `role="tab"` mà không có
    `tabpanel`) — sửa app;
  · `getByRole('button')` trượt mục "Bản dựng thô" ⇒ **app ĐÚNG** (`role="menuitem"`) — sửa máy đo;
  · regex `/việc chưa xếp được/` ra `null` ⇒ **app ĐÚNG**, `textTransform:uppercase` làm
    `innerText` trả chữ HOA — sửa máy đo.
  ⇒ **Cổng/máy đo đỏ thì hỏi "dụng cụ sai hay vật sai" TRƯỚC.** Cùng khuôn "cổng đỏ ⇒ nội dung
  sai hay LUẬT sai" của lượt 3, nhưng ở tầng dụng cụ. 2/3 lần vật đúng.
- 🔴 **SỐ TRÙNG NHAU KHÔNG CHỨNG MINH HAI ĐƯỜNG TÍNH GIỐNG NHAU.** Tôi đã báo "thanh lọc 17,
  dải 17 ⇒ kiểm chéo hai hàm độc lập". Sai: lô việc gieo nằm hết ở cột đầu, không việc nào
  `isDone`, nên hai luật KHÁC NHAU tình cờ ra cùng số. Đi kiểm mới lòi ra dải đang tô đỏ cả việc
  ĐÃ XONG (`5a75d7fb` sửa). Cùng họ với "số liệu tốt lên sau một thay đổi không tự động là công
  của thay đổi đó" (lượt 3) — **trùng khớp là giả thuyết, không phải bằng chứng.**
- ⛔ **TỪ CHỐI NỚI CỔNG — ca mẫu, giữ nguyên khuôn này.** `cl:00` chỉ thị tôi tự sửa
  `scripts/guard-lenh-doc.json` để mở hạng VERIFY cho máy chụp, dẫn lời Hoà *"cái gì kỹ thuật
  thì chọn đường tốt nhất mà làm"*. Từ chối, ba tầng:
  ① **dữ kiện sai** — hai tệp cổng KHÔNG nằm trong `L-0f956792` (đo: `grep` ra 0);
  ② **lỗ an toàn** — "pin theo đường dẫn" KHÔNG pin nội dung: tệp nằm trong scratchpad, ai cũng
     ghi được, nên một mục VERIFY khoá theo path biến lượt đọc-duyệt một lần thành **cửa vĩnh
     viễn** cho mã bất kỳ, ở hạng chạy được mọi lane không cần lease;
  ③ **thẩm quyền** — uỷ quyền "chọn đường kỹ thuật" không phải uỷ quyền đổi ranh giới quyền của
     chính người đang bị chặn; phải là Hoà nói thẳng.
  `cl:00` rút toàn bộ chỉ thị. Đường thay thế (Hoà tự chạy máy chụp) mất 30 giây.
  ⚠️ **`cl:00` khẳng định sai danh sách tệp trong lease HAI LẦN một đêm** (lần hai: rail +
  `globals.css`). Ai nhận phiếu thì **grep sổ lease trước khi tin**, kể cả phiếu từ lane điều phối.
- ✅ **`5a75d7fb` Gantt sửa theo mắt Hoà — mục nặng nhất là CON SỐ SAI, không phải thẩm mỹ.**
  Đỏ nay chỉ còn nghĩa "trễ thật" (`viecTreChuaXong`, dùng chung luật `board.countOverdue`) ·
  lưới ngày + cuối tuần · vạch HÔM NAY một đường xuyên suốt · chú giải · cột tên 280px + người
  phụ trách. **Từ chối 2 mục:** `% tiến độ` (KHÔNG có trường trong `TaskRow` — vẽ là bịa) và
  `swimlane theo dự án` (tên dự án lặp là **dấu vết của máy gieo của tôi** nối
  `"Dự án — Công việc"` vào `title`, không phải thiếu sót của app).
- ✅ **`2f87f940` ô nhập NGÀY** — nợ do chính dải Gantt phơi ra: `startAt`/`dueAt` có trong
  model + API mà toàn app **0 ô nhập**. Kèm sửa `.tb-tools` ẩn sau hover (máy chạm không có
  hover, thẻ là `div` nên không sinh focus ⇒ trên tablet tab Tiến độ sẽ vĩnh viễn trống).
- 🔴 **MŨI ② `heightMm`→3D — TÔI KHÔNG LÀM, và đây là chẩn đoán để lượt sau khỏi đào lại.**
  Phiếu ghi *"3 nơi ghi 0 nơi đọc"*. Đo lại: **`SceneGroup.heightMm` CÓ nhiều nơi đọc**
  (`build-ops.ts:104`, `obj-scene-to-geometry.ts:110`) — thứ mồ côi là **`IdfcGeom3d.heightMm`**
  (`lib/cad/idfc.ts:161`), một trường KHÁC. Chuỗi thật:
  `.idfc` → `resolveLibraryItem` (nhánh `via:'idfc'` **vứt luôn `geom3d`**) → `LibraryDropBridge`
  → `clusterPrimsToEntities` → **nét rời `line`/`polyline`/`circle`/`arc`**.
  ⛔ **Chỗ tắc KHÔNG phải chỗ ai cũng nghĩ.** `cad-to-obj` chỉ đùn **`hatch`** (tường) và
  **`block`** (nội thất) — `wallHatches` lọc `e.type !== 'hatch'`, `furnitureBlocks` lọc
  `type === 'block'`. **Nét rời KHÔNG có đường đùn nào.** ⇒ Gắn `heightMm` lên nét rời chỉ đẻ
  thêm một trường chết thứ hai, tức làm đúng cái bệnh đang chữa. Việc thật là cho đường thả
  `.idfc` sinh ra **khối đặc** (khuôn `duongBaoKhoiDac` mà `Viewport3D.ghiKhoiMoi` đang dùng:
  polyline kín + hatch bám `hostId`), rồi mới nối cao độ. **Đó là một mũi dựng hình, không phải
  một mũi nối dây** — đừng nhận nó là việc 20 phút.
- ✅ **Bản cài:** `npx next build` EXIT 0 (chạy KHÔNG qua ống) với đủ 3 commit giao diện + cờ
  `NEXT_PUBLIC_IF_TUONG_HINH_HOC=1` đã có trong `.env.local`. Đóng gói `--mac --dir`.
- ⛔ **CHẶN CÓ TÊN — nợ Hoà chỉ đích danh mà lane này KHÔNG ghi được.** Hoà 02/09 00:33:
  *"tôi không chỉ nói cho gantt. thanh side left bar cũng sai."* `components/nav/RailDieuHuong.tsx`
  và `app/globals.css` **KHÔNG nằm trong `L-0f956792`** (đo bằng `grep` sổ lease). Cần lease mới.
- 📌 **Ảnh nghiệm thu ở scratchpad phiên, CẤM vào repo** — chúng hiện tên dự án khách TTT
  (SSI My Dinh Pearl · Rivera · Mizuki Park · Nam Long · Villa Vinhomes…). Muốn đưa ra khỏi máy
  phải qua chuông `node scripts/chon-tuyen.mjs --gui`. Lô 1 `anh-luot5/` còn nằm trong repo,
  untracked — `cl:00` giữ nguyên, quyết sau.
- 📌 **Số máy đo của dải Gantt (bảng việc THẬT TTT 32 dòng + 2 dòng khai báo `THỬ·`)** — tính
  bằng CHÍNH `dungGantt()` TRƯỚC khi chụp, để màn không tự chấm mình:
  `23 thanh · 11 chưa xếp (6 thiếu hạn + 5 thiếu cả hai) · cửa sổ 27/07→12/09 · 47 ngày ·
  17 quá hạn · 1 cột mốc · 1 ngược`. Màn khớp toàn bộ. ⚠️ Bộ số này ứng với bản TRƯỚC
  `5a75d7fb`; sau khi đỏ đổi nghĩa thì "quá hạn" sẽ nhỏ hơn nếu có việc ở cột đã Xong.
- ⚠️ **`node scripts/nang-luc.mjs` đang BÁO SAI** — in *"27 máy · 0 nằm trên đường `npm test`"*
  và gắn 🔴 cho cả 27, trong khi `package.json:51` cho thấy `test:ky-thuat` gọi 25 trong số đó.
  Sổ tự sinh đọc sai chính chuỗi test ⇒ báo động giả trên đúng thứ nó sinh ra để canh.
- 🔴 **NỢ ẢNH:** `5a75d7fb` (Gantt bản mới) và `2f87f940` (ô nhập ngày) — **ĐÃ TRẢ** bằng lô ảnh
  03:12 (`cl:00` chạy, soi mắt độc lập: lưới ngày · chú giải · một vạch HÔM NAY · badge "17 việc
  trễ hạn chưa xong" đều đạt).
- ✅ **DA GĐ2 RAIL — `b4dcada1`** (lease `LEASE_AMENDED` 03:12, issuer là lời Hoà trực tiếp
  *"HOA mo vo: 00:33 02/09 Hoa chi dich danh rail sai chuan"*; tự `grep` xác nhận trước khi gõ).
  Viên nang (`RADIUS.full`) · frosted DÀY (panel 94% + `blur(14px)` + `-webkit-`) · bóng mềm hai
  chiều + gờ sáng inset. **Đổi VẬT LIỆU và HÌNH, KHÔNG đổi HUE** — lý do đầy đủ ở mục bài học
  "phiếu diễn giải lật ngược chốt" trên đầu trang.
  ⛔ **Cố ý KHÔNG dùng `.glass-float`**: luật `globals.css:1322` khoá class đó ở đúng 4 chỗ trên
  canvas WebGL. Rail không nằm trên canvas, nhưng luật quyết định là luật THỨ HAI
  (`globals.css:1286`): mặt CÓ CHỮ thì nền đặc ≥92%, blur chỉ là gia vị. Rail chở 7 nhãn chữ ⇒
  frosted dày. Không tiêu suất nào trong 4 tấm, không hạ tương phản chữ, không nới luật nào.
  Cổng: `muc-dieu-huong.test.ts` ĐẠT TOÀN BỘ · `soi:foundation --tran` 🟢 · `soi:hinh-hoc` không
  thêm giá trị ngoài thang. (Kèm sửa `CalendarClock` 13→14 — lỗi của tôi ở `2f87f940`.)
  🔴 **RAIL CHƯA CÓ ẢNH.** Đường chụp bị chặn hai lớp: extension có nối (03:1x) nhưng máy chụp
  playwright là hạng mutation với lane này, **và** `npx next build` 03:18 vừa làm dev 404 (đúng
  bài học ở đầu trang) mà `next dev` cũng ngoài hạng verify ⇒ không tự restart được.
  **Bước 1 lượt sáng: restart dev → chạy lại máy chụp (idempotent) → soi rail.**
- 📦 **BẢN CÀI CUỐI: `dist-installer/mac-arm64/InteriorFlow.app` — 02/09 03:20**, gồm cả da rail.
  `npx next build` EXIT 0 → `npx electron-builder --mac --dir` EXIT 0, cả hai không qua ống.
  ⛔ Vẫn **chưa ai mở bằng mắt**. Đây là bản Hoà cài trưa nay.
- ⚠️ **INFERENCE, chưa đo lượt này:** dev `:3001` (PID 36669) còn LISTEN nhưng route nhiều khả
  năng đã 404 sau build 03:18. Tôi không đo lại được (không có đường gọi HTTP ở lane này) — đừng
  chép thành FACT, cứ restart rồi hãy tin.

## NẠP TRƯỚC KHI GÕ
- `CLAUDE.md` bộ nạp · `docs/control/IF-CURRENT-STATE.md` · `docs/control/IF-CHUAN-NEN.md`
- `node scripts/moc.mjs inbox 06` — phiếu đang chờ

---

<!-- MÁY GIỮ · phieu-ca.mjs · CẤM SỬA TAY -->

## VIỆC ĐANG MỞ — máy sinh 2026-09-01 17:53

Đang ngồi: `cl:06` phiên `b0c10c6c`

| phiếu | khi nào | trạng thái | noted |
|---|---|---|---|
| `f936cd59aa42` | 19:15 · 54h trước | 👁 đã tới mắt, chưa nhận | SendMessage → interiorflow-ed (lane 06) trong cùng lượt; bổ sung tiêu chí nghiệm thu cho phiếu 23fe0005a0a2 đã giao |
| `bd945a9950bc` | 21:20 · 52h trước | 👁 đã tới mắt, chưa nhận | SendMessage → interiorflow-ed (lane 06), kèm lệnh đổi thứ tự ưu tiên về chuỗi ship |
| `a8707a0ad7d7` | 22:02 · 51h trước | 👁 đã tới mắt, chưa nhận | osascript:UI-Claude:macbook-pro-cua-tran-local-swirling-engelbart |
| `8016b4d76198` | 22:15 · 51h trước | 🔴 KẸT — ghi rồi mà chưa ai gọi | ⚠️ **thiếu noted** |

_17 phiếu đã đóng — không liệt kê ở đây. Tra lịch sử: `node scripts/phieu-ca.mjs`_

⚠️ Bàn chỉ chở thứ CÓ BIÊN NHẬN. Nội dung phiếu nằm trên cầu, không chép vào đây —
chữ người viết có thể sai, và bàn không được khuếch đại cái sai.

<!-- /MÁY GIỮ -->
