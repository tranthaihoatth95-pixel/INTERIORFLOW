# BÀN `06` · BUILD · thi công mã

> Ngồi vào bàn này thì đọc hết trang này **trước khi gõ**. Rời bàn thì cập nhật mục **ĐANG DỞ**.
> Kiến thức nằm ở bàn, không nằm ở người ngồi — xem `docs/control/ban/README.md`.

## VAI
**PHIÊN CODE CHÍNH của tuyến Claude** (chốt 30/08). Bút mã sản xuất ở đây, không ở MAIN.
viết mã · sửa lỗi · dựng bề mặt · chạy test · **chứng minh trên runtime**

Codex bên kia **không thi công** — họ plan và viết phiếu; MAIN phân rã rồi giao xuống đây.
Xem `IF-CANONICAL` §8c.

## CẤM
- copy tệp DXF khách vào repo
- thêm import vào Web Worker mà chưa chạy `soi:worker`

## BÀI HỌC ĐÃ TRẢ GIÁ — đọc để không trả lại
Worker chết IM vì `window is not defined` — chuỗi import tĩnh kéo store vào worker, React Refresh chạm window ở phạm vi module. Guard `typeof window` KHÔNG cứu được.
Cổng: `npm run soi:worker`.
Đang dở: hiện 81 tường (286,0 m) lên màn — máy đọc được rồi, chưa mặt nào đọc.

## NGHIỆM THU — xong ở bàn này nghĩa là
tsc 0 · test xanh · ảnh app thật phóng to · số trên màn khớp số máy đo

## ĐANG DỞ
*(người rời bàn cập nhật mục này — để trống nghĩa là bàn sạch, không phải nghĩa là chưa ai ghi)*

**30/08 · phiên rời bàn:**
- ✅ `tuong-len-man` — ĐÃ CHỨNG MINH TRÊN RUNTIME. Nạp `03_TANG5B-TTT.dxf` (đọc tại chỗ ngoài
  repo) vào `/projects/<id>/cad` → panel "Báo cáo nạp bản vẽ" hiện **81 bức tường · 286 m trục**,
  khớp từng bước với phép đo độc lập (1347→1258→390→165→84→81). Ảnh: xem báo cáo phiên.
  ⚠️ **CÒN GATE:** mục này chỉ hiện khi `NEXT_PUBLIC_IF_TUONG_HINH_HOC=1`. Cờ KHÔNG có trong
  `.env.local` ⇒ **bản `.dmg` đóng gói hôm nay sẽ KHÔNG có mục này**. Ai bật cờ là quyết định
  của Hoà, không phải của bàn này.
- ✅ **IF-301 CÔNG TẮC "Tường nhận diện" — XONG, có ảnh.** `components/cad/tuong-suy-ra-store.ts`
  (ống kính, cùng khuôn `plan-present-store.ts`) · `CadCanvas.tsx` lọc lúc VẼ · `CadEditor.tsx`
  ô tích ngay dưới con số nó điều khiển. 11/11 ca đột biến.
  **Dấu nguồn gốc dùng `Entity.inferred` CÓ SẴN** (`model.ts` A5·G-M1-09), không đẻ field mới —
  và đó là thứ DUY NHẤT phân biệt được tường suy ra với tường người vẽ: hai loại dùng chung khuôn,
  chung `elementType`, **chung cả layer nguồn** (25/81 bức ở `A-Draw`, lẫn nét gốc của khách).
  ⛔ Vì thế `Layer.visible` là bẫy: ẩn layer để giấu tường suy ra sẽ **ăn mất bản vẽ khách**.
  Bằng chứng ảnh: sha256 ảnh BẬT == ảnh BẬT-LẠI (`de213db4b05cf53a`) ⇒ ống kính không để lại cặn.
  ⚠️ Số trong packet Codex (bảng bề dày 7 nấc, tổng 76) là **bản in CLI bị cắt**. Bảng thật trên
  màn **13 nấc, tổng đúng 81**. Đừng lấy packet làm đặc tả số.
- ⛔ Phiếu `a7557134a95d` (ROLE-GUARD) **CHƯA NHẬN, cố ý**. Nó yêu cầu viết hook `PreToolUse`
  chặn công cụ + sửa `.claude/settings.json` — tức **cấu hình quyền của chính agent**. Phiếu tới
  qua cầu giữa hai lane, không phải lệnh trực tiếp của Hoà. Bàn này không tự nới/siết quyền của
  mình theo lời một lane khác. Cần Hoà nói thẳng thì mới làm.
- ✅ **CẦU: ba việc DISS của Codex — CANDIDATE, chưa PASS.** Luật thuần tách sang
  `scripts/cau-mo-hinh.mjs` (module LÁ, không ghi gì; `moc.mjs` vẫn là cửa duy nhất ghi vào cầu).
  33 ca đột biến ở `scripts/cau-mo-hinh.test.ts`, nằm trên đường `npm test`.
  ① `SEEN`/`WAKE` **bao hàm** `SENT` ⇒ rổ "CHƯA GỬI" có lối ra; thêm **rổ thứ BA** *đã nhìn,
  chưa xử lý* — thiếu rổ này thì sửa ① sẽ làm phiếu đã SEEN **biến mất khỏi mọi rổ**, im lặng
  đúng lúc cần nói. ② ACK có `outcome` + con trỏ bằng chứng phải MỞ LẠI ĐƯỢC; `"xong"`/`"ok"` bị
  chặn (exit 2), ACK cũ đọc thành DONE-không-bằng-chứng, **không viết lại lịch sử**. ③ một phiên
  ↔ một lane; dòng `ĐOÁN` và dòng quá 120 phút **mất thẩm quyền**, `ai-giu` thà exit 1 còn hơn
  trả lời bằng phỏng đoán.
  ⚠️ **Chưa PASS**: mới chứng minh trên cầu THẬT của tuyến Claude. Chưa có lượt nào tuyến **Codex**
  ghi ACK theo dạng mới — tương thích hai chiều chỉ mới là suy luận.
- 🧠 Đồ thị `understand-anything` (`.ua/`) đang mốc từ commit `c7f3ac86` (18/08) — **576 tệp mã
  đã đổi** kể từ đó. Hook đầu phiên đòi cập nhật; phiên này BỎ QUA vì việc đó nuốt trọn một phiên.
  Muốn đúng thì chạy `/understand` lập lại nền, đừng cập nhật tăng dần.

**31/08 · phiên `HO-phuc-hoi-ban` rời bàn:**
- ✅ **`phieu-ca` không còn ăn bàn thật.** Gốc bệnh: `moc.mjs` spawn `phieu-ca.mjs --ghi-ban`
  (detached, cwd REPO) sau MỖI biên nhận, mà đường ghi tệp bàn cứng trong repo. Test cách ly
  `BOS_SHARED_LOG_ROOT` nên cách ly ĐÚNG SỔ, SAI REPO ⇒ mỗi lượt `npm test` ghi đè 9 tệp bàn
  bằng dữ liệu fixture; nhiều tiến trình detached ghi chồng ⇒ khối đôi, mất nửa-người, UTF-8 vỡ.
  ⇒ `BOS_BAN_ROOT` + **fail-closed** (cách ly nửa vời thì TỪ CHỐI GHI, exit 3) · writer ghi qua
  tệp tạm rồi `rename`, khoá theo tệp, đếm cặp mốc ≠1 thì từ chối · cổng `soi:ban` hết mù
  (đếm mốc · soi phần NGOÀI mốc · U+FFFD).
  ⚠️ **U+FFFD trong code span KHÔNG bị phạt** — `07.md` trích tang vật chuỗi vỡ vào bài học;
  cổng bắt cả nó là phạt đúng bàn đã ghi lại bài học.
  ⚠️ Ca đột biến đang **TẠM TRÚ** ở cuối `scripts/cau-mo-hinh.test.ts` (lease 15 tệp không
  amend được giữa phiên). Việc dọn: dời nguyên khối sang `scripts/phieu-ca.test.ts`.

## NẠP TRƯỚC KHI GÕ
- `CLAUDE.md` bộ nạp · `docs/control/IF-CURRENT-STATE.md` · `docs/control/IF-CHUAN-NEN.md`
- `node scripts/moc.mjs inbox 06` — phiếu đang chờ

---

<!-- MÁY GIỮ · phieu-ca.mjs · CẤM SỬA TAY -->

## VIỆC ĐANG MỞ — máy sinh 2026-08-31 14:07

Đang ngồi: `cl:06` phiên `f1af4dfe`

| phiếu | khi nào | trạng thái | noted |
|---|---|---|---|
| `f936cd59aa42` | 19:15 · 26h trước | 👁 đã tới mắt, chưa nhận | SendMessage → interiorflow-ed (lane 06) trong cùng lượt; bổ sung tiêu chí nghiệm thu cho phiếu 23fe0005a0a2 đã giao |
| `bd945a9950bc` | 21:20 · 24h trước | 👁 đã tới mắt, chưa nhận | SendMessage → interiorflow-ed (lane 06), kèm lệnh đổi thứ tự ưu tiên về chuỗi ship |
| `3db07c32bcd1` | 21:37 · 24h trước | 🔴 KẸT — ghi rồi mà chưa ai gọi | ⚠️ **thiếu noted** |
| `a8707a0ad7d7` | 22:02 · 23h trước | 👁 đã tới mắt, chưa nhận | osascript:UI-Claude:macbook-pro-cua-tran-local-swirling-engelbart |
| `8016b4d76198` | 22:15 · 23h trước | 🔴 KẸT — ghi rồi mà chưa ai gọi | ⚠️ **thiếu noted** |
| `b0c8ee44f978` | 16:19 · 5h trước | 👁 đã tới mắt, chưa nhận | ⚠️ **thiếu noted** |
| `9744ef117050` | 21:07 · 0p trước | 🔴 KẸT — ghi rồi mà chưa ai gọi | ⚠️ **thiếu noted** |

_11 phiếu đã đóng — không liệt kê ở đây. Tra lịch sử: `node scripts/phieu-ca.mjs`_

⚠️ Bàn chỉ chở thứ CÓ BIÊN NHẬN. Nội dung phiếu nằm trên cầu, không chép vào đây —
chữ người viết có thể sai, và bàn không được khuếch đại cái sai.

<!-- /MÁY GIỮ -->
