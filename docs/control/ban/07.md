# BÀN `07` · QUALITY · bằng chứng

> Ngồi vào bàn này thì đọc hết trang này **trước khi gõ**. Rời bàn thì cập nhật mục **ĐANG DỞ**.
> Kiến thức nằm ở bàn, không nằm ở người ngồi — xem `docs/control/ban/README.md`.

## VAI
SBOM · biên nhận · giấy phép · phản biện · threat model · fixture · tiêu chí coverage ·
counterproof · phản biện trước merge.
**Không cầm bút mã sản xuất** — 06 BUILD thi công theo packet của bàn này.

## CẤM
- PASS giả — chỉ NOT ASSESSED/PARTIAL/PASS/FAIL kèm bề mặt proof
- electron:publish

## Packet đang mở

| packet | việc | trạng thái |
|---|---|---|
| [`PACKET-07-COVERAGE-GIAY-PHEP.md`](../../quality/PACKET-07-COVERAGE-GIAY-PHEP.md) | coverage cổng giấy phép phát hành — threat model 6 đường lọt, hợp đồng nhãn, ngưỡng coverage, 4 ca đột biến | **CANDIDATE**, chờ 06 thi công |

Fixture dựng lại được: `node scripts/quality/dung-fixture-giay-phep.mjs` (`--don` để xoá).
Sinh lúc chạy, >64MB, **không commit** — `.gitignore` đã chặn.

## NGHIỆM THU — xong ở bàn này nghĩa là
ba thứ đủ: SBOM · biên nhận artifact · biên nhận giấy phép

## ĐANG DỞ
*(người rời bàn cập nhật mục này — để trống nghĩa là bàn sạch, không phải nghĩa là chưa ai ghi)*

- 🔴 **`npm test` ĐANG ĐỎ** tại `soi:ban --chan` (exit 1, đo 31/08) ⇒ chặn mọi commit đi qua
  `npm test`. Gốc đã truy được — xem bài học **BỘ TEST TỰ GHI ĐÈ TỆP BÀN** dưới. Chưa vá.
- 🔴 **`01 · 06 · 08` vẫn hỏng trong cây làm việc.** Ngoài allowlist của bàn 07, không chạm.
  Bàn 07 đã tự dựng lại từ `fed3e56f`.
- ✅ **Bài chấm 1 — commit `5936b6c1`** (HO-giay-to-khung, writer 8d): **A PASS · B PASS ·
  C PARTIAL** (thiếu đúng một mục phiếu nêu tên: `env-prefix BOS_SESSION_ID` trong nợ GUARD-V3).
  tsc 0 · 3 tệp · +209/−0. Biên bản đã gửi cl:00.

## NẠP TRƯỚC KHI GÕ
- `CLAUDE.md` bộ nạp · `docs/control/IF-CURRENT-STATE.md` · `docs/control/IF-CHUAN-NEN.md`
- `node scripts/moc.mjs inbox 07` — phiếu đang chờ

## BÀI HỌC ĐÃ TRẢ GIÁ — đọc để không trả lại

**Máy soi giấy phép bản cũ ĐÃ NÓI DỐI:** báo clean trên artifact bẩn thật (trần độ sâu 6, đường
thật sâu 8). Cổng chưa có ca đột biến chỉ là lời chúc. Đang dở: chuỗi dựng lại sạch chưa kiểm.

**T5 hết `NOT ASSESSED` (30/08).** Kho nén 320,6 MB trong artifact chính là
`dist/InteriorFlow-0.1.0-arm64.dmg` — **thứ thật sự giao cho người dùng** — và cổng chưa từng mở
ra soi. Cổng đang soi *cây build* mà không soi *bản giao*. GPL nằm trong `.dmg` là vô hình tuyệt
đối. Bài học chung: **quét nơi sản xuất không thay được quét nơi giao hàng.**

**Cơ chế mạnh hơn đếm.** `soi-giay-phep-phat-hanh.mjs:141` lọc ĐUÔI chạy **trước** `:144` kiểm CỠ
⇒ nhị phân lớn không bao giờ tới bước đo cỡ. Đếm nói "trần 64MB bắn 0 phát"; cơ chế nói *vì sao*
0 và *khi nào* sẽ khác 0. Chẩn đoán vòng 1 phải RÚT vì dựa trên đếm.

**Một cổng, hai con số thật, trái ngược nhau.** Coverage theo **tệp 79,76%** vs theo **bytes
22,18%**. Cả hai đều đúng. Báo cáo bằng số theo tệp là nói thật mà che 78% khối lượng ⇒ packet bỏ
hẳn ngưỡng phần trăm, thay bằng bất biến *không-nhãn = 0* và *nhóm nguy hiểm 100% có nhãn*.

**Ngưỡng 99% là chỗ nấp.** 43 tệp nhị phân = 0,22% số tệp — lọt trọn dưới mọi ngưỡng 99%.

**Ca xanh-giả.** Ca 65MB (T3) đứng một mình sẽ đỏ và làm ta tưởng đã vá xong, trong khi bốn đường
lách kia vẫn lọt. Ca đột biến chỉ có nghĩa khi chạy cùng cả bộ.

**Counterproof bắt buộc.** Cổng `exit 1` vô điều kiện thoả mọi ca ĐỎ. Không có ca XANH thì không
phân biệt được cổng thật với cổng luôn kêu.

**🔴 CỔNG CANH BÀN ĐANG SOI ĐÚNG CHỖ NÓ TỰ VIẾT RA (31/08 — đo tại bàn này).**
`phieu-ca.mjs --ghi-ban` phá **nửa-người** của tệp bàn, và `soi:ban` báo **✅** trên chính tệp đã
bị phá. Đây là ca sạch nhất của bài học *"cơ chế mạnh hơn đếm"* mà bàn này đã ghi ở trên — lần
này nạn nhân là bàn này.

- **Đo được:** cả 9 tệp bàn ghi cùng một giây `31/08 00:19:02`. Sau lượt ghi đó, `01 · 06 · 07 ·
  08` mất phần người viết (tiêu đề, VAI, CẤM, NGHIỆM THU, ĐANG DỞ, bảng packet). `06 · 07 · 08`
  còn mọc **khối MÁY GIỮ thứ hai** — `grep -c` cho 2, sáu tệp kia cho 1. `08.md` để lại mảnh
  `��i` — **chuỗi UTF-8 vỡ**, thứ `String.slice` trên chuỗi đã giải mã không tạo ra được.
- **Cổng mù ở đâu:** `--kiem-ban` (`phieu-ca.mjs:207`) lấy `indexOf(MO)`/`indexOf(DONG)` — **lần
  xuất hiện ĐẦU TIÊN** — rồi chỉ so vùng giữa hai mốc đó với nguồn. Nó **không** kiểm ① có đúng
  MỘT khối hay không, ② phần ngoài khối còn sống hay không. Khối đầu của `07` là khối vừa sinh
  nên khớp nguồn ⇒ in `✅ 07 — 0 phiếu mở` trên một tệp đã mất sạch nửa-người.
- **Đỏ vì may, không vì bắt được.** `06` đỏ chỉ vì khối đầu của nó chở phiếu `aaaaaaaaaaaa` không
  có trong cầu — cổng bắt **rò fixture**, không bắt **phá tệp**. Nếu lượt ghi đó chạy trên cầu
  sạch thì cả 9 tệp đều `✅` và hỏng đi thẳng vào commit.

**🔴 BỘ TEST TỰ GHI ĐÈ TỆP BÀN — gốc thật, truy được 31/08 sau khi cl:00 bác chẩn đoán vòng 1.**

- **Tôi đã dán nhãn `FACT` lên một suy luận, và nhãn đó sai.** Vòng 1 tôi viết *"dữ liệu fixture
  nằm trong cầu THẬT"*. Tôi **chưa hề mở sổ cầu** — tôi đọc `aaaaaaaaaaaa` ở
  `docs/control/ban/06.md:13`, tức ở **đầu ra**, rồi suy ngược ra **đầu vào**. cl:00 đo lại và
  bác đúng: sổ cầu `~/PROJECT/SHARED/LOG/agent-handoffs.jsonl` **nguyên vẹn — 161 event, 0 kết
  quả cho `aaaaaaaaaaaa`** (tôi đã tự chạy lại `grep -c`, xác nhận). Bài học cho chính bàn này:
  *suy từ đầu ra ra đầu vào là suy luận, dù đầu ra có thật tới đâu.* Bàn 07 mà dán nhãn sai thì
  bàn 07 hỏng — nhãn là sản phẩm duy nhất của bàn này.
- **Gốc thật (đo được, không suy):** `moc.mjs:117` — **mọi** lượt ghi biên nhận đều
  `spawn(phieu-ca.mjs --ghi-ban)` **`detached` + `unref` + `cwd: REPO`**, thừa kế `process.env`.
  `scripts/cau-mo-hinh.test.ts:155` và `:194` chạy thật `moc.mjs` với
  `BOS_SHARED_LOG_ROOT=<thư mục tạm>`. Sổ cầu **được** cách ly; **đường tệp bàn thì KHÔNG** — nó
  dựng từ `REPO`, không dựng từ `BOS_SHARED_LOG_ROOT`.
  ⇒ **`npm test` đọc cầu fixture rồi ghi đè 9 tệp bàn THẬT trong repo.**
  Chú thích ở `cau-mo-hinh.test.ts:140` viết *"trên một cây cầu TẠM — không chạm sổ thật"*:
  **đúng về sổ, sai về repo.** Phiếu `HO-20260830150000-aaaaaaaaaaaa` là hằng số `H` ở
  `buu-ta-cau.test.ts:12` — nó vào `ban/06.md` bằng đường này, không bằng đường sổ cầu.
- **Vì sao hỏng LOẠN chứ không hỏng đều:** `detached`+`unref` ⇒ nhiều tiến trình `--ghi-ban`
  chạy **chồng nhau**, mỗi con `readFileSync` → sửa → `writeFileSync` **không nguyên tử**, trên
  cùng 9 tệp. Đó là **đua đọc-sửa-ghi**, và nó giải thích trọn bộ triệu chứng mà một lượt ghi
  đơn không giải thích nổi: mất-cập-nhật (khối đôi), ghi đè lệch (mất nửa-người), và **cắt giữa
  một ký tự nhiều byte** (`��i` ở `08.md`). Chẩn đoán vòng 1 của tôi — *"`String.slice` không tạo
  ra được UTF-8 vỡ"* — đúng, và chính chỗ đúng đó chỉ sang thủ phạm thật: **không phải một tiến
  trình cắt sai, mà nhiều tiến trình ghi đè nhau.**
- **Giả thuyết ⑵ của cl:00 (reader rơi event legacy) — không cần tới.** `0 phiếu đã đóng` /
  `chưa ai khai giữ lane` là **kết quả đúng** khi đọc một cầu fixture chỉ có vài event. Máy đọc
  không ốm; nó bị **chĩa vào sổ khác**. Vẫn nên vá reader, nhưng nó **không** là gốc của ca này.
- **Vì sao đây là hạng nặng nhất:** cổng canh không chỉ mù — **chính bộ test là thủ phạm**.
  Chạy `npm test` để chứng minh cây sạch thì làm bẩn cây. Càng kiểm càng hỏng, và `soi:ban` nói
  `✅`. Đây là ca sạch nhất của luật 5 *"không PASS giả"* mà bàn này giữ.
- **Đã từng đi thẳng vào commit rồi.** `git show 90e9e9fe:docs/control/ban/07.md` — bàn này **đã**
  ở dạng hỏng (hai dòng trống · `---` · khối máy ở đầu) từ 30/08. Bản còn nguyên cuối cùng là
  `fed3e56f`. Phiên sau không biết, dựng tay lại một tiêu đề **ngắn hơn** ⇒ `VAI · CẤM · NGHIỆM
  THU · ĐANG DỞ · NẠP TRƯỚC KHI GÕ` của bàn 07 **mất khỏi mọi commit** từ đó tới nay. Bàn này vừa
  khôi phục chúng từ `fed3e56f`.
- **Vì sao đắt:** hàng `0z` của bộ nạp trỏ thẳng vào tệp bàn. Nửa-người **chính là** thứ bộ nạp
  bảo người mới đọc. Máy xoá nó, cổng nói xanh, người sau dựng lại bằng trí nhớ — đúng ba nhịp
  của N8, lần này chạy **bên trong** chính cơ chế dựng ra để chống N8.
- **Nợ có tên (việc của 06, không phải của bàn này):** ① `--ghi-ban` phải ghi **nguyên tử** và từ
  chối khi tệp không có đúng một cặp mốc; ② `--kiem-ban` phải đếm số cặp mốc và **soi cả phần
  ngoài khối**, không chỉ phần trong; ③ ca đột biến: tệp hai khối · tệp không khối · tệp mất
  nửa-người — cả ba phải làm cổng ĐỎ; ④ counterproof: một tệp lành phải XANH;
  ⑤ **cách ly test — nợ ĐI TRƯỚC bốn cái trên**: `--ghi-ban` phải lấy đích ghi từ **một biến đi
  cùng `BOS_SHARED_LOG_ROOT`** (vd `BOS_BAN_ROOT`), và **từ chối ghi** khi cầu là thư mục tạm mà
  đích lại là repo thật. Không vá ⑤ thì mỗi lần `npm test` lại phá lại, và bốn vá kia chỉ dọn
  hiện trường.
- **Chưa vá.** `01 · 06 · 08` vẫn hỏng trong cây làm việc lúc bàn này ghi dòng này.

---

<!-- MÁY GIỮ · phieu-ca.mjs · CẤM SỬA TAY -->

## VIỆC ĐANG MỞ — máy sinh 2026-08-31 14:07

Đang ngồi: `cl:07` phiên `92b82a3e`

✅ Không phiếu nào đang mở.

_3 phiếu đã đóng — không liệt kê ở đây. Tra lịch sử: `node scripts/phieu-ca.mjs`_

⚠️ Bàn chỉ chở thứ CÓ BIÊN NHẬN. Nội dung phiếu nằm trên cầu, không chép vào đây —
chữ người viết có thể sai, và bàn không được khuếch đại cái sai.

<!-- /MÁY GIỮ -->
