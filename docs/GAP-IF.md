# GAP-IF — lỗi IF do TASK lộ ra (backlog sản phẩm, TRUNG TÍNH)

Ngôn ngữ trung tính: mô tả IF THIẾU GÌ, không nêu tên khách/số liệu dự án.
Dữ liệu dự án ở 2407-Test/. Đây chỉ là năng lực IF.

| GAP | Task lộ | IF thiếu gì (trung tính) | Subsystem | Build? | Trạng thái |
|-----|---------|--------------------------|-----------|--------|------------|
| (vòng chẩn đoán 06/08 — các phiên điền vào đây, CHƯA sửa) |
| G-M1-01 | T3 | Nạp DXF chạy trên LUỒNG CHÍNH, **không tiến độ, không huỷ, không worker** — đường DWG đã có đủ ba thứ đó, đường DXF thì chưa. Máy rảnh thì bản thân phép phân tích nhanh (0,2–1,5 s cho file 5–27 MB, DƯỚI ngưỡng 2 s) nên đây KHÔNG phải nút cổ chai thuật toán; nhưng máy bận thì cùng file đó đo được 12–68 s, và cả quãng đó giao diện đứng hình không có cách nào thoát. Bộ nhớ đỉnh 100–515 MB (tới ~20× dung lượng file). | lõi-cad | Có | ✅ ĐÃ SỬA 06/08 — `lib/cad/dxf-worker.ts` + `dxf-import.ts` + `dxf-open.ts` nối vào `CadEditor.tsx:424-495` (AbortController + thanh Huỷ). Verify TỔNG 07/08: `dxf-worker.ts` tồn tại (`M-HINH-HOC-OUT.md:113`) |
| G-M1-02 | T3 | Báo cáo nạp `DxfLoadReport` (7 trường: đọc được/bỏ qua/block/layer/bbox/cảnh báo) ĐÃ CÓ nhưng KHÔNG CÓ NƠI TIÊU THỤ — màn CAD gọi bản mỏng `parseDxf()` rồi vứt báo cáo, chỉ hiện "đã mở N đối tượng". Người dùng không biết mình vừa mất gì. | lõi-cad + vỏ CAD | Có (nối dây) | 🟡 ĐẠT MỘT PHẦN — đã nối `parseDxfEx` + panel báo cáo; còn 5 lỗi chữ/số trên panel (jargon DXF thô, hai con số "bỏ qua" lệch nhau 344 vs 314) |
| G-M1-03 | T3 | Bộ đọc ngữ nghĩa mặt bằng (lưới trục · lõi cứng · diện tích khung tên · đối chiếu diện tích) đã viết đủ và chạy đúng trên file thật nhưng có **0 nơi gọi** trong app ⇒ chặng zoning không có đường đi từ bản vẽ vừa nạp. | lõi-cad + vỏ CAD | Có (nối dây) | ✅ ĐẠT — 6/6 file: trục 6+5 · lõi 3–4 vùng · 4–15 ms |
| G-M1-04 | T3 | Zoom-extents sau khi nạp dùng khung bao TOÀN BỘ entity. Một bản sao cũ để xa trong model space (ca thật: cách gốc ~12 km) làm khung bao phình ~400× ⇒ mở file ra màn hình gần như trống. Hàm lọc cụm vẽ chính đã có nhưng không được gọi. | lõi-cad + vỏ CAD | Có (nối dây) | 🟡 ĐẠT MỘT PHẦN — chạy + có đường quay lại, nhưng bật trên 6/6 file (giấu 9–76% số hình) và nút "về cụm chính" ở panel cho khung KHÁC khung lúc nạp |
| G-M1-05 | T3 | Không tính được diện tích sàn TỪ HÌNH HỌC (cả 6/6 file trả `method:'none'`): đường bao sàn vẽ bằng nhiều đoạn thẳng rời, muốn thành đa giác phải dò mặt phẳng khép kín — thuật toán chưa có. ⇒ không nghiệm thu được diện tích của khu vừa chia. | lõi-cad | Có | 🔴 chưa sửa |
| G-M1-06 | T3 | Entity làm phẳng từ block chỉ mang TÊN block, KHÔNG mang id BẢN CHÈN. Ca thật: 1 tên block bị chèn 18 lần, 828 hình phẳng mang cùng một chuỗi ⇒ không chọn / không di chuyển / không đếm được **MỘT** cấu kiện — chỉ chọn được từng đường rời hoặc cả loạt. | lõi-cad | Có | ✅ ĐẠT — 6 file × 3 block + ca lồng sâu 5 cấp: 0 lem, 0 sót |
| G-M1-07 | T3 | Sửa 1 đường rồi xuất DXF lại: hình học giữ nguyên 100% (2984→2984, đủ layer) nhưng **cấu trúc block bị san phẳng** — 0 INSERT trong file ra, nhãn block 2965→0. Người nhận bản vẽ mất khả năng sửa theo block, mọi bản lặp thành hình rời. | lõi-cad | Có | 🟡 ĐẠT MỘT PHẦN — 0 → 91–457 INSERT, cấu trúc cân; nhưng cây lồng 5 cấp bị ép còn 1, tên block 127→29. **Chưa nghiệm thu được bằng CAD ngoài** (xem G-M1-13) |
| G-M1-08 | T3 | Vùng tô (poché tường, 126–161 mảng/file) KHÔNG neo vào cấu kiện — sửa đường tường thì mảng tô đứng nguyên tại chỗ cũ. Đúng nghĩa "sửa 1 chỗ vỡ chỗ khác". | lõi-cad | Có | 🟡 THU HẸP 07/08 (gộp `docs/M-HINH-HOC-OUT.md` VIỆC 1) — engine ĐÃ XONG và ĐÃ TEST: `poche.ts` `syncPocheAnchors`/`normalizeRing`/`propagatePocheEdits`/`expandIdsWithPoche`, 49 ca pass (30 `poche.test.ts` + 19 `poche-import.test.ts`), nâng 0 → 80–90 mảng tô neo được/file trên 6 file thật. Nối dây trong phiên (`store.ts:531,540,572,623,633`) XONG. **Còn đúng 1 chỗ đứt: `importDoc` → tách sang `G-M1-20`.** |
| G-M1-09 | T3 | Sau khi nạp, **0/12.274 entity có `elementType`** — toàn bộ mặt bằng là đường rời, app không biết đâu là cột / tường / lõi. Ràng buộc chia khu ("không cắt vách ngang cột") không có gì để bám. Suy ngữ nghĩa từ tên layer chỉ là quy ước từng bộ hồ sơ, chưa có bộ suy + cờ `inferred`. | lõi-cad | Có | 🟡 ĐẠT MỘT PHẦN — 0 → 52–89% entity có loại, 100% mang cờ `inferred`; nhưng có HỒI QUY (xem G-M1-15) và báo cáo mất phân biệt suy/khai sau vòng xuất–nạp |
| G-M1-10 | T3 | `ATTRIB`/`ATTDEF` bị bỏ (14–17 bản ghi/file) — đó là nơi phần mềm CAD cất **giá trị khung tên** (tên bản vẽ, tỉ lệ, số hiệu, ngày, phiên bản) và nhãn trục dạng thuộc tính. Nạp xong không có gì để điền khung tên hồ sơ. | lõi-cad | Có | ✅ ĐẠT — đính chính 07/08 (`M-HINH-HOC-OUT.md` VIỆC 2): `case ATTRIB` CÓ (`lib/cad/dxf.ts:833`, đọc như TEXT). `ATTDEF` cố ý nằm trong `SILENT_RECORDS` (`import-summary.ts:75`) vì nó là ĐỊNH NGHĨA, giá trị thật ở ATTRIB ⇒ bỏ không mất chữ khung tên. Mục tiêu gốc "khung tên đọc được" đã đạt. |
| G-M1-11 | T3 | `ELLIPSE` bị bỏ (9/file) trong khi kho kiểu dữ liệu ĐÃ CÓ sẵn entity ellipse thật — chỉ thiếu nhánh đọc. `POINT` bị bỏ 27–300/file, `VIEWPORT` 2/file, và 2 `HATCH`/file dựng lỗi. Tất cả im lặng vì G-M1-02. | lõi-cad | Có | 🟡 THU HẸP 07/08 — `case ELLIPSE` CÓ (`dxf.ts:840`). `VIEWPORT` cố ý bỏ (khung nhìn giấy, `import-summary.ts:70-71`). **Còn thiếu thật: `POINT` (27–300 bản ghi/file, có báo trong `report.skipped` nên không im lặng) — cần `PointEntity` kiểu mới trong `model.ts` (K4) · và "2 HATCH/file dựng lỗi" CHƯA VERIFY.** |
| G-M1-12 | T3 | 2/6 file không lấy được diện tích ghi trong khung tên: chuỗi nằm trong **định nghĩa block không được chèn** (rác copy-paste của file gốc). IF chưa có đường đọc khung tên độc lập với việc block đó có được chèn hay không, cũng chưa cảnh báo "file có định nghĩa block mồ côi". | lõi-cad | Có | 🔴 chưa sửa |
| G-M3-01 | T2 | Bốc món từ ảnh chỉ làm được **MỘT món/lượt** (tách nền 1 foreground · đo 1 món). Không có bước liệt kê/đếm N món trong một ảnh ⇒ muốn ra bảng N món phải chạy tay N lượt rồi tự gõ lại tên/mã. | vision | Có | 🟠 KHÔNG ĐÓNG ĐƯỢC như phiếu mong (M-FIX-C §111) — khối `util.ffetable` đã đăng ký nhưng đường bốc N món chưa thông |
| G-M3-02 | T2 | Luồng node **không có kiểu dữ liệu BẢNG** (chỉ ảnh/chữ/mặt nạ/số/video) ⇒ một danh sách món không tồn tại được như dữ liệu chạy giữa các khối; không khối nào xuất ra bảng món. | vision + luồng node | Có | 🔴 chưa sửa |
| G-M3-03 | T2 | Hai bước cuối của dây chuyền ảnh→bản vẽ (**khớp mẫu block theo tỉ lệ** và **ba hình chiếu**) đã viết xong + có test nhưng **0 nơi gọi** trong app ⇒ từ món đã đo KHÔNG có nút nào ra được block/bản vẽ. Dây chuyền đứt đúng giữa vision và block. | vision + vỏ CAD | Có (nối dây) | 🔴 chưa sửa |
| G-M3-04 | T2 | Hồ sơ FF&E chỉ có mức **1 món = 1 tấm ảnh JPG** (ảnh + số đo). Không có hồ sơ nhiều món kiểu chuẩn ngành (mã · ảnh · finish · vendor · giá · số lượng · ô duyệt trước sản xuất). | trình bày | Có | ✅ ĐÓNG MỨC FILE 06/08 (M-FIX-C) — `buildFfeSheet` → .xlsx 14.083 B, 2 ảnh neo đúng ô. Nút đã có trong cửa nhập; **chưa bấm được** · ✅ **NÂNG LÊN XONG 07/08 (p4)** — nút `Tải hồ sơ FF&E (N món)` có CHỮ, đã bấm thật (`M-FIX-C-OUT.md:107,377`) |
| G-M3-05 | T2 | Cửa nhập bảng vào kho chỉ có **9 trường** ⇒ bảng bốc tách mất **vật liệu · màu · độ tin cậy · phòng**. Kho dữ liệu THẬT thì đã có sẵn trường vật liệu/màu/cao — chỉ là cửa nhập không nối tới. | kho vật liệu | Có (nối dây) | ✅ ĐÓNG 06/08 (M-FIX-C) — trình duyệt thật: 16 ô ghép cột hiện đủ, vật liệu·màu·phòng·độ tin cậy vào đúng ô |
| G-M3-06 | T2 | **Bug ghép cột đo được**: từ khoá đoán cột dùng chữ cái đơn và khớp theo chuỗi con ⇒ cột "Phòng" bị gán vào ô **Cao**, cột Cao thật bị bỏ; cả lô nhập vào mất chiều cao mà không báo gì. Rủi ro y hệt đang treo với chữ cái rộng/sâu. | kho vật liệu | Có | ✅ ĐÓNG 06/08 (M-FIX-C, vá lần 2) — trình duyệt thật + 119 test + 14 ca gán-sai đảo đúng |
| G-M3-07 | T2 | Nhập bảng luôn ép loại **'vật liệu'** ⇒ món nội thất rời vào kho thành vật liệu, dù kho có sẵn loại 'nội thất'. Người nhập không được chọn loại. | kho vật liệu | Có | ✅ ĐÓNG 06/08 (M-FIX-C) — trình duyệt thật: ô "Nhập vào ngăn nào của kho" tự đoán Nội thất rời, đổi được |
| G-M3-08 | T2 | Bản ghi sản phẩm **không có trường phòng/vị trí** ⇒ bảng FF&E theo phòng không có chỗ lưu, không thống kê được "phòng này gồm những món nào". | kho vật liệu | Có | ✅ ĐÓNG 06/08 — **verify chạy thật trên máy Hoà**: ghi `room="Phòng ngủ Master"` + `confidence="measured"` → đọc lại đúng nguyên văn → `count({where:{room}})` = 1 → xoá sạch bản ghi kiểm. Chỗ lưu ĐÃ CÓ (DB 34 cột + 3 đường `specNormalize`·`specPatch`·`specToDto`). ⚠️ Đường ghi TỪ CỬA NHẬP xuống DB tách thành `G-M3-17` — việc khác, đừng nhét chung một mã. |
| G-M3-09 | T2 | **BOQ không tính món rời và không hề báo lỗi**: máy tính khối lượng chỉ quét vùng tô (m²). Món rời đã gán mã + đơn giá vẫn không sinh dòng nào, bảng vẫn trông "đủ" ⇒ báo giá thiếu âm thầm. Cũng không có đơn vị đếm cái/bộ, không có cột số lượng. | BOQ | Có | ✅ ĐÓNG MỨC ENGINE 06/08 (M-FIX-C) — `computeBoq` chạy thật trên Doc 11 entity + 120 test. **CHƯA bấm được trên UI** ⇒ chưa đạt N6 (tính năng chạy thật) · ✅ **NÂNG LÊN XONG 07/08 (p4)** — đã BẤM THẬT nút `Bảng khối lượng (BOQ)`, ra bảng 11 cột có Ảnh + Khối lượng + Đơn vị; món rời đếm đúng **8 cái × 2.450.000 = 19.600.000** (`M-FIX-C-OUT.md:377-392`). ⚠️ Verify TỔNG 15:4x: `grep computeBoq *.tsx`=0 vì nút gọi qua `POST /api/boq/[projectId]` (`BoqScreen.tsx:155`), KHÔNG import trực tiếp — TỔNG grep sai chỉ báo (§0y) |
| G-M3-10 | T2 · T3 | Block thư viện và cụm bàn khi thả vào bản vẽ bị **làm phẳng thành đường rời, mất danh tính** ⇒ bảng thống kê gộp hết vào "Chưa phân loại", không gán được mã sản phẩm, không lên được BOQ. (Ca đo: cụm 8 chỗ = 41 đường rời.) Cùng gốc với G-M1-06. | lõi-cad + thư viện | Có | 🔴 chưa sửa |
| G-M3-11 | T2 | Bảng khối lượng **không có cột ảnh** và file .xlsx xuất ra **không nhúng được ảnh** (bộ ghi file tự dựng, chỉ có chữ) ⇒ không ra được bảng FF&E có hình. | BOQ + trình bày | Có | ✅ ĐÓNG MỨC FILE 06/08 (M-FIX-C) — .xlsx thật có `xl/media/`+`xl/drawings/`, openpyxl đọc được. **Chưa mở bằng Excel thật, chưa bấm nút xuất trên UI** · ✅ **NÂNG LÊN XONG 07/08 (p4)** — nút `Xuất xlsx` có CHỮ (G6), đã bấm thật (`M-FIX-C-OUT.md:100,377`) |
| G-M3-12 | T3 | Kho block **văn phòng mỏng: 8 block** (2 bàn · ghế · 2 vách · 3 tủ hồ sơ). Thiếu hẳn: quầy lễ tân · buồng gọi điện · tủ rack máy chủ · máy in/copy · locker · dãy kệ kho · bàn cao pantry · màn hình/bảng viết phòng họp. | thư viện | Có | 🔴 chưa sửa |
| G-M3-13 | T3 | Cụm sinh theo tham số mới có **6 loại, đều là bàn làm việc + bàn họp**. Không có cụm dựng sẵn cho lễ tân · lounge · pantry · phòng họp có thiết bị. | thư viện | Có | 🔴 chưa sửa |
| G-M3-14 | T3 | **Thả món từ Thư viện không rơi xuống bản vẽ** — sự kiện thả có 1 chỗ phát, **0 chỗ nghe**, chỉ hiện thông báo. Riêng kệ cụm bàn chạy được vì gọi thẳng vào bản vẽ. | thư viện + vỏ CAD | Có (nối dây) | 🔴 chưa sửa |
| G-M3-15 | T3 | **Hai cửa thư viện song song**: 54 block .dxf không hiện trong Thư viện, chỉ vào được qua panel nội thất của màn CAD và một trang demo — trái chốt "Thư viện là cửa duy nhất". | thư viện + vỏ CAD | Có | 🔴 chưa sửa |
| G-M3-16 | T3 | Máy tự bố trí theo đề bài **đã có** nhưng bảng công năng ghi văn phòng = **một cái bàn**. Không có đường: số nhân sự → số chỗ → chọn kiểu cụm → rải cụm theo lưới cột; cũng không đặt được các không gian dùng chung. Kiểm diện tích tự động mới có cho bàn họp. | lõi-cad (bố trí) | Có | 🔴 chưa sửa |
| G-M5-01 | T1 · T3 | **Màn đầu tiên của cả hai task không có hợp đồng giao diện**: cụm "nhập bản vẽ có sẵn" (chọn tệp → tiến độ → nút huỷ → báo cáo nạp: đọc được/bỏ qua/cảnh báo) — grep 67 trang mock cho "nhập bản vẽ"/"báo cáo nạp"/"tiến độ nhập" = **0**. Năng lực tiến độ/huỷ đã có ở một đường nạp (G-M1-01) nhưng chưa từng được VẼ, nên port xong vẫn không biết đặt nó ở đâu. | giao diện | Có | 🔴 chưa sửa |
| G-M5-02 | T1 | **Inspector cấu kiện mới có hợp đồng cho TƯỜNG, thiếu CỬA và KHỐI.** 🟠 **Đính chính 06/08 (bản đầu của dòng này SAI, kiểm phản biện bắt được)**: trang mock chế độ cấu kiện ĐÃ vẽ trọn Inspector tường — loại tường · tim tường · dày · cao · nối tường · sửa-loại-ăn-mọi-tường-cùng-loại · cửa nằm trong tường. Cái thiếu thật: **trang CỬA riêng**, **trang KHỐI (đồ rời)**, và **lịch sử sửa của cấu kiện**. Trang CAD shell còn lại chỉ có Inspector "phòng" nên đừng lấy trang đó làm chuẩn. | giao diện | Có | 🟠 thu hẹp |
| G-M5-03 | T1 · T3 | **Sáu trang mock cùng tả MỘT màn (chặng 2D), không trang nào ghi "bản chốt"** — ba bản CAD shell v3/v4/v5 dùng **chung một tiêu đề**, cộng một bản 2D khác và hai bản chế độ. Phiên port không có cách chọn bản đúng ngoài đoán theo ngày sửa file. Bộ mock thiếu hẳn quy ước đánh dấu bản hiệu lực / bản đã thay thế. | giao diện | Có | 🔴 chưa sửa |
| G-M5-04 | T1 | **Cụm xuất in không đọc được thành hợp đồng**: 4 trang (hộp xuất PDF · tờ giấy · bảng nét in · bảng tròn) đều là bản xuất công cụ thiết kế còn nguyên chỗ trống dữ liệu. Mở thật bằng trình duyệt: khổ giấy hiện `{{ }}`, danh sách tờ trống, vùng xem trước rỗng. T1 kết thúc bằng phát hành lại hồ sơ ⇒ bước cuối T1 mất hợp đồng. | giao diện | Có | 🔴 chưa sửa |
| G-M5-05 | (chung) | **10/67 trang là bản xuất công cụ thiết kế, chạy không nổi ngoài công cụ đó**: cần một tệp kịch bản kèm theo **không có trong repo**, và còn 2–58 chỗ chữ mẫu chưa thay mỗi trang. Nặng nhất: trang "Thư viện" trỏ tới **4 trang con không tồn tại** ⇒ mở ra kệ trống trơn. Cửa kiểm hiện bắt được "trỏ tệp cục bộ"/"còn chữ mẫu" nhưng **không có luật bắt trang con thiếu**, nên bộ này vẫn đếm là "đã có mock". | giao diện | Có | 🔴 chưa sửa |
| G-M5-06 | T1 | **Không màn nào, không mock nào cho PHIÊN BẢN hồ sơ**: so trước–sau bản vẽ, đánh dấu chỗ vừa sửa, đóng dấu bản phát hành. T1 nguyên văn là "sửa lại" một thành phần của hồ sơ **đã giao** — thiếu bước này thì bản sửa không giao lại được cho ai. | giao diện + hồ sơ | Có | 🔴 chưa sửa |
| G-M5-07 | T2 | **Cửa sổ công cụ bốc tách/đo món không có mock.** Trang tool-window duy nhất tả việc KHÁC (phác thảo → ảnh thật). Trong khi đó bước bốc tách là trục chính của T2 và đã có hàm chạy được ở tầng dưới (G-M3-01). | giao diện | Có | 🔴 chưa sửa |
| G-M5-08 | T2 | **Không trang nào có BẢNG N MÓN** (kết quả bốc tách nhiều món) hay hồ sơ nhiều món kiểu chuẩn ngành (mã · ảnh · hoàn thiện · nhà cung cấp · số lượng · ô duyệt). Giao diện chưa từng vẽ ra thứ mà G-M3-01/02/04 nói là thiếu ⇒ hai tầng cùng trống, không tầng nào kéo tầng nào lên. | giao diện | Có | 🔴 chưa sửa |
| G-M5-09 | T2 | 🟠 **Đính chính 06/08 — bản đầu đếm SAI và kết luận NGƯỢC.** Đếm lại: mock bảng khối lượng có **10 ô tiêu đề** (không phải 7 — phép đếm cũ dùng biểu thức chỉ bắt ô toàn chữ nên bỏ sót ô "Thành tiền ƒx" và ô thêm-cột), và mock **đi TRƯỚC code ở 3 điểm**: người dùng tự thêm cột (6 kiểu, có trần), popover công thức ƒx, và truy vết sửa-tay ↔ số-máy. Grep trong `components/present-editor/boq/` + `lib/boq/` = 0 chỗ làm 3 việc đó (4 dòng khớp chỉ là công thức ghi vào tệp bảng tính khi xuất). ⇒ **"sửa mock theo code" là xoá đặc tả** — hướng đúng là code đuổi theo mock. Cái thiếu THẬT của mock: **cột số lượng đếm (cái/bộ)** — trang hồ sơ trình khách đã vẽ bảng món có cột SL + đơn vị "cái/tấm", trang bảng khối lượng thì không, và máy tính khối lượng cũng chỉ quét m² (G-M3-09). | giao diện + BOQ | Có | 🟠 thu hẹp |
| G-M5-10 | T2 | **Ba màn đã CODE mà chưa bao giờ có mock** — kho vật liệu, cửa nhập bảng tính (ghép cột/xem trước/báo dòng hỏng), bảng màu sơn. Grep 67 trang cho "kho vật liệu"/"nhập bảng"/"ghép cột"/"màu sơn" = **0**. Ngược luật "mock là hợp đồng": màn ship trước, hợp đồng không tồn tại ⇒ không có gì để nghiệm thu, và phiên sau dễ vẽ lại từ đầu. | giao diện | Có | 🔴 chưa sửa |
| G-M5-11 | T3 | **Màn nhận ĐỀ BÀI không có mock** (app đang có một panel 3 bước nằm lọt trong màn vẽ). Bước MỞ ĐẦU của T3 không có hợp đồng ⇒ mọi thứ phía sau (chia khu, bố trí) không biết bám vào dữ liệu nào trên giao diện. | giao diện | Có | 🔴 chưa sửa |
| G-M5-12 | T3 | **Không mock, không màn cho ZONING theo chương trình**: chia khu từ đề bài · bảng diện tích từng khu · đối chiếu số người ↔ diện tích · bảng xếp bộ phận theo tầng. Grep "zoning"/"chia khu"/"xếp tầng" trên 67 trang = **0**. (Panel "zone" đang có là zone MÀU để trình bày — việc khác.) Đây là khoảng trống lớn nhất của T3 ở tầng giao diện. | giao diện + lõi-cad | Có | 🔴 chưa sửa |
| G-M5-13 | T3 | 🟠 **Đính chính 06/08 — bản đầu của dòng này SAI.** Panel kiểm chuẩn **ĐÃ CÓ và chạy** trong màn vẽ (mở bằng nút, có cả gợi ý sửa), tựa trên một bộ luật khá dày (thoát hiểm · mật độ người · phòng cháy · chiếu sáng · tiếp cận · nhà ở · Neufert · ISO bản vẽ). Cái thiếu thật, hẹp hơn nhiều: **(a) không trang mock nào tả panel này** ⇒ không có hợp đồng để nghiệm thu; **(b) luồng bố trí của T3 không tự đẩy kết quả sang panel đó** — bố trí xong vẫn phải nhớ mở tay, và chưa đo được bộ luật hiện có phủ tới đâu cho ca văn phòng (lối đi giữa cụm bàn · diện tích trên đầu người). | giao diện | Có | 🟠 thu hẹp |
| G-M5-14 | (chung) | **44/67 trang ĐỎ ở cửa kiểm mock**, trong đó **19 trang chỉ dựng MỘT theme** — đúng nguyên nhân đã ghi của lỗi màn tối. Mọi trang đỏ là hàng chặn: port một trang chỉ-sáng là đẻ lại lỗi cũ. | giao diện | Có | 🔴 chưa sửa |
| G-M5-16 | (chung) | **Cửa kiểm mock cho qua 5 kiểu hỏng đo được tận mắt** (vòng phản biện 06/08): ① **khoá theme sai từ vựng** — 6 trang dùng nhánh sáng gắn vào một tên theme mà app KHÔNG bao giờ phát ra (app chỉ có auto/sáng/tối, grep tên đó trong `lib`+`components`+`app` = 0) ⇒ đặt theme sáng lên trang đó nền vẫn tối, mà luật ④ vẫn xanh vì chỉ tìm chuỗi `data-theme`; ② **chữ mẫu không viết bằng `{{}}`** — một trang có 62 chỗ chữ "PLACEHOLDER" lộ thẳng ra giao diện, hai trang khác 28 và 21 chỗ, cửa kiểm xanh hết; ③ **ruột là TÊN COMPONENT thay vì hình** — một trang in chuỗi khai báo component và nhãn "tóc 2…tóc 8" ra màn, trái luật thumbnail-vẽ-thật đã chốt; ④ **phụ thuộc mạng vẫn tính là tự đủ** — 8 trang nạp thư viện icon/phông từ Internet (một trang ghim `@latest` ⇒ hợp đồng trôi theo phiên bản, mất mạng là mất 44 icon hoặc rơi phông chữ Việt); ⑤ **trùng màn không ai bắt** — 3 trang CAD shell mang **cùng một tiêu đề**. | giao diện | Có | 🔴 chưa sửa |
| G-M5-17 | (chung) | **Hợp đồng giao diện nằm NGOÀI vùng cửa kiểm**: cửa chỉ quét `docs/mocks/*.html` không đệ quy ⇒ bỏ sót ít nhất 4 tài sản ĐANG được git theo dõi và ĐANG được tài liệu gọi là nguồn thiết kế — bản gieo hệ thiết kế (`docs/IF-design-system-seed.html`, 12 KB, sổ chốt gọi thẳng là "nguồn sự thật cho công cụ dựng mock") · 2 trang đề xuất giao diện nằm ở GỐC repo (54 KB + 62 KB) · hệ thiết kế bản PDF 812 KB (cửa lọc `.html` nên vô hình). Thêm vào đó, thư mục vừa tách ra cũng lập tức ra khỏi tầm quét. ⇒ con số "N trang sạch" không bao giờ nói đúng sức khoẻ của bộ hợp đồng. | giao diện | Có | 🔴 chưa sửa |
| G-M5-15 | (chung) | **10/67 trang trong bộ mock của app này thực ra là màn của app song song** (4 trang cùng họ tiền tố + 6 trang tự khai trong tiêu đề). Ba trang trong số đó mang tiền tố của app này nhưng tiêu đề lại là app kia ⇒ phiên port rất dễ port nhầm màn của sản phẩm khác vào sản phẩm này. Bộ mock chưa có quy ước tách hai sản phẩm. | giao diện | Có | 🔴 chưa sửa |
| G-M1-13 | T3 · vòng SỬA | **Xuất DXF đổi tên lớp và GỘP MẤT lớp**: bộ làm sạch tên thay khoảng trắng bằng gạch dưới ⇒ hai lớp tên `A B` và `A_B` xuất ra chỉ còn MỘT (tái hiện được: 2 lớp → 1, entity vẫn 2, không cảnh báo). Hồ sơ thật có lớp 7.151 hình bị đổi tên. | lõi-cad | Có | ✅ ĐÃ SỬA 07/08 (p9) — thêm `buildUniqueLayerNames()` vào `lib/cad/dxf.ts`, gán hậu tố `_2/_3…` khi đụng tên, dùng CHUNG cho bảng LAYER (group 2) và `layerName(id)` (group 8) ⇒ một nguồn, hết 2 chỗ tự gọi `sanitizeName` rời nhau. Test `dxf.roundtrip.test.ts` mục [8]: 48/48 pass. Không hồi quy 5 bộ dxf khác (156 ca). `tsc` sạch. **Đây là điều kiện tiên quyết của `G-M1-14` — nay đã gỡ.** |
| G-M1-14 | T3 · vòng SỬA | **Vùng tô poché của IF không sống sót vòng xuất DXF** — xuất ra rồi nạp lại thì hatch quay về đường nhiều đoạn, mất luôn liên kết neo vừa dựng ⇒ tường lại rách sau một vòng xuất/nhập. | lõi-cad | Có | ✅ ĐÓNG 07/08 (phiên p9, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M1-15 | T3 · vòng SỬA | **Byte điều khiển thô trong mã nguồn làm cả tệp tàng hình trước công cụ tìm kiếm mặc định** — tệp bị xếp loại nhị phân, lệnh tìm kiếm trả rỗng GIẢ (exit 1) thay vì báo lỗi; `-a`/`--text` mới thấy. Hệ quả: luật "tìm trước khi thêm" cho kết quả sai ở tệp đó — vòng này báo sai 2 lần vì nó. Quét toàn repo: **chỉ 1 tệp dính** (đã được gỡ lúc 15:54, tệp nay là văn bản thuần). Còn lại là **bẫy công cụ vẫn treo**: cần luật cấm gõ byte điều khiển thô + 1 lệnh quét trong CI. | lõi-cad · vệ sinh mã | Có | 🟠 tệp đã sạch, bẫy công cụ CHƯA có rào | ✅ ĐÃ SỬA 06/08 (M1) — thay ký tự NUL thô bằng escape `\u0000`, chuỗi lúc chạy y hệt; `lib/cad/dxf.ts` nay là văn bản UTF-8, grep thấy được. Chính lúc gỡ nó mới lộ ra 1 chỗ ghi tên khách mà grep đã bỏ sót. |
| G-M1-16 | T3 · vòng SỬA | **Phỏng đoán của máy bị ghi vào file giao cho bên thứ ba**: bản xuất DXF mang cả cờ suy đoán loại cấu kiện. Nạp lại trong IF thì vẫn là suy đoán (đúng), nhưng người nhận ngoài IF không phân biệt được đâu là khai báo của người, đâu là máy đoán. | lõi-cad | Cần Hoà chốt | 🟡 mới, chờ quyết |
| G-M1-17 | T3 · vòng SỬA | **Chọn cả cụm ở cấp ngoài cùng = chọn gần cả bản vẽ** (đo: cụm ngoài cùng ôm 2.963/2.984 hình). Đường chọn theo cấp ngoài cùng hiện chưa nối vào giao diện — nối thẳng vào là ra thao tác vô dụng. | lõi-cad + vỏ CAD | Có | 🟡 mới |
| G-M1-18 | T3 | 🔴 **File DXF do IF xuất ra KHÔNG mở được bằng bộ đọc DXF chuẩn** — thử `ezdxf` (kể cả chế độ cứu hộ): hỏng 6/6 file, trong khi file gốc mở tốt. Cô lập được nguyên nhân thứ nhất: `LWPOLYLINE` thiếu dấu lớp con `100/AcDbPolyline` (file chỉ có line/circle/text/dim thì mở sạch). Có sẵn từ trước, bản `HEAD` hỏng y hệt — nhưng nó CHẶN phép nghiệm thu "giao file cho người khác sửa tiếp". | lõi-cad | Có | ✅ ĐÃ SỬA 06/08 (M1) — ghi `100 AcDbEntity` + `100 AcDbPolyline` cho LWPOLYLINE và `100 AcDbHatch` cho HATCH. Bộ đọc độc lập (`ezdxf`) nay mở **6/6 file, 0 lỗi audit**; bung khối ra đúng 12.274/11.775/10.085/9.891/10.035/2.984 hình — khớp tuyệt đối số entity trong `Doc`. Khoá bằng `dxf-openable.test.ts` (20 ca) |
| G-M1-19 | T3 | 🔴 **Bộ suy loại cấu kiện tự bật cho MỌI nơi gọi**, kể cả đường nạp block thư viện ⇒ **28/54 block bị gán sai**: layer đặt theo tên phòng (`NT_VAN_PHONG`, `NT_PHONG_KHACH`) khớp luật "phòng" nên sofa · giường · bàn làm việc · tủ hồ sơ thành `space`; nơi tiêu thụ có thật xếp chúng thành **ghi chú** khi trình bày mặt bằng. Hồi quy do chính vòng sửa 06/08. | lõi-cad + thư viện | Có | ✅ ĐÃ SỬA 06/08 (M1) — `block-library.ts` truyền `inferRules: null`; đo lại kho block: **30/54 → 0/54** bị gán loại (455 hình `space` sai nay = 0). Khoá bằng `block-library-infer.test.ts` (khoá CẢ hành vi lẫn đường gọi, + ca đối chứng chứng minh test có răng) |
| G-M1-20 | T3 | Ô duyệt trước khi xuất DXF vẫn ghi **"đã làm phẳng block — mở lại sẽ không còn cấu trúc block"** trong khi file vừa ghi có tới 457 khối; và `insertsWritten`/`blockDefsWritten`/`preservedBlocks`/`elementTypes` **khai rồi bỏ đó, 0 nơi tiêu thụ trên giao diện** (vi phạm K4). Người dùng không hề biết bản vẽ vừa được tự phân loại 10.957 hình. | lõi-cad + vỏ CAD | Có (nối dây) | ✅ ĐÃ SỬA 06/08 (M1) — nhãn + tiêu đề + câu cảnh báo nay nói đúng phần GIỮ ĐƯỢC trước phần mất; `insertsWritten`/`blockDefsWritten`/`preservedBlocks`/`elementTypes` đều có nơi tiêu thụ thật trên giao diện (mục "Tự phân loại" mới + hộp duyệt xuất DXF). Bảng "Bỏ qua" nay dùng chung từ điển với thanh trạng thái. Verify browser thật |
| G-M2-01 | T1 | **Một bức tường không phải MỘT vật**: vùng tô (poché) và đường bao là hai hình rời không liên kết. Bấm chọn chỉ trúng một nửa; dời đi thì tường **rách làm đôi** (đo: nửa tô sang chỗ mới, nửa bao đứng lại, lệch 450 mm) — không cảnh báo, không nhóm lại được. | lõi-cad | Có | 🔴 chưa sửa |
| G-M2-02 | T1 | **2D và 3D đọc hai nửa khác nhau của cùng bức tường** ⇒ sau khi sửa, cùng một bức tường đứng ở **hai vị trí khác nhau** tuỳ chặng đang mở (3D chỉ dựng từ vùng tô). Không màn nào báo hai bản đang lệch. | lõi-cad + 3D | Có | 🔴 chưa sửa |
| G-M2-03 | T1 | **Diện tích ghi trên bản vẽ là chữ chết**: đổi hình phòng thì nhãn "… m²" vẫn y nguyên, trong khi thanh trạng thái tính lại số khác (đo cùng một màn: nhãn giữ nguyên, tổng chạy 59,9 → 58,5 → 51,2 m²). Hai con số mâu thuẫn hiển thị cạnh nhau, không ai bắt lỗi. | lõi-cad | Có | 🔴 chưa sửa |
| G-M2-04 | T1 | **Không có đối tượng PHÒNG**: nhãn phòng chỉ là chữ, biên phòng dò lại mỗi lần vẽ ⇒ diện tích đổi theo sửa đổi hình học lân cận; phòng dò không ra biên thì **im lặng rơi khỏi tổng** thay vì báo "không đo được". Không có chỗ treo trần/sàn/phào của phòng. | lõi-cad | Có | 🔴 chưa sửa (đúng §0.5 SPEC-TANG-DU-LIEU-CAU-KIEN) |
| G-M2-05 | T1 | **Chặng 3D không có Hoàn tác**: ⌘Z ở chặng 3D không làm gì và không báo gì (đo 2 lần, dữ liệu không đổi). Lịch sử thao tác sống riêng trong từng màn (5 ngăn rời) ⇒ sửa ở chặng này, lùi ở chặng kia là không thể. | hạ tầng | Có | 🔴 chưa sửa |
| G-M2-06 | T1 | **Lệnh sửa hình không có bản xem trước**: dời/xoay/lấy đối xứng chỉ hiện dây thun + số đo, **không hiện bóng vật sắp dời** ⇒ chỉ thấy kết quả sau khi đã chốt. (Bóng theo con trỏ mới có ở bước đặt khối mới.) | vỏ CAD | Có | 🔴 chưa sửa |
| G-M2-07 | T1 | **Hình dẫn xuất tự chảy vào bản vẽ và bị đếm như hình người vẽ**: sửa 1 tường ⇒ bản vẽ tự sinh thêm hình khoét cửa/cửa sổ, bộ đếm "tường chưa phân loại" nhảy sau một thao tác không liên quan (đo 29 → 38). Ngược lại, dời tường làm **mất im lặng** khối khoét của cửa mất tường chủ (đo 128 → 126 hình). | lõi-cad | Có | 🔴 chưa sửa |
| G-M2-08 | T1 | **Số khai ≠ hình vẽ, không ai đối chiếu**: khai "dày 220 mm" trong khi vùng tô vẽ 100 mm — cả hai cùng tồn tại, không cảnh báo lệch, không rõ hồ sơ/BOQ lấy số nào. Ô khai độ dày cũng chỉ ghi khi rời ô (gõ xong chưa đủ). | lõi-cad | Có | 🔴 chưa sửa |
| G-M2-09 | T1 | **Phiên đăng nhập hết hạn giữa lúc sửa = mất sạch Hoàn tác/Làm lại** và bản vẽ biến mất khỏi màn hình (khung nhìn hỏng), chỉ báo bằng một băng chữ nhỏ ⇒ người dùng tin là mất bài. Bản vẽ thực tế còn, nhưng lịch sử thao tác thì mất thật. | hạ tầng | Có | 🔴 chưa sửa |
| G-M2-10 | T1 | **Nghi mất bước Hoàn tác**: quan sát 1 lần — sau một thao tác gán thuộc tính, ngăn Hoàn tác tụt 1 bước và bấm lùi nhiều lần vẫn không đưa tường về chỗ cũ. **CHƯA tái hiện được** ở lần thử lại ⇒ ghi để điều tra, không kết luận. | hạ tầng | ? | 🟡 nghi vấn, chờ tái hiện |
| G-M4-01 | vo-app | **KHÔNG PHẢI LỖI SẢN PHẨM — đã điều tra xong, ghi lại để phiên sau khỏi log lại.** Triệu chứng ban đầu: một route CÓ trang thật, biên dịch sạch, vẫn trả 404 (`/login` 3 lần liên tiếp, kể cả sau khi ép biên dịch lại; server khác thì `/` và `/cad-editor` cũng vậy). Nhưng lần đo sau `/login` trả **200**. Nhật ký máy chủ phát triển lộ đúng cơ chế: `✓ Compiled /login → ✓ Compiled /_not-found → GET /login 404`, rồi gọi lại là 200 — **lần gọi đầu ngay sau khi một route vừa biên dịch đôi lúc rơi vào nhánh không-tìm-thấy**. Đây là tật của máy chủ phát triển (Next 14.2), KHÔNG có trong bản dựng phát hành. ⚠️ Hệ quả THẬT cần nhớ: **404 trên máy chủ phát triển chạy lâu không phải bằng chứng lỗi** — phải đo lại trên tiến trình mới rồi mới kết luận. Một máy chủ chạy >1 ngày trong phiên này 404 cả `/` lẫn `/login` trong khi trang khác vẫn 200. | (không) | Không | ⚪ đã rút, không phải GAP |
| G-M4-02 | vo-app | **Công tắc chế độ màn vẽ 2D lộ KHOÁ KỸ THUẬT ra mặt người dùng**: ba nút hiện đúng chữ `Sketch` · `Pro` · `Revit` (khoá nội bộ, lại còn mượn tên một phần mềm của hãng khác), trong khi bộ tên chính thức đã chốt (`Sơ phác` · `Kỹ thuật` · `Nội thất`) bị đẩy vào **tooltip** — ngược đúng chiều. Trái luật ngôn ngữ chỉ dẫn ("cấm jargon nội bộ lộ UI") và trái chốt tên 03/08; cũng ngược cách đã làm cho tên khối 05/08 (nhãn Việt trên mặt, tên Anh trong tooltip). Đọc DOM xác nhận, không suy từ ảnh. Điểm sửa gọn: 3 nhãn trong `ModeSwitch`. | vỏ CAD | Có | ✅ **ĐÃ SỬA 06/08** — nút hiện tên chính thức, EN qua `useT` ra `Sketch·Technical·Interior`; khoá kỹ thuật giữ nguyên trong `onChange`. Verify trình duyệt thật: 0 chuỗi khoá lọt ra UI. Chưa commit (V6). |
| (mảng M8 = WORKSPACE/SyncWork — TỔNG audit 06/08, chứng cứ ở `docs/AUDIT-WORKSPACE-SYNCWORK-2026-08-06.md`) |
| G-M8-01 | (mảng) | **Lớp VIỆC không SỬA được việc.** Bảng Kanban đã chạy thật nhưng **CHỈ ĐỌC** — kéo-thả không ghi ngược nguồn dữ liệu (`components/dashboard/LarkPanels.tsx:8` ghi rõ *"kéo-thả kanban KHÔNG đổi trạng thái"*, `:176` `LarkKanbanTab`, mount `components/Dashboard.tsx:367`). Người dùng nhìn thấy việc nhưng muốn đổi trạng thái vẫn phải mở app dữ liệu bên ngoài ⇒ SyncWork chưa làm đúng nghĩa "lớp việc". Đây là lỗi **định vị**, không phải lỗi màn. | SyncWork | Có | 🔴 chưa sửa |
| G-M8-02 | (mảng) | **0 hạ tầng nhiều người.** `grep -na "yjs\|liveblocks\|socket.io\|partykit\|pusher\|ably" package.json` = **0**. Thứ đang gọi là "cộng tác" chỉ là ghi chú lưu cục bộ (`components/nodes/CommentPin.tsx:26` · `components/CommentLayer.tsx:17` · `components/filemanager/FileManagerShell.tsx:526`). ⇒ **Chat nhóm / con trỏ nhiều người / khoá tranh chấp chưa vẽ được** vì chưa chốt kiến trúc. Đây là nút CHẶN, phải quyết trước khi vẽ bất kỳ màn workspace nào. | SyncWork + hạ tầng | Có | 🔴 CHẶN — chờ chốt kiến trúc |
| G-M8-03 | (mảng) | **Gantt / tiến độ = 0 tuyệt đối** — `grep -rina "gantt" components/ lib/ app/ docs/mocks/` = **0 dòng**. Màn duy nhất trong mảng chưa có một dòng code lẫn một mock nào. Không có đường: việc có deadline (`lib/lark/task-utils.ts`) → trục thời gian → phụ thuộc giữa việc → đường găng. | SyncWork | Có | 🔴 chưa sửa |
| G-M8-04 | (mảng) | **6 màn có ruột, chưa có vỏ (0 file .dc)**: Kanban · Tổng quan dự án (`components/Dashboard.tsx`, mount `home/HomeScreen.tsx:611,:627` + `studio/AppShell.tsx:160`) · Vitals (`components/studio/Vitals*.tsx`) · Notebook (`components/notebook/*` + trang `app/projects/[id]/notebook/page.tsx:180`) · Knowledge (`lib/notebook/rag.ts`) · Lịch/nhắc (`app/api/lark-tasks/*`). Không có hợp đồng giao diện ⇒ mỗi phiên port tự bịa một chuẩn (cùng cơ chế đẻ ra G-M5-03). | giao diện | Có | 🔴 chưa sửa |
| G-M8-05 | (mảng) | **Hai mock tên "collab" KHÔNG phải chat nhóm** — `docs/mocks/mock-mood-collab-g2-2026-08-03.html` `<title>` = *"IF · Dựng ảnh · Mood+Collab"*, `mock-mood-collab.html` = *"Mood + Collab (Miro · tablet/pen · mindmap)"*. Cả hai tả cộng tác **trong chặng Dựng ảnh**. Bản đồ phủ trước đây ghi *"collab + spec"* làm phiên sau tưởng đã có hợp đồng cho chat nhóm dự án. Cùng lớp bệnh với G-M5-05 (đếm mock là "đã có" mà không mở ra xem tả gì). | giao diện | Có (sửa nhãn) | ✅ ĐÃ SỬA 06/08 — nhãn bản đồ đã đính chính, ghi rõ 2 mock thuộc chặng Dựng ảnh |
| (mảng M9 = HỆ SINH THÁI IDF — TỔNG audit 06/08, luật §0v L-EXT1/L-EXT2) |
| G-M9-01 | (hệ) | **Tên nhà cung cấp nằm trong XƯƠNG dữ liệu.** `prisma/schema.prisma:319` `larkRecordId String @unique` (khoá đối chiếu) · `:321` `larkProjectName` · `:322`+`:74` `larkProjectCode` · `:344` `larkAccount @unique`. Tổng **45 file** dính chuỗi `lark` (`grep -rlia "lark" lib/ components/ app/ prisma/`). ⇒ Đổi nhà cung cấp hạ tầng = phẫu thuật lõi + migrate + sửa 29 file code. Thiếu **bảng cầu** `ExternalRef { system, externalId, entityType, entityId }` để lõi không cần biết dữ liệu đến từ đâu. Tầng adapter thì ĐÃ ĐÚNG (`lib/integrations/providers/lark.ts` + `registry.ts:37 REGISTRY`) — hỏng ở tầng lõi và tầng cầu. | hạ tầng | Có | 🔴 chưa sửa — phiếu đã soạn, dán được ngay (`docs/BANG-LENH-2026-08-06-DESIGN-FIRST.md` khối ③, mảng `S6-chuan`) |
| G-M9-02 | (hệ) | **Hai app cùng nhà nối nhau bằng dịch vụ NGOÀI.** `docs/ARCHINOTE-MAP.md:17` — *"Hai app hiện KHÔNG chung định dạng dữ liệu nào. Kênh chung mà spec chủ ý là Lark Base"*. ⇒ IF ↔ ArchiNote không nói chuyện trực tiếp được; nhà cung cấp ngoài chết thì cả hệ IDF đứt, dù cả hai app đều tự làm. `.idf` chưa gánh việc đó — `grep -na "external\|source\|origin" lib/cad/idf.ts` = **0**. | hạ tầng + .idf | Có | ⚪ **HOÃN VÔ THỜI HẠN 07/08** — Hoà chốt: *"archinote chưa code. xử if trước"*. ArchiNote chưa có một dòng code nào ⇒ đây KHÔNG phải lỗ hổng của IF, không tính vào đợt nào. Mở lại khi ArchiNote khởi động. — thuộc phiếu M6·archinote |
| G-M9-03 | (hệ) | **ArchiNote không có bản sao cục bộ.** `docs/ARCHINOTE-MAP.md:130` — *"Toàn bộ ở Lark, không có bản sao local. **Mất mạng = trắng màn.**"* ⇒ app THU dữ liệu hiện trường (số đo · ảnh · ghi âm) mà công trường mất sóng là không dùng được — đúng chỗ nó phải dùng được nhất. Không có tầng cache/offline nào trong `src/`. | ArchiNote | Có | ⚪ **HOÃN VÔ THỜI HẠN 07/08** — Hoà chốt: *"archinote chưa code. xử if trước"*. ArchiNote chưa có một dòng code nào ⇒ đây KHÔNG phải lỗ hổng của IF, không tính vào đợt nào. Mở lại khi ArchiNote khởi động. — thuộc phiếu M6·archinote |
| (delta từ LÀN A apply-design — `M-APPLY-A-OUT.md` §A2.3, TỔNG gộp 06/08 theo §0u) |
| G-A-01 | apply-A | **Kho vật liệu không có cột thông số** (mã · hãng · nguồn · đơn vị · **giá** · nhám/bóng) ⇒ chọn vật liệu xong không dùng để dự toán được. Mock gọi khối `CotThongSo` nhưng file con không tồn tại. | thư viện | Có | 🔴 chưa sửa |
| G-A-04 | apply-A | **Hợp đồng thiết kế RỖNG**: 4 khối `dc-import` của `Thư viện.dc.html` (`KeVatLieu`·`KeDoDac`·`KeDangGom`·`CotThongSo`) trỏ vào file **không tồn tại** ⇒ phiên port buộc phải tự chế. `docs/M5-OUT.md:44-45` đã cảnh báo, **tình trạng chưa đổi**. Cùng lớp bệnh G-M5-05. | quy trình thiết kế | Không (sửa mock) | 🔴 chưa sửa |
| G-A-05 | apply-A | **Mock `Thư viện.dc.html` mâu thuẫn với chốt 05/08 của Hoà** (kính vs đặc · dính đáy vs card rời · 214 vs 186 px) — chưa ai gỡ mâu thuẫn ⇒ port theo mock là port sai chốt. | quy trình thiết kế | Không (gỡ mâu thuẫn) | 🔴 chưa sửa |
| G-C-01 | apply-C | **Hộp xuất PDF ở chặng Trình chiếu mới nhận 1 "tờ"** (trang đang mở) và **checklist rỗng** — thanh công cụ chưa cầm `Sheet[]`/dữ liệu kiểm thật. Phiên apply cố ý KHÔNG bịa dữ liệu giả cho đủ mock (đúng §9). | trình bày | Có (nối dây) | 🔴 chưa sửa |
| G-C-02 | apply-C | **Bảng tròn (bút trên giấy/markup) chưa gắn vào thao tác nào** — component đã sẵn (`onPick`/`onClose`/`x`/`y`), thiếu chỗ gọi; vùng đó thuộc làn khác đang giữ. | trình bày + vỏ CAD | Có (nối dây) | 🔴 chưa sửa |
| G-M3-17 | T2 | **Nhập xong không lưu xuống đâu cả** — cửa nhập giữ `room`/`qty`/`confidence` trong `FfeTable` mà `FfeTable` **chỉ sống trong state màn hình** ⇒ nhập 100 món rồi đóng tab là mất sạch phòng + số lượng + độ tin cậy. Chỗ lưu ở DB nay ĐÃ CÓ (`G-M3-08` đóng 06/08) nhưng **chưa ai nối đường ghi từ cửa nhập xuống**. Phiên `1·fix-gocc` tự khai mục này ở `M-FIX-C-OUT` — tách mã riêng để không lẫn với `G-M3-08`. | kho vật liệu + FF&E | Có (nối dây) | 🔴 chưa sửa — đã giao ở phiếu vòng 2 (`docs/PHIEU-CODE-3-PHIEN-2026-08-06.md`, cửa sổ `1·fix-gocc` mục 1) |
| (mảng M10 = SyncWork lõi — TỔNG gộp từ `PHU-OUT.md` 06/08 23:00, §0u) |
| G-M10-01 | (hệ) | **IF KHÔNG CÓ `model Task` nội bộ nào** — chỉ có bản sao CHỈ ĐỌC từ nguồn ngoài (`LarkTaskRef`). Đây là **gốc chặn kép**: Kanban không ghi ngược được (`components/dashboard/LarkPanels.tsx:176-210` chỉ đọc), và Gantt không có gì để dựng. PHU gộp `G-P-01`+`G-P-02` thành một hạng mục vì cùng một gốc. ⇒ **Không dựng được lớp VIỆC của SyncWork cho tới khi có model này.** Thiết kế đã có: `docs/SPEC-GANTT-DATA.md`. | SyncWork + hạ tầng | Có | 🔴 chưa sửa — CHẶN `G-M8-01` và `G-M8-03` |
| G-M10-02 | (hệ) | **`ExternalRef` mới khai schema, CHƯA migrate** (`prisma/schema.prisma:478-479`) ⇒ đường nối Task ↔ nguồn ngoài mới là thiết kế trên giấy, chưa dùng được. Cùng lớp bệnh `G-M3-08` trước khi migrate: khai cột ≠ có cột. | hạ tầng | Có | 🔴 chưa sửa — cần chủ dự án chạy `prisma db push` |
| G-M10-03 | (sổ) | **Ba file sổ mô tả LỆCH hiện trạng** — `docs/LICENSE-NOTES.md §5` nói *"chưa đưa vào CI"* nhưng script đã nối vào `npm test` · `docs/mocks/README-mocks.md` lệch 3 điểm · cặp mock *"Lịch việc"* trùng chưa ai đánh dấu bản chốt. Đúng lớp bệnh §0i (sổ trỏ sai thì phiên sau tự nghĩ ra chuẩn của mình). PHU phát hiện nhưng KHÔNG tự sửa — đúng ràng buộc phiếu. | sổ sách | Không (sửa doc) | 🔴 chưa sửa — việc của TỔNG |
| G-M10-04 | (hệ) | **Mock Gantt chưa mở khoá được** — `docs/mocks/Tiến độ · Gantt.dc.html` là bản xuất công cụ thiết kế, thiếu tệp kịch bản `support.js` ⇒ không mở ngoài công cụ đó, không đối chiếu field với `SPEC-GANTT-DATA.md` được. Cùng lớp `G-M5-05` (10/67 trang cùng bệnh). | giao diện | Có | 🔴 chưa sửa |
| G-M10-05 | (hệ) | **`.idf` có 0 field `external`/`source`** — xác nhận lại bằng đọc trực tiếp `lib/cad/idf.ts` (230 dòng). Cần `IdfFieldNote` (additive, bump `IDF_VERSION` → 3 theo pattern `IDF_MIGRATIONS` `idf.ts:72-97` đã có sẵn) + `ExternalRefLite` mới làm được cầu ArchiNote theo **L-EXT2**. Thiết kế đã có: `docs/PHUONG-AN-CAU-IDF.md`. Module "Hiện trường" của ArchiNote hiện **0% code** ⇒ đây là spec chuẩn bị trước (đúng §9), không phải audit luồng đang chạy. | .idf + ArchiNote | Có | 🔴 chưa sửa |
| G-M10-06 | (sổ) | **Docstring `lib/cad/idf.ts:1-6` không khớp interface thật** — docstring liệt `entities/layers/markups/photos` nhưng `interface IdfFile` (`idf.ts:109-131`) KHÔNG có field `photos` cấp `IdfFile`. PHU gắn nhãn CHƯA VERIFY vì chưa đọc `lib/cad/model.ts` (1.273 dòng) để biết `photos` có nằm trong `Doc` không. Cùng lớp bệnh §0i — sổ/chú thích trỏ sai thì phiên sau tin theo. | sổ sách | Không (sửa docstring) | 🔴 chưa sửa — cần phiên có quyền đọc `model.ts` |
| G-M11-01 | (hạ tầng) | **`jose` dùng API Node không có trong Edge Runtime.** `next build` 07/08 cảnh báo `CompressionStream`(:10) + `DecompressionStream`(:26) ở `node_modules/jose/dist/webapi/lib/deflate.js`, kéo từ `jose/index → jwe/flattened/decrypt`. Chỉ nổ khi middleware phải giải nén JWE CÓ NÉN; JWT thường không nén ⇒ hiện chưa hỏng. Bom hẹn giờ nếu đổi nhà cung cấp đăng nhập. | hạ tầng | Có | 🟠 theo dõi — chưa gây lỗi thật |
| G-M11-02 | (hạ tầng) | **8 trang vượt ngưỡng First Load JS.** Đo lần đầu bằng `next build` 07/08 (trước đó chưa ai đo): `/` và `/projects/[id]/render` **734 kB** · `/materials` 716 · `/projects/[id]/cad` 712 · `/colors` 675 · `/projects/[id]/present` 640 · `/settings` 568 · `/files` 564. Ngưỡng Next.js khuyến nghị 250 kB. Chia chung 88,2 kB. Electron nạp từ ổ cứng nên ít thấy; bản web thấy rõ. | hạ tầng | Có | 🟡 xếp đợt 6 — không gấp |
| G-M12-01 | (chất lượng) | **`components/` che phủ test 0%.** Đo 07/08: `lib/` 70.158 dòng nguồn / 30.720 dòng test = **43%**; `components/` 71.004 dòng nguồn / **170 dòng test** = **0%**; `app/` 6.845 dòng / **0**. 16 mảng có 0 test, tổng **48.589 dòng nguồn**, gần trọn là `components/`: `present-editor` 13.696 · `cad` 11.378 · `render-studio` 5.111 · `nodes` 2.556 · `photo-editor` 2.205 · `three` 1.750 · `avatar` 1.744 · `library` 1.599 · `notebook` 1.319 · `print` 1.258 · `ui` 1.155 · `materials` 1.077 · `form` 1.032 · `intro` 1.016 · `colors` 865 · `filemanager` 828. ⇒ 3.016 phép kiểm xanh CHỈ nói về động cơ; lớp người dùng chạm vào chưa có lưới an toàn nào. | chất lượng | Có | ✅ ĐÓNG 07/08 (phiên p12, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M13-01 | (vỏ) | **9 chỗ gọi mạng/xuất file KHÔNG bắt lỗi** — hỏng mạng thì màn hình im lặng, không báo, không lùi được. Đo 07/08 (script quét `await fetch` / `.json()` / `await export*` không có `catch` trong ±14 dòng): `components/entry/LoginForm.tsx:106,111` (**đăng nhập** — nặng nhất) · `components/notebook/useNotebook.ts:148,222,227` (nạp tài liệu + hỏi AI) · `components/present-editor/PresentEditor.tsx:263,268` · `components/render-studio/ToolModeForm.tsx:570` · `components/entry/LoginBackdrop.tsx:202`. | vỏ | Có | ✅ ĐÓNG 07/08 (phiên p2, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M13-02 | (trung tính) | **Tên khách thật hiện lên giao diện.** `lib/filemanager/mock-data.ts:50` = `'2026-06 Detech Complex'` · `:216` = `'Detech-brief-khach.pdf'`. Không phải comment — chuỗi hiển thị, có đường ra màn: `lib/filemanager/queries.ts:3` → `app/settings/_components/StorageCard.tsx:10`. Vi phạm luật trung tính. | trung tính | Có (đổi tên mẫu) | ✅ ĐÃ SỬA 07/08 (p6) — verify TỔNG 15:3x: `grep -c Detech lib/filemanager/mock-data.ts` = **0**. P2 báo còn (`M-SOI-16-MANG-OUT:417`) nhưng báo cáo đó viết 13:10, p6 sửa 14:13 ⇒ báo cáo lỗi thời, không phải sai |
| G-M13-03 | (vỏ) | **`components/print` 1.258 dòng chưa có trang nào mount.** `ls app \| grep print` = 0. `LineweightTable` `PaperSheetFrame` `RadialToolMenu` mỗi cái chỉ 1 nơi nhắc và đều là COMMENT; chỉ `ExportPdfDialog` có 4 nơi thật. Đây là 4 mock in ấn `4·apply-ingiay` port xong chưa nối. Số đo cụ thể cho `G-C-01/02`. | vỏ | Có (nối dây) | ✅ ĐÓNG 07/08 (phiên p12, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M14-01 | (dữ liệu) | 🔴 **41/41 Flow có `projectId = NULL` — không flow nào gắn dự án, và không dự án nào có flow.** Đo 07/08 trên `prisma/dev.db` đang chạy: `select count(*) from Flow where projectId is null` = 41/41; 7 Project đều 0 flow (4 cái tiền tố `__nb:` là notebook tự sinh). Hệ quả: app tự mở `/projects/cms915kza0001w9a613z8tp65/render` — id đó là **flowId mồ côi**, không phải projectId. Chặng Render chạy (làm việc trên flow), chặng CAD/Present không có gì để hiện (cần dữ liệu thuộc Project). `lib/project-scope.ts:73-74` đã lường trước ca này nhưng không báo cho người dùng. **KHÔNG phải lỗi nối chặng** — đường dây `StageSwitcher:268 → AppChrome:222 → stage-nav.ts:37 → project-scope.ts:52 → scope-core.ts:69` đã grep đủ, không thiếu mắt xích. | dữ liệu + vỏ | Có | 🔴 chưa sửa — VIỆC SỐ MỘT |
| G-M14-02 | (vỏ) | **Màn chặng không có dữ liệu thì TRẮNG TRƠN, không nói gì.** Vào `/projects/<flowId>/cad` khi flow mồ côi ⇒ không có trạng thái trống, không hướng dẫn, không nút thoát. Người dùng không biết mình đang thiếu gì. Nặng hơn cả `G-M14-01` về mặt trải nghiệm. ⚠️ CHƯA VERIFY bằng mắt — extension Chrome lỗi `Script injection timed out` trên mọi trang kể cả `example.com`, không chụp được màn. | vỏ | Có | 🔴 chưa sửa |
| G-M15-01 | (giao diện) | **Nút chặng không được chọn MỜ tới mức trông như bị khoá** — người dùng không bấm vì tưởng bị vô hiệu hoá. `components/studio/StageSwitcher.tsx:280` dùng `color: on ? 'var(--t1)' : 'var(--t4)'`. Đo tương phản trên nền panel: `--t4` chế độ sáng `#9a938a` = **2,60:1** · chế độ tối `#6e6e78` = **4,32:1** — cả hai trượt luật **G2** (≥4,5:1); chặng đang mở `--t1` `#211e19` = 14,22:1. Nút KHÔNG hề bị khoá (`:264-280` không có `disabled`/`pointerEvents:none`) — **giao diện nói dối**. Đề nghị: dùng `--t3` hoặc đậm hơn cho tab không active, giữ `--t1` cho tab đang mở. | giao diện | Có | ✅ ĐÓNG 07/08 (phiên p3, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M15-02 | (giao diện) | **Nút chặng gộp tên CHẶNG với tên MODE.** Thanh chặng hiển thị `"Thiết kế 2D · Sơ phác"` — nhưng theo chốt 07/08, "Thiết kế 2D" là CHẶNG còn "Sơ phác/Chuyên" là MODE **bên trong** nó (mode đã có dải riêng `Sơ phác · Kỹ thuật · Nội thất` ở thanh công cụ dưới, nhìn thấy trên màn 11:40). Nút chặng chỉ được ghi `"Thiết kế 2D"`. Kèm theo: 2/3 `tagline` trong `lib/phases.ts` chưa khớp chốt — `:36` (concept) thiếu **Revit 2D** + khái niệm 2 mode; `:51` (render) thiếu **Revit 3D** + **dựng khối**. ID `concept`/`render`/`present` GIỮ NGUYÊN, chỉ đổi nhãn. | giao diện | Có | ✅ ĐÓNG 07/08 (phiên p3, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M15-03 | (sổ lệch) | ✅ ĐÓNG 07/08 — **HAI BỘ "3 CHẶNG" KHÁC NHAU dùng chung một chữ.** `docs/IF1_IF2_BIGPICTURE.md:41-48` gọi "3 Chặng IF2" = ① CAD kỹ thuật ② BIM/IFC 4.0 ③ Viewer 3D; chốt 07/08 (`00-CHOT.md`) gọi 3 chặng IF = ① Thiết kế 2D ② Thiết kế 3D ③ Trình chiếu. Phiên nào đọc BIGPICTURE trước sẽ hiểu "chặng 3" = Viewer 3D thay vì Trình chiếu. ⇒ Thống nhất: "chặng" CHỈ dùng cho bộ của chốt 07/08; bộ IF2 gọi là "mảng IF2". Phải sửa nhãn trong BIGPICTURE hoặc đánh dấu bản cũ. | sổ | Có | ✅ ĐÓNG 07/08 (phiên p3, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M15-04 | (sản phẩm) | ✅ **ĐÃ CHỐT 07/08 — người dùng TỰ BẤM chọn mode**, áp cho cả Thiết kế 2D (Sơ phác·Chuyên) lẫn Thiết kế 3D (Dựng khối·Render). `IF1_IF2_BIGPICTURE.md:22-25` ("tự động theo role+stage, not user-toggled") **BỊ HUỶ**. Code đã đúng hướng: `lib/cad/store.ts:156` đặt `cadMode==='pro'\|\|'revit'` TRƯỚC hai dòng role ⇒ lựa chọn thủ công thắng. | sản phẩm | — | ✅ đã chốt |
| G-M15-05 | (sổ lệch) | **`lib/cad/store.ts:156` gọi lựa chọn thủ công là "override thủ công (backward-compat)"** — sai chữ sau chốt 07/08. Người dùng tự bấm chọn mode nay là **đường CHÍNH**, không phải override tạm. Sửa comment cho khớp, kẻo phiên sau tưởng là đường phụ rồi bỏ đi. Logic KHÔNG cần sửa — `:156` đã đặt trước hai dòng `role` nên thủ công đã thắng. | sổ | Có (chỉ comment) | ✅ ĐÓNG 07/08 (phiên p3, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M15-06 | (giao diện) | ✅ ĐÓNG 07/08 — **Chữ "CAD" còn hiện cho người dùng ở 14 chỗ** — Hoà chốt 07/08 *"có cad là sai thôi"*, nhãn phải là "Thiết kế 2D". Đã grep: `lib/phases.ts:37` · `components/present-editor/PresentEditor.tsx:337` · `components/render-studio/ModeSwitchCell.tsx:33` · `components/LibraryPanel.tsx:25` · `lib/library/types.ts:86` · `lib/refingest.ts:45` · `lib/library/shelves.ts:155` · `app/library/ingest/page.tsx:16,291` · `components/settings/GuModelSettings.tsx:130,131` · `lib/nodes/registry.ts:210` · `lib/nodes/defs/render-v2.ts:292`. ⚠️ **CHỈ đổi NHÃN** — `lib/cad/` `components/cad/` `useCadStore` `CadMode` route `/cad` GIỮ NGUYÊN (đổi là vỡ route + localStorage + DB). ⚖️ Chỗ nói về ĐỊNH DẠNG TỆP (DWG/DXF) thì "CAD" vẫn đúng nghĩa — phân loại từng dòng, CẤM thay thế hàng loạt. | giao diện | Có | ✅ ĐÓNG 07/08 (phiên p3, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M16-01 | (dữ liệu) | 🔴 **Cấu kiện bị CHẺ LÀM ĐÔI — hình học và dữ liệu không nối nhau.** Đo 07/08: hình học ở `public/cad-library/manifest.json` (54 block `.dxf`, 12 nhóm) — 0 dữ liệu kèm; tham số ở `lib/cad/workstation-clusters.ts` `CLUSTER_SPECS` (20 mục) — sinh trong code, không lưu ra tệp được; dữ liệu ở `model ProductSpec` (kind·name·brand·sku·vendor·w/d/hUp·materials·finishes) — 0 hình học. Ghế trong ProductSpec và ghế trong .dxf là hai thứ khác nhau. Gốc của `G-A-01`. ⇒ **Hoà chốt 07/08: dựng format `.idfc`** = một tệp cho MỘT CẤU KIỆN, gói hình học + dữ liệu + tham số, dùng lại giữa mọi dự án (cặp `.idf`/`.idfc` ↔ Revit `.rvt`/`.rfa`). Không phá luật "cấm đẻ format thứ hai" (`IDF-TONG-QUAN §10a`) vì đó là hai CẤP ĐỘ, không phải hai format cho cùng một thứ. | dữ liệu | Có | 🔴 chưa sửa |
| G-M16-02 | (giao diện) | 🔴 **NĂM khái niệm "thư viện" rời nhau, không cái nào biết cái nào.** `lib/library/` 795 dòng (tự khai *"Master Library — KHÔNG lộ ra UI"*, `types.ts:15`) · `components/library/` 1.599 dòng **0 test** · `components/LibraryPanel.tsx` panel cũ · `components/NodeLibraryPanel.tsx` thư viện node · `components/cad-library/` 318 dòng tự khai *"DEMO độc lập"* (`BlockLibraryDemo.tsx:2`). ⇒ **Hoà chốt 07/08: gộp HẾT về MỘT tấm**, chia kệ theo loại — Cấu kiện · Vật liệu · Node · Ảnh tham chiếu. Khớp `docs/SPEC-STAGE-LIBRARIES.md` (chốt 02/08). | giao diện | Có | 🔴 chưa sửa |
| G-M16-03 | (dữ liệu) | 🔴 **`.idfc` phải gói đủ BA MẶT cho ba chặng, không chỉ hình+dữ liệu** (Hoà chốt 07/08). ① Thiết kế 2D: ký hiệu·block CAD·dữ liệu Revit-style — đã có `public/cad-library/*.dxf` (54 block) + `lib/cad/block-library.ts`, nhưng KHÔNG mang dữ liệu. ② Thiết kế 3D: khối 3D·PBR·render — đã có `lib/three/cad-to-obj.ts` + `csg.ts` + `lib/materials/pbr-from-category.ts` + `export-d5.ts`/`export-vray.ts`, nhưng 3D suy từ MẶT BẰNG chứ không gắn vào cấu kiện. ③ Trình chiếu: giá·thông số — đã có `ProductSpec.priceVnd` (Decimal) + `wastagePercent`, nhưng không nối từ bản chèn ngược về. ⇒ Ba mảnh có sẵn, **thiếu sợi dây gộp chúng vào MỘT cấu kiện**. Đây là luật K1 áp xuống cấp cấu kiện. | dữ liệu | Có | 🔴 chưa sửa |
| G-M16-04 | (dữ liệu) | 🔴 **Sửa một cấu kiện phải lan sang cả 5 nơi: 2D · 3D · hồ sơ khách · GIÁ · TIẾN ĐỘ** (Hoà chốt 07/08 — điểm ăn tiền của sản phẩm, Revit/SketchUp/D5 không có vì phải xuất-nhập). Ràng buộc: ① lan MỘT CHIỀU `.idfc`→chặng, chặng KHÔNG ghi ngược vào mẫu gốc (KS3/KS4); ② bản chèn giữ được ĐÈ cục bộ qua `srcInsertId` (`model.ts`, G-M1-06/07/18 đã đóng); ③ nhánh TIẾN ĐỘ phụ thuộc `model Task` chưa có (`G-M10-01`, phiếu P1) ⇒ làm ①②③+giá trước, nối tiến độ sau, ĐỪNG chặn nhau. | dữ liệu | Có | 🔴 chưa sửa |
| G-M15-07 | (giao diện) | 🟡 **P0 làm đúng 5/5 việc, nhưng KIỂM CHÉO 07/08 lộ 3 chỗ cần sửa lại.** ① SAI NGỮ NGHĨA: `lib/nodes/defs/render-v2.ts:292` đổi thành `'Tầng lõi tất định (Thiết kế 2D→OBJ extrude)'` — câu này mô tả KỸ THUẬT đùn khối từ hình học, không phải tên chặng; "Thiết kế 2D→OBJ extrude" đọc vô nghĩa. Trả về "CAD→OBJ extrude" hoặc viết "hình học 2D→OBJ extrude". ② SÓT: `lib/library/types.ts:87,88` `STAGE_META` mới đổi `cad→'Thiết kế 2D'`, còn `render: 'Render'` và `present: 'Present'` VẪN tiếng Anh cụt — phải là "Thiết kế 3D" / "Trình chiếu" theo chốt 07/08. ③ BẤT NHẤT: `components/LibraryPanel.tsx:25` đổi `'CAD / Sketch'→'Thiết kế 2D / Sketch'` (lẫn tên CHẶNG vào danh sách LOẠI NỘI DUNG: Ref nội thất·Style dàn trang·Vật liệu), trong khi `lib/refingest.ts:45` cùng bản chất lại GIỮ `'CAD / Bản vẽ'`. Chọn một chuẩn. | giao diện | Có | 🔴 chưa sửa — rẻ |
| G-NB-01 | (luồng node) | **Dây nối SAI KIỂU chỉ đếm được, không nhìn ra dây nào** trên bảng nút — thiếu một lần gắn class đỏ (hàm trả `edgeId` đã có). Nguồn: `docs/M-NODE-BOARD-OUT.md:135`. | luồng node | Có (nối dây) | ✅ ĐÃ SỬA 06/08 (làn A vòng 2) — verify 07/08 bằng đọc code: `components/FlowCanvas.tsx:400-401` gán `className 'bn-edge-bad'` + `stroke: 'var(--danger)'` + `strokeDasharray: '6 5'` cho đúng cạnh `findMistypedEdges` trả về, dùng CHUNG luật so kiểu với `StatusBar`. Verify browser thật |
| G-NB-02 | (luồng node) | **Luồng node không có kiểu cổng 'vật liệu'** dù thư viện vật liệu (`matId`) đã là công dân hạng nhất ở 2 chặng khác ⇒ swatch vật liệu không đi qua dây được. Cùng họ `G-M3-02`. Nguồn: `docs/M-NODE-BOARD-OUT.md:136`. | luồng node | Có | ✅ ĐÃ SỬA — verify 07/08: `lib/types.ts` `DataType` nay có `'material'` (bộ đủ: image·mask·material·number·table·text·video). Chưa verify swatch đi qua dây được trên browser |
| G-NB-03 | (design system) | **Màu 3 kiểu cổng còn lại (`text`/`video`/`table`) vẫn hex rời, không đổi theo theme** — nền Kem giữ nguyên giá trị nền Mực. Nguồn: `docs/M-NODE-BOARD-OUT.md:137`. Verify 07/08 (`lib/types.ts:163-172`): `table` ĐÃ sửa (dùng token `--accent-warm`), nhưng **`text: '#38bdf8'` (:165) và `video: '#fb7185'` (:168) VẪN hex cứng** ⇒ hai màu này không đổi theo theme. | design system | Có | ✅ ĐÓNG 07/08 (phiên p2, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M17-01 | (dữ liệu) | 🔴 **VẬT LIỆU bị chẻ BA, không mảnh nào biết mảnh nào** — đo 07/08. ① `MaterialPbr` (`lib/materials/schema.ts`) = THỊ GIÁC: baseColor·roughness·metallic·specular·normalUrl·heightUrl·aoUrl·emissive·opacity·transmission·clearcoat·sheen — đủ chuẩn glTF metal/rough, khớp thẳng D5/Enscape, V-Ray có `export-vray.ts` dịch. **0 giá, 0 NCC, 0 thi công.** ② `ProductSpec` (DB) = THƯƠNG MẠI: vendor·supplierId·priceVnd·wastagePercent·packagingSpec·unit·altSku. **0 thông số render.** ③ `MaterialDef` (`lib/cad/materials.ts:29`) = 2D: hatch·color·tones. **0 khoá nối sang ProductSpec.** `grep "ProductSpec" lib/materials/*.ts` = 2 dòng COMMENT, 0 code nối. ⇒ Đổi vật liệu ở kho thì render KHÔNG đổi, giá KHÔNG đổi. Luật 2.1.9.i (30/07) cố ý tách — hợp lý (render không cần giá; giá đổi hằng ngày, texture thì không) ⇒ **cách sửa KHÔNG phải nhồi giá vào PBR mà THÊM KHOÁ NỐI**, để một cấu kiện trỏ được cả ba. Áp cho cả **furniture** và **fit-out**, không chỉ vật liệu. | dữ liệu | Có | 🔴 chưa sửa — gốc của G-M16 |
| G-M17-02 | (lõi 3D) | **Lệnh dựng khối quá mỏng so với chuẩn ngành.** Đo 07/08: `lib/three/csg.ts:17` có đủ 3 phép boolean (union·subtract·intersect); `build-ops.ts` chỉ 2 hàm (`geometryOf`·`resolveGroupGeometry`). **Thiếu hẳn**: bevel · chamfer · loft · sweep · revolve · array · mirror · taper — đều là lệnh cơ bản của 3ds Max/Blender. Với nội thất, **bevel/chamfer là thiếu nặng nhất** (mọi cạnh bàn tủ đều bo). Đối chứng phần LÀM TỐT: ánh sáng gần đủ chuẩn (`lighting.ts:154` `sunFromDateTime` — nắng theo ngày giờ thật + `julianDayFromMs` thiên văn; `RoomLightKind` ceiling/wall/strip/spot), camera nói bằng tiêu cự thật (`camera.ts:36` 24/35/50/85mm + `fovFromLens`). | lõi 3D | Có | ✅ ĐÃ SỬA 07/08 (p14) — `lib/three/build-ops.ts` từ 2 hàm lên 13: `prismBeveledEx`(:295 bán kính mm·segments 1=chamfer ≥2=bo·cạnh all/vertical/top) · `filletPolygonMm` · `prismChamfered` · `arrayGrid` · `arrayRadial`(vòng kín chia n kiểu 3ds Max) · `mirrorGeometry`(det<0 tự đảo winding) · `sweepProfile`(miter phân giác, phào cắt 45° đúng) · `revolveProfile`(dùng THREE.LatheGeometry sẵn có, KHÔNG kéo lib ngoài) · `loftSections` · `prismTapered` · `offsetPolygonInwardMm`. Test 51/51 ĐO HÌNH HỌC THẬT (toạ độ vertex·bbox·ring 45° co đúng 5·(1−cos45°)), tsc exit 0, ảnh N6 render TỪ BufferGeometry: `docs/screenshots/build-ops-dot1-2026-08-07.png`. Verify TỔNG 07/08: grep ra đủ 13 hàm |
| G-M18-01 | (lõi 3D) | 🟠 **Bevel CŨ vát NON ~29% so số khai** — p14 phát hiện 07/08, CHƯA sửa vì đổi là đổi hình dữ liệu người dùng đã lưu. `lib/three/cad-to-obj.ts:75` `insetPolygonMm` dịch **ĐỈNH** d mm theo phân giác, KHÔNG phải lùi MẶT ⇒ đường cũ (`setEntityBevel`→`prismBeveled`) vát non. Lệnh MỚI dùng `offsetPolygonInwardMm` (offset thật + bẫy chống co-quá-đà mà check `signedArea` không bắt được). ⚠️ **CẦN HOÀ QUYẾT**: sửa đường cũ (hình cũ đổi, đúng số khai) hay giữ (hình cũ nguyên, số khai sai)? | lõi 3D | Có | ⚪ chờ Hoà quyết |
| G-M18-02 | (nối dây) | **Lệnh dựng khối mới CHƯA nối vào app** — p14 làm xong 13 hàm nhưng: ① `ops[]` chưa persist (`BuildOp` ở `lib/cad/model.ts` — vùng p14 bị cấm chạm) ② nút "Bo cạnh" trong `ToolDock3D` vẫn `disabled`. ⇒ Người dùng CHƯA bấm được lệnh nào trong 13 lệnh vừa viết. Đúng bệnh "động cơ xong, thiếu sợi dây". | nối dây | Có | 🔴 chưa sửa |
| G-M19-01 | (giao diện) | 🔴 **Thẻ thư viện quá nhỏ — tấm 720px quá hẹp, KHÔNG có núm chỉnh cỡ.** Đo 07/08 `components/library/library-sheet-css.ts`: tấm `min(720px,100vw−24px)`(:61) − kệ 214px(:97) − padding 28px ⇒ lưới **478px**; `minmax(122px,1fr)` gap 11(:135) ⇒ **3 cột, thẻ ~141px**, ảnh xem trước ~141×80 — không phân biệt được vân gỗ. Tra ngành: **D5 Render và Blender đều có núm chỉnh cỡ thẻ**, không app nào cố định một cỡ. ⇒ **TỔNG quyết 07/08 (Hoà uỷ quyền)**: ① nới tấm **720→960px** (mock gốc 980; số 720 là TỔNG tự hạ sáng nay vì tưởng cột thông số chiếm chỗ thường trực, nhưng nó CHỈ hiện khi chọn món) ⇒ lưới 718px; ② **3 nấc cỡ thẻ** nhớ được (localStorage): Nhỏ 122px/5cột · **Vừa 168px/4cột ⭐mặc định** · Lớn 232px/3cột; ③ nấc Lớn hiện thêm `w×d×h`. Giữ nguyên: kệ 214px · nổi giữa không dính đáy · cột thông số chỉ hiện khi chọn · chuyển cảnh 180–220ms. Chi tiết `docs/00-CHOT.md`. | giao diện | Có | 🔴 chưa sửa |
| G-M20-01 | (present-editor) | 3 hàm chết hẳn — xoá hoặc nối dây: `lib/present-editor/custom-templates.ts:101` `renameCustomTemplate` · `:174` `customTemplatesAsEditorTemplates` · `lib/present-editor/sample.ts:38` `makeShortDemoDeck`. Nguồn: P2 `M-SOI-16-MANG-OUT.md:403`. | vỏ | Có | ✅ ĐÓNG 07/08 (phiên p2, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M20-02 | (present-editor) | `components/present-editor/boq/BoqScreen.tsx:127-134` **nuốt lỗi `fetch('/api/specs')` im lặng**, không báo UI. Nguồn: P2 `:404`. | vỏ | Có | ✅ ĐÓNG 07/08 (phiên p2, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M20-03 | (sổ mock) | `docs/mocks/mock-trinh-bay.html` không có trong bảng trạng thái `README-mocks.md:204` — chưa rõ đã port đủ. Nguồn: P2 `:405`. | sổ | Có | 🟡 cần xác minh |
| G-M20-04 | (nodes) | "Chạy nút tổng" `components/nodes/MacroNodeFace.tsx:131-139,199` không bắt lỗi đồng bộ, nút **không tự khoá khi đang chạy** ⇒ bấm lặp được. Nguồn: P2 `:406`. | vỏ | Có | ✅ ĐÓNG 07/08 (phiên p2, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M20-05 | (nodes) | Upload ảnh vào node **không khoá nút trong lúc decode** — click lặp được. `components/nodes/InteriorNode.tsx:205-219`. Nguồn: P2 `:407`. | vỏ | Có | ✅ ĐÓNG 07/08 (phiên p2, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M20-06 | (studio) | `lib/lockscreen.ts:39-43` `lockScreenNow()` khoá cứng sau **200ms cố định**, không chờ autosave xong thật ⇒ nguy cơ mất dữ liệu chưa lưu. Nguồn: P2 `:408`. | vỏ | Có | ✅ ĐÓNG 07/08 (phiên p2, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M20-07 | (vision) | `lib/nodes/defs/metrology.ts:142` `measureObjectTiered` không có bước tiến độ thật giữa 2 mốc 0.5→1 ⇒ **thanh chờ đứng yên khi đang tính**. Nguồn: P2 `:409`. | vỏ | Có | ✅ ĐÓNG 07/08 (phiên p2, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M20-08 | (photo-editor) | `components/photo-editor/PhotoEditor.tsx:279-291` `onCommitLayerMask` (invert-mask) **không try/catch, không trạng thái chờ** — lệch chuẩn so với nhánh liền kề TRONG CÙNG FILE. Nguồn: P2 `:410`. | vỏ | Có | ✅ ĐÓNG 07/08 (phiên p12, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M20-09 | (notebook) | `components/notebook/useNotebook.ts` **không kiểm `res.ok` trước khi đọc JSON** ⇒ lỗi HTTP hiện thành câu trả lời RỖNG (:148,222,227,153). Verify TỔNG 07/08: grep `res.ok` = **1 chỗ** ⇒ mới vá một phần, còn 3. Nguồn: P2 `:412`. | vỏ | Có | ✅ ĐÓNG 07/08 (phiên p12, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M20-10 | (notebook) | `components/notebook/NotebookButton.tsx` viết xong nhưng **đã bị gỡ khỏi Header, nay mồ côi** — xoá hoặc nối lại. `lib/notebook/resolveProject.ts:23`. Verify TỔNG: 1 nơi nhắc ngoài file gốc, cần đọc xem là code hay comment. Nguồn: P2 `:413`. | vỏ | Có | 🟡 cần xác minh |
| G-M20-11 | (sổ mock) | 2 mock intro (`mock-if-intro-C3.html` · `mock-if-intro-bong-hoi-tu-2026-08-03.html`) **0 tham chiếu trong code** — chưa rõ đã port. Nguồn: P2 `:415`. | sổ | Có | 🟡 cần xác minh |
| G-M20-12 | (commands) | `lib/commands/registry.ts:362,389-393` `findByAlias` · `lib/commands/vcb.ts:42,92` `parseVcbToken`/`applyVcbToMoveCopy` — **viết xong, CÓ TEST, nhưng chưa nối UI**, TODO tự thú trong code. Nguồn: P2 `:418`. | nối dây | Có | ✅ ĐÓNG 07/08 (phiên p12, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M20-13 | (trung tính) | ✅ ĐÓNG — **dòng này LỖI THỜI, việc đã chốt 05/08.** `docs/LICENSE-NOTES.md:136` §9 ghi rõ: bảng Pantone TCX **2310 mã đã bỏ hẳn**, máy tra đổi sang nguồn cắm rời (`lib/colors/`), app không kèm bảng màu của hãng nào. `lib/colors/trend.ts` nay giữ **trần cứng 1 mục/năm + bắt buộc trường `source`** trỏ link công bố chính thức, và có **test chặn** (`registry.test.ts` §5). Lập luận pháp lý đã viết sẵn ở §9.5: *ranh giới là QUY MÔ, không phải màn hình* — nhắc 'Pantone chọn Peach Fuzz làm màu của năm 2024' có dẫn nguồn là **tham chiếu biên tập** (báo chí làm mỗi ngày, không thay thế sản phẩm họ bán); chép 2310 mã vào phần mềm bán ra mới là thay thế. ⚠️ TỔNG đã đưa mục này vào phiếu build final như 'chờ Hoà quyết' — **SAI, Hoà đã quyết từ 05/08**. Đúng bệnh §0ab: sổ GAP cũ hơn tài liệu chốt. Việc CÒN LẠI (ghi ở LICENSE-NOTES §9.5, KHÁC mục này): `git filter-repo` xoá `pantone-tcx.json` khỏi lịch sử git + đối chiếu 11 link nguồn. |
| G-M21-01 | (pháp lý) | ✅ **GPL-3 TUÂN THỦ ĐỦ 4 NGHĨA VỤ** — p6 làm 07/08, TỔNG verify 15:5x. ① trang Third-party licenses: `app/settings/licenses/` có 1 trang `.tsx` + `lib/legal/gpl-3-0-text.ts` (bản GPL-3 đầy đủ nhúng trong app) + `lib/legal/third-party-licenses.ts`. ② Corresponding Source: chọn đường (b) written offer. ③ Ranh giới code SẠCH — `grep -E "^import .*libredwg"` trên CẢ 7 file nhắc tên = **0 import tĩnh**; `lib/cad/dwg-worker.ts` nạp động, đúng thiết kế cô lập. ④ Ghi rõ phần nào dưới GPL. | pháp lý | — | ✅ xong |
| G-M21-02 | (trung tính) | ✅ **BỐN MỤC TRUNG TÍNH ĐÃ SẠCH** — TỔNG verify 07/08 15:5x bằng grep: `lib/filemanager/mock-data.ts` "Detech" = **0** (p6 sửa) · `lib/present-editor/content-deck.ts` "DETECH" = **0** · `package.json` "com.ttt" = **0** · `components/intro/IntroSequence.tsx` mật khẩu = **0**. CÒN LẠI: 53 ảnh render khách (`public/wallpapers/ttt-*` `covers/` `detech/`) — p6 LIỆT KÊ, KHÔNG xoá, **chờ Hoà duyệt** (KS4). | trung tính | Có | 🟠 chờ Hoà duyệt xoá 53 ảnh |
| G-M21-03 | (3D) | ✅ **VỎ 3D ĐÃ DỰNG** — p3 tạo `components/render-studio/ToolDock3D.tsx` MỚI (13.212 B, 254 dòng, 07/08 14:24) theo mock chuẩn `3D Dựng khối.dc.html`. p3 tự đính chính brief trước khi port (đúng LUẬT SỐ 0) và đếm lại **24 file UI 0 test** (không phải 27 như brief). ⚠️ CHƯA verify tay bằng trình duyệt. | vỏ | — | 🟡 chưa verify tay |
| G-M22-01 | (giao diện) | ✅ ĐÓNG 07/08 — **`text-shadow` áp NHẦM NGỮ CẢNH ở màn đăng nhập — chữ nhoè viền.** `app/globals.css:800` `.lq-card > .lq-content { text-shadow: 0 1px 3px rgba(0,0,0,.45) }`. Comment `:797` ghi rõ shadow sinh ra 26/07 cho **chữ TRẮNG nổi trên ẢNH NỀN** (card kính tint alpha .14/.07). Nhưng ở **theme sáng, nền kem đặc, chữ đen** ⇒ bóng đen dưới chữ đen ⇒ nhoè. Hoà bắt 07/08 qua ảnh: "Ghi nhớ đăng nhập" + "HOẶC TIẾP TỤC VỚI". ⚠️ **CHÍNH FILE NÀY đã viết luật đúng ở `:819`** — *"KHÔNG text-shadow (thừa khi nền đã đặc, chỉ làm chữ bẩn)"* — luật có rồi, chưa áp cho `.lq-content`. Sửa: chỉ áp khi card thật sự nổi trên ảnh nền VÀ chữ trắng. ĐỪNG xoá thẳng — theme tối trên ảnh nền vẫn cần. | giao diện | Có | ✅ ĐÃ SỬA 07/08 (p-login) — chọn phương án selector theo theme/tone, gói trọn trong khối `.lq-*` (không đụng `LoginScreen.tsx`, ngoài vùng sở hữu). `text-shadow:none` cho 2 ngữ cảnh chữ mực trên nền sữa (`:root[data-theme='light']` + `[data-login-tone='light']`), giữ shadow cho chữ trắng trên nền ảnh/tối. Đo computed style browser thật đủ 4 nhánh. **Vá thêm cùng họ:** `[data-login-tone='dark'] .lq-card` và `.lq-field` thua specificity nhánh theme sáng ⇒ theme sáng + ảnh nền ra card sữa đè lên ảnh, trái chính comment 'tone dark cưỡng bức'. Thêm `:root ` prefix, verify lại. `tsc` 0 · console 0. ⚠️ CHƯA VERIFY ca ảnh THẬT (thư viện ảnh server trống, đại diện bằng gradient cùng nhánh CSS). |
| G-M22-02 | (giao diện) | **"Ghi nhớ đăng nhập" DÍNH LIỀN "Quên mật khẩu?"** — không khoảng hở, đọc thành một cụm. `components/entry/LoginForm.tsx:272` dùng `flex items-center justify-between` nhưng ảnh Hoà 07/08 cho thấy hai cụm sát nhau ⇒ container không đủ rộng hoặc `justify-between` bị đè. | giao diện | Có | 🟡 KHÔNG TÁI HIỆN 07/08 (p-login, khai thẳng theo N3) — đo thật: gap 102,6px @1280 · 45,6px @375 · ≈34px bản EN; `justify-between` không bị đè. Vẫn vá lưới đỡ theo brief (`flex-wrap gap-x-4 gap-y-1`, hở tối thiểu 16px, chật thì xuống dòng), comment ghi rõ đây KHÔNG phải fix gốc. **Cần Hoà cấp: kích thước cửa sổ lúc thấy dính + đó là màn đăng nhập hay màn khoá.** |
| G-M22-03 | (giao diện) | **Nút Home đứng RIÊNG ngoài menu, đáng lẽ là một MỤC trong menu.** Hoà chốt 07/08: đưa Home vào danh sách, đặt **PHÍA TRÊN** mục "Về Thư viện dự án". Hiện `components/studio/AppChrome.tsx:254` `{logoMenu && <HomeButton compact />}` — icon nhà đứng bên trái logo, tách khỏi `AppLogoMenu`. Menu hiện 5 mục: Về Thư viện dự án · Tổng quan · Dự án & Flow · Files · Thư viện (`AppLogoMenu.tsx:7`). ⇒ Thành 6 mục, Home đứng đầu. `HomeButton.tsx:13` tự khai nó trùng vai với mục "Về Thư viện dự án" — gộp là đúng, bớt một chỗ bấm mơ hồ. | giao diện | Có | ✅ ĐÓNG 07/08 (phiên p3, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M17-03 | lib/materials | ✅ ĐÓNG 07/08 (phiên p13, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M1-22 | lib/cad | ✅ ĐÓNG 07/08 (phiên p9, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M1-21 | lib/cad | ✅ ĐÓNG 07/08 (phiên p9, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-VS-01 | vệ sinh repo | 🔴 **3 worktree MỒ CÔI trên đĩa, git KHÔNG biết** — `.worktrees/dot-b/` · `.worktrees/pbr-schema/` · `.worktrees/so-lenh/`, tổng **183 MB**, trong khi `git worktree list` chỉ liệt kê 1 (main). Chúng chứa **bản code CŨ**: `.worktrees/pbr-schema/components/three/Scene3DViewer.tsx:174` vẫn là `groups.filter(g => !g.entityId)` (bản hiện tại đã đổi sang `isMassingWallGroup`). ⇒ Mọi `grep -rn` toàn repo trả về kết quả NHIỄU lẫn code chết — đúng cái vừa làm TỔNG suýt đọc nhầm dòng 07/08. Trái luật worktree trong `CLAUDE.md` ("trước sprint mới: `git worktree list`, còn worktree cũ → DỪNG, báo chủ dự án"). Cần Hoà duyệt xoá (KS4) — kiểm 4 điều kiện an toàn trước, KHÔNG `--force`. |
| G-VS-02 | vệ sinh repo | ✅ ĐÓNG 07/08 (phiên p12, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M18-03 | lib/three | ✅ ĐÃ SỬA 07/08 (TỔNG) — **nội thất trong 3D không chọn được**: `cad-to-obj.ts` cố ý không gán `entityId` cho `Furn_i` vì Viewer đời trước lọc scene tĩnh bằng `!g.entityId` (gán vào là ghế BIẾN MẤT). Khe đó nay đã bịt: `isMassingWallGroup()` (`obj-scene-to-geometry.ts:92`) đòi CẢ `entityId` VÀ `heightMm`, `Scene3DViewer.tsx:253-255` lọc theo hàm đó, đã có test khoá (`obj-scene-to-geometry.test.ts:37,50`). Gỡ chốt + gán `entityId: b.id` ⇒ `cad-to-obj.test.ts` **74 pass 1 fail → 75 pass 0 fail**, `obj-scene-to-geometry` 10/10 không hồi quy. Đây là ca "chốt chặn tạm sống lâu hơn lý do của nó" — comment ghi lý do đã cứu được, nhưng KHÔNG ai rà lại khi lý do mất. |
| G-M18-04 | lib/three · render-studio | ✅ ĐÓNG 07/08 — **3D không có nút đưa camera về toàn cảnh.** `grep "fitToScene\|zoomExtents\|zoomToFit\|fitCamera\|Toàn cảnh\| ✅ ĐÓNG 07/08 (phiên p3, báo cáo `docs/M-*-OUT.md`). **Verify TỔNG độc lập bằng grep, không tin báo cáo suông (N1)** — xem bảng kiểm cuối `docs/00-DANG-CHO.md`. `tsc` toàn repo **0 lỗi** (lần đầu), 218 file test **0 fail logic**. |
| G-M5-18 | giao diện | 🟠 **CẢ MẢNG G-M5 (dòng 37-53) LÀ SỐ ĐO LỖI THỜI — đo lại 07/08.** Sổ ghi "67 trang mock", thực tế `docs/mocks/**/*.html` = **96 file**, trong đó **36 file `.dc.html`** mang đúng tên những màn sổ đang khai là "grep = 0, chưa có mock": `Nhập bản vẽ có sẵn.dc.html` (G-M5-01) · `Nhận đề bài.dc.html` (G-M5-11) · `Kết quả chia khu.dc.html` (G-M5-12) · `Bảng món nội thất.dc.html` (G-M5-08) · `Phiên bản hồ sơ.dc.html` (G-M5-06) · `HopXuatPDF/ToGiay/BangNetIn/BangTron.dc.html` (G-M5-04) · `Thư viện.dc.html` (G-M5-05) · `Xem cấu kiện.dc.html` (G-M5-02). ⇒ **13/14 màn ĐÃ CÓ mock**; TỔNG suýt phát phiếu bảo Claude Design vẽ lại thứ đã có (Hoà chặn kịp: *"có hết rồi, mà cũng ko thể nào giao claude design 1 loạt dc"*). **Việc THẬT không phải vẽ, mà là đối chiếu 36 mock `.dc` với code xem chỗ nào code chưa làm theo.** Đây là lần thứ HAI trong ngày TỔNG tin số cũ trong GAP mà không kiểm (lần đầu: so chuỗi NFC/NFD, Hoà cũng phải chặn). ⇒ Luật §0ab bên dưới. |
| G-DB-01 | prisma | 🔴 **SCHEMA ĐI TRƯỚC DB — 3 bảng khai rồi nhưng KHÔNG tồn tại trong `dev.db`.** Đo 07/08: `schema.prisma` có **20 model**, `dev.db` thật chỉ có **17 bảng**. Thiếu: **`WorkflowState`** · **`Task`** · **`ExternalRef`**. `lib/server/tasks.ts` đã viết đủ `listWorkflowStates`/`listTasks`/`createTask`/`updateTask`/`deleteTask` + cờ `TASK_TABLES_READY`, và `lib/server/tasks.test.ts:4` tự khai *"🔴 Bảng Task/WorkflowState CHƯA có trong dev.db"*. ⇒ Vi phạm K4 (khai trường/bảng phải có nơi tiêu thụ THẬT). Đây cũng là lý do 2 test `tasks`/`credits` không chạy được. **`ExternalRef` chính là bảng cầu mà `G-M9-01` đòi** (`schema.prisma:554`, có `system`/`externalId`/`entityType`) — ai đó đã khai nhưng chưa migrate ⇒ trước khi giao G-M9-01 phải kiểm lại phạm vi, phần khai đã xong. |
| G-DB-02 | prisma | 🔴 **CHỈ MỘT MIGRATION, TỪ 03/07 — schema trôi khỏi migration.** `prisma/migrations/` có đúng 1 thư mục `20260703141955_init`. Mọi thay đổi lược đồ hơn một tháng qua đi bằng `db push` (không sinh migration) ⇒ **không dựng lại được DB từ đầu trên máy khác**, và không có đường lùi khi lược đồ hỏng. Với sản phẩm đóng gói bán ra (Electron, máy khách tự chạy DB) đây là rủi ro thật, không phải sạch sẽ hình thức. Cần: sinh migration bù cho phần lệch, hoặc chốt rõ "dự án cố ý dùng db push" và ghi vào CLAUDE.md kèm cách dựng lại DB. |
| G-DB-03 | prisma | 🟡 **443 MB bản sao `dev.db` nằm trong `prisma/`** — 5 file `dev.db.bak-*` (`truoc-gumodel` · `20260801` · `truoc-cot-room` · `truoc-kho-vat-lieu-v1` · `truoc-vacuum`), file lớn nhất 143 MB. `.gitignore:6 prisma/dev.db*` nên **git KHÔNG theo dõi** ⇒ không vào repo, nhưng chiếm chỗ trên máy và làm chậm mọi lệnh quét thư mục. `CLAUDE.md` ghi đã tách `dev.db.bak-*` ra ngoài repo hôm 24/07 — thực tế vẫn còn 5 cái. Cần Hoà duyệt xoá/chuyển (KS4), **không tự xoá**. |
| G-VS-03 | vệ sinh mã | 🟡 **125 chỗ tắt luật kiểm mã (`eslint-disable`)**, trong đó **52 chỗ tắt `react-hooks/exhaustive-deps`** — luật này bắt thiếu phụ thuộc trong hook, và thiếu phụ thuộc là nguồn kinh điển của lỗi "màn không cập nhật khi dữ liệu đổi". 40 chỗ tắt `no-img-element` (dùng `<img>` thay component ảnh tối ưu — ảnh hưởng 8 trang nặng >250 kB, xem `G-M11-02`), 16 chỗ `no-explicit-any`. Không phải lỗi ngay, nhưng là **nợ đã đo được**: cần rà 52 chỗ hook, phân loại "cố ý và an toàn" vs "tắt cho qua". |
| G-VS-04 | vệ sinh mã | 🟡 **Hai tệp vượt 3.000 dòng**: `components/cad/CadCanvas.tsx` **3.511** · `components/cad/CadEditor.tsx` **3.122**. Kế đó `present-editor/PresentEditor.tsx` 2.258 · `ProjectSelect.tsx` 2.151 · `present-editor/Inspector.tsx` 1.903. Tệp cỡ này khiến hai phiên gần như chắc chắn đụng nhau khi làm song song (đúng rủi ro đang gặp giữa p9/p13), và khiến `components/` khó viết test (`G-M12-01`). Chưa cần tách gấp, nhưng **phải là điều kiện khi giao việc**: hai phiếu cùng chạm một trong hai tệp này thì không được dán song song. |
| G-AUTH-01 | components/entry | ✅ ĐÓNG 07/08 (TỔNG) — **Màn đăng nhập hiện câu lỗi vô nghĩa `Unexpected token '<', "<!DOCTYPE"… is not valid JSON`** (Hoà chụp được trên bản Electron đóng gói, cổng 3777). Gốc: `LoginForm.tsx:111` (và `:125` nhánh đăng ký) đặt `const body = await res.json()` **TRƯỚC** `if (!res.ok)`. Máy chủ trả trang lỗi HTML ⇒ `res.json()` nổ ngay, câu `throw new Error(body.error ?? …)` — vốn viết ĐÚNG Ý — không bao giờ chạy tới. Vá: thêm `readJsonSafe()` đọc `res.text()` rồi thử parse, không phải JSON thì trả object có `error` đọc được (phân biệt "trang lỗi HTML" và "phản hồi không đọc được", song ngữ, kèm mã HTTP). `tsc` 0 lỗi. ⚠️ **Đây chỉ vá phần HIỆN THỊ — nguyên nhân máy chủ trả 500 CHƯA tìm ra, xem `G-AUTH-02`.** |
| G-AUTH-02 | electron · prisma | ✅ ĐÓNG 07/08 (phiên build final) — **GỐC THẬT KHÁC HẲN GIẢ THUYẾT CỦA TỔNG. Ghi lại nguyên văn để không lặp lỗi suy đoán.** TỔNG đoán: *DB dựng theo lược đồ 03/07 vì chỉ có 1 migration* — **SAI**. Verify sau khi vá: migration VẪN 1, 3 bảng `WorkflowState`/`Task`/`ExternalRef` VẪN THIẾU, mà đăng nhập ĐÃ CHẠY. Gốc thật: **`electron-builder` bỏ rơi `node_modules/.prisma`** (client Prisma sinh lúc `postinstall`) ⇒ app đóng gói chết MỌI API trên máy người dùng thật. Máy dev không lộ vì app nằm cạnh repo nên Node trèo lên thư mục cha mượn được — đúng kiểu bug chỉ hiện ở máy sạch. Vá: `package.json` `extraResources` chép `node_modules/.prisma` → `app/node_modules/.prisma`, kèm lọc `!**/*.db` `!**/*.db-journal` (3 file DB test rác đang nằm trong `.prisma/client/`). Chứng minh trước/sau: login **500 → trả user JSON**. ⚠️ CHƯA VERIFY trên máy SẠCH ngoài repo — cần Hoà cài DMG một lần ở máy khác. |
| G-BUILD-01 | vận hành | 🟡 **Dev server của phiên khác ghi đè `.next` GIỮA hai lần đóng gói** — phát hiện khi build final 07/08, là bug thứ hai của phiên đó. Hệ quả: bản đóng gói mang chunk lẫn lộn giữa hai lần build, triệu chứng khó lần vì code nguồn đúng. **Luật: đóng gói phát hành PHẢI tắt hết dev server trước.** Cùng họ với `§0aa` (một thư mục repo = một dev server) — nay mở rộng sang khâu đóng gói. Cần đưa vào `CLAUDE.md` mục quy trình phát hành. |
| G-EMPTY-01 | (sản phẩm) | 🔴 **APP GIAO CHO NGƯỜI DÙNG MỚI KHÔNG ĐƯỢC CÓ DỰ ÁN MẪU.** Hoà chốt 07/08 nguyên văn: *"bỏ hết dự án mẫu. app xuất ra trung tính, chưa ai xài thì có mẫu đó chi trời"* + *"app thật bắt đầu bằng card +"*. Đo được: **1.039 dòng code mẫu / 10 file** (`lib/demos/*.ts` 444 · `lib/present-demo.ts` 197 · `lib/present-editor/akh-sample.ts` 138 · `demo-enso-sample.ts` 147 · `sample.ts` 78 · `lib/demo-seeds.ts` 35), **4 route công khai** (`/present` `/library` `/present-editor` `/demo-resort`), **6 nơi tiêu thụ**, **23 ảnh render khách** (`public/detech/*` 18 · `public/covers/render_*` 5). ⚠️ **NÚT CHẶN — làm sai thứ tự là vỡ**: `components/present-editor/PresentStageScreen.tsx:25` gọi `makeSampleDeck` ⇒ màn Trình chiếu THẬT đang chạy bằng deck mẫu. Gỡ mẫu trước khi dựng trạng thái rỗng = chặng 3 trắng trơn (đúng lỗi `G-M14-02`). ⇒ **Hai bước, đúng thứ tự**: ① dựng TRẠNG THÁI RỖNG cho 4 màn (Dự án = thẻ `+` · 2D = canvas trắng + lối vào "nhập bản vẽ / vẽ mới" · 3D = "chưa có khối" · Trình chiếu = "chưa có slide, tạo từ ảnh đã dựng") → ② gỡ mẫu + route + ảnh. Tiền lệ đúng trong repo: `components/intro/TitleSequence.tsx:39` đã thay ảnh khách bằng gradient vật liệu. |
| G-MOCK-01 | (cửa kiểm) | 🟡 **Hai luật cửa kiểm đang phạt mock vì mock làm ĐÚNG việc của mock.** Đo 07/08: 892 vi phạm / 104 file = **11 loại**, ba loại chiếm 84%: `PLACEHOLDER-LO` 429 · `HANDLEBARS` 172 · `FONT-SHORTHAND` 151. Trong đó: **`HANDLEBARS` bắt chuỗi `{{ }}`** — nhưng `{{tên dự án}}` chính là cách mock nói "chỗ này đổ dữ liệu thật vào", tức đúng bản chất hợp đồng giao diện. **`PLACEHOLDER-LO`** thì khác hẳn và LÀ lỗi thật (gõ thẳng chữ `PLACEHOLDER` ra màn, người xem tưởng là thiết kế) — nặng nhất 62 chỗ/file. ⇒ Đề xuất: **hạ `HANDLEBARS` xuống VÀNG** với điều kiện file phải có chú giải đầu file liệt kê biến; giữ `PLACEHOLDER-LO` ĐỎ. Cộng với việc bỏ dự án mẫu (`G-EMPTY-01`) thì phần lớn 429 chỗ `PLACEHOLDER` tự biến mất theo. **Đã xoá 07/08**: 7 file `Canvas*.dc.html` (13 dòng, ruột `<x-dc></x-dc>` RỖNG, 7 bản giống hệt nhau — vỏ do công cụ thiết kế xuất ra, không tả màn nào). |
| G-TOOLBAR-01 | components | 🔴 **Thanh công cụ dưới: 15 nút TOÀN ICON, 9/15 nhãn tiếng Anh hoặc lai, tooltip là đường DUY NHẤT biết nút làm gì.** `components/BottomToolbar.tsx` (174 dòng) — chuỗi viết THẲNG vào code, không qua lớp dịch, trái chốt song ngữ VI/EN: `:117 "Select (V)"` · `:120 "Pan (Space giữ / H)"` · `:137 "Sticky note"` · `:141 "Undo"` · `:144 "Redo"` · `:148/154 "Zoom out/in"` · `:157 "Fit view"` · `:161 "Tự sắp xếp graph (auto-layout)"` · `:164 "Snap lưới"` · `:167 "Command palette"`. Còn lọt jargon ra UI (`graph` · `snap` · `palette` · `pan`) — CÙNG LỖI đã sửa cho `ModeSwitch` hồi 06/08 (`G-M4-02`), thanh này bị bỏ sót. **Hệ quả đo bằng mắt 07/08 (ảnh Hoà gửi):** hai nút `⊞` và `▦` cạnh nhau nhìn gần như nhau nhưng là hai việc chẳng liên quan (tự sắp xếp sơ đồ vs bám lưới bật/tắt); nút `⌘` không ai đoán được là bảng lệnh. Không hover được (cảm ứng, hoặc đang kéo chuột) thì mù hoàn toàn. |
| G-TOOLBAR-02 | components | ⚪ RÚT — **TỔNG ghi SAI, Hoà đính chính 07/08.** TỔNG viết: *công tắc 'Vẽ 3D' đứng nhầm cấp, nó là chuyển CHẶNG mà lại nằm chung thanh với công cụ*. **Sai.** Hoà: *"vẽ 3D là 1 MẶT KHÁC, ở mỗi chặng đều có toggle chuyển MODE"* — tức mỗi chặng có công tắc mode riêng, đặt ở thanh dưới là ĐÚNG kiến trúc: chặng 1 (Thiết kế 2D) = Sơ phác ↔ Kỹ thuật (gồm cả Revit) · chặng 2 (Thiết kế 3D) = dựng ↔ render · chặng 3 = trình chiếu. Thanh TRÊN đổi CHẶNG, thanh DƯỚI đổi MODE trong chặng — hai cấp rõ ràng, không lẫn. Bài học: TỔNG suy ra 'lỗi phân cấp' từ MỘT ảnh chụp mà không đọc kiến trúc mode đã chốt (§0ab). |
| G-TOOLBAR-03 | render-studio | 🔴 **Thanh 3D dùng icon MƯỢN TẠM, 3 chỗ sai nghĩa và 2 cặp trùng nhau.** Đo `components/render-studio/ToolDock3D.tsx` (256 dòng, 15 công cụ): ① `:119` công cụ **"Góc"** (đo góc, phím G) mang icon `MoreHorizontal` — dấu ba chấm, chẳng liên quan đo góc, và **trùng đúng icon nút "Thêm"** ở `:180-181` ngay cạnh ⇒ nhìn ra hai nút giống hệt nhau (Hoà chụp được 07/08). ② `RotateCw` dùng cho CẢ **"Bo cạnh"** lẫn **"Xoay"** — hai việc khác hẳn, cùng một hình. ③ `Triangle` gán cho **"Kéo mặt"** (push/pull) — tam giác không gợi được thao tác đùn khối. **Gốc chung: 12/15 nút đang KHOÁ** (`disabled: true`) nên chưa ai bấm để phát hiện; icon mượn tạm sống sót vì không ai dùng tới. ⇒ Sửa icon phải làm CÙNG lúc mở khoá công cụ, không tách rời — mở khoá mà giữ icon sai thì người dùng bấm nhầm ngay ngày đầu. |
| G-LOGO-01 | public · nhận diện | ✅ ĐÓNG 07/08 (TỔNG) — **Icon ứng dụng trái hẳn nguồn sự thật của chính logo.** Hoà bắt bằng mắt: *"LOGO IF TÍM LÀ SAI"*. Nguồn sự thật là `components/entry/IFLogo.tsx` (chốt 19/07, yêu cầu nguyên văn của Hoà ghi trong file: *"trắng đen minimalist nhưng cực kỳ tinh tế ấn tượng và sang"*) — **đơn sắc tuyệt đối, vẽ bằng `currentColor`, KHÔNG gradient, KHÔNG bo tròn, KHÔNG serif**, 3 phương án `stem`/`framed`⭐/`wordmark`. Nhưng `public/icon.svg` (icon app + favicon — **bộ mặt đầu tiên của sản phẩm**: tab trình duyệt, dock, màn hình chính) lại là **gradient tím→hồng `#8b5cf6`→`#c026d3` + bo tròn `rx=112` + chữ Helvetica đậm** ⇒ vi phạm đúng 3 điều file kia viết rõ. Ba tệp `icon-192/512/maskable-512.png` (112 KB) đều sinh từ nó. **Vá**: viết lại `icon.svg` chép nguyên hình học `IFLogo` variant `framed` (nhân tỉ lệ 512/44 = 11,6364, ghi bảng quy đổi trong comment), nền mực trung tính `#12131a` **KHÔNG dùng accent tím của giao diện** (logo không đi theo accent), nét trắng; sinh lại 3 PNG bằng `cairosvg` — **112 KB → 5,3 KB**. ⚠️ CHƯA VERIFY bằng mắt trên tab trình duyệt/dock thật. |
| G-PANEL-01 | components | 🔴 **Tay cầm thu/mở panel mới có ở chặng Trình chiếu, các màn khác gần như không có.** Hoà chốt 07/08: mẫu tay cầm của Present (dải dọc mảnh sát mép + mũi tên `›`) **làm rất tốt, áp cho toàn hệ thống**. Đo được: `components/library/` **0/7 file** có cơ chế thu/mở (tấm Thư viện là panel TO NHẤT app mà không thu được) · `cad/` 2/20 · `render-studio/` 2/18 · `studio/` 3/31 · `nodes/` 5/10 · `dashboard/` 1/2. Ưu điểm của mẫu này (lý do đáng nhân bản): chiếm ~0 diện tích khi không dùng · vị trí đoán được (luôn ở mép panel) · một cú bấm không cần menu · thu rồi VẪN THẤY tay cầm để mở lại. ⇒ Việc: tách thành **MỘT component dùng chung** (không chép code 6 lần — sẽ lệch hành vi ngay), lắp vào mọi panel bên, trạng thái thu/mở **nhớ giữa các phiên**. |
| G-CHOT-01 | vệ sinh · quy trình | 🔴 **Cửa kiểm chốt↔code chạy lần đầu 07/08: 9 luật · 35 vi phạm chặn · 5 cảnh báo.** Dựng `scripts/check-chot.mjs` sau khi Hoà chỉ ra BA lỗi cùng một dạng trong một tối (logo tím · phễu AI tên "Mẫu" · chặng 3 chỉ có deck) — cả ba đều **đã chốt trong `docs/`, thi công đi lệch, không ai rà lại**. Đó là lỗ hổng QUY TRÌNH nên vá bằng cửa kiểm chạy được, không vá bằng lời nhắc. Kết quả: ✅ `LOGO-DON-SAC` · `LOGO-KHONG-ACCENT` · `PHEU-AI-TEN-MAGIC` · `VAT-LIEU-KHONG-TRON`; 🔴 `KHONG-NUOT-LOI` **25 chỗ** (p2 mới dọn vùng nó, còn 25 chỗ vùng khác) · `TEN-CHANG` **4** (*"chặng Drafting CAD"*, *"CAD / Bản vẽ"* lọt tooltip) · `TRUNG-TINH` **4** (`public/__testcases/present.json` còn trỏ `/detech/*`); 🟡 `AI-CAM-TU-TU-DONG` 4 (phải rà tay — *"đánh số tự động"* hợp lệ, không phải AI) · `KHONG-DU-AN-MAU` 1. **Cần nối vào `npm test`** như `check:mocks` để không trôi lại. Mỗi chốt mới phải thêm một luật — tệp là tệp SỐNG. |
| G-IDFC-10 | lib/cad · thư viện | 🔴 **GOM MỌI KỆ THƯ VIỆN VỀ MỘT ĐỊNH DẠNG `.idfc`** (Hoà chốt 07/08 khuya, xem `docs/00-CHOT.md` mục 11). Hiện mỗi kệ một định dạng, một cơ chế riêng ⇒ kệ Mẫu trang không có thumbnail thật còn kệ Vật liệu thì có; không tham chiếu chéo được (mẫu trang ↔ vật liệu, video ↔ cấu kiện). Việc: ① đổi `IdfcFile` sang **vỏ chung + ruột theo loại** (discriminated union) — `meta` chung cho mọi loại, `body` đổi theo `kind`; ② `geom2d` từ BẮT BUỘC chuyển vào `body` ⇒ **đổi cấu trúc file, phải viết hàm nâng v1→v2 trong `IDFC_MIGRATIONS` (hiện RỖNG) + test round-trip file cũ**; ③ sidebar Thư viện chia theo `kind`, bỏ nhóm "Cấu kiện (.idfc)" riêng; ④ mở rộng `IdfcMeta.kind` 5→11 loại (thêm `fitout` `soft` `page` `video` `doc` `asset` `brandkit`), ánh xạ 1-1 với `ThumbKind` để hết ba hệ phân loại chồng chéo (`ThumbKind` 12 loại · `IdfcMeta.kind` 5 · `BlockGroup` 10 — hiện không ánh xạ được với nhau). ⚠️ Việc LỚN, đụng định dạng file đã lưu ra đĩa. |
| G-THUMB-01 | components/library | 🔴 **Thẻ thư viện: tên món CÓ trong code và dữ liệu nhưng KHÔNG lên màn; nấc "Nhỏ" cắt cụt chữ.** `LibrarySheet.tsx:571-574` render đủ `it.name` + `it.code` + `dimsLabel`; `lib/library/shelves.ts` có tên đầy đủ (*"Bìa · ảnh tràn"/COVER-FULL*, *"Mặt bằng + chú thích"/PLAN-ANNO*). Nhưng ảnh Hoà chụp 07/08: nấc **Vừa** không thấy chữ nào, nấc **Nhỏ** chữ bị **xén nửa ở đáy thẻ** ⇒ chiều cao thẻ không đủ chỗ cho khối `.mt`. Kèm: kệ "Mẫu trang" 12 mục rơi hết xuống bậc thấp nhất của bậc thang thumbnail (vân + icon) ⇒ 12 ô giống hệt nhau, trong khi đường đúng ĐÃ CÓ (`renderEditorSlide`, chốt 01/08 §3c). Kèm: ô chưa có nội dung vẫn vẽ ô xám giả thay vì **"+ Thêm mẫu"** (Hoà: *"cái nào chưa có mẫu thì hiện dấu + Thêm mẫu chứ"*). |
