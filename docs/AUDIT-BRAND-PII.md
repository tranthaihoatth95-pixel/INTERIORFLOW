# AUDIT — Thương hiệu & dữ liệu riêng còn sót trong repo (Task #16 / VIỆC 6)

> Ngày quét: 25/07/2026 · Nhánh: `audit/brand-pii` (từ `feat/present-layout-ml-p1`, commit gốc `17629c2`)
> Phạm vi: toàn bộ file **được git track** (853 file) + 2 thư mục **untracked trên đĩa** (`2407-Test/`, `prisma/dev.db`).
> Chuỗi quét (case-insensitive, cả tên file lẫn nội dung): `TTT`, `Detech`, `MSB`, `Vinhomes`, `Sun Group/Sungroup`, `CapitaLand`, `HVH`, `Amanoi`, `Nobu`, `IKI`, `AKH`, `Riviera`, `Hương Việt`, tên người thật, `0\d{9}`/`+84`, email công ty, địa chỉ.
> Luật đối chiếu: `CLAUDE.md` §"LUẬT NỀN TẢNG" (IF độc lập global) · `docs/CONTENT-RULES.md` (APP / DEMO / DỰ ÁN tách bạch).
> **Đây là báo cáo. KHÔNG sửa code trong đợt này.**

Quy ước mức:
- 🔴 **lộ ra UI/output người dùng thấy** (route sống, màn app thật, file xuất, metadata bundle)
- 🟡 **trong repo nhưng không hiện UI** (test fixture, asset chưa dùng, doc kèm sản phẩm, DB/đĩa)
- ⚪ **chỉ comment/docs** (không ảnh hưởng hành vi hay output)

Không tìm thấy: `MSB`, `Vinhomes`, `CapitaLand`, `Nobu`, `HVH`, `Hương Việt` trong code/asset track. Không có số điện thoại thật, không có địa chỉ công ty, không có email cá nhân trong code track (email trong test đều là fixture tổng hợp).

---

## 🔴 Mức đỏ — lộ ra UI / output người dùng thấy

| File | Chuỗi dính | Đề xuất |
|---|---|---|
| `lib/present-editor/content-deck.ts:113` | `kicker: 'DETECH · CONCEPT'` — hardcode trên slide Cover của **mọi deck user tự sinh** từ text dán vào (`slidesFromContent`, dùng thật ở `components/present-editor/PresentEditor.tsx:46`) | **Đổi trung tính NGAY** — kicker rỗng, hoặc đọc từ `deck.brand`/Brand Kit dự án đang mở. Đây là ca vi phạm nặng nhất: tên khách hàng bị in lên nội dung của người dùng khác. |
| `components/entry/LoginBackdrop.tsx:92-98` + `public/wallpapers/ttt-01.jpg … ttt-30.jpg` (**30 file**) | Bộ nền đăng nhập **mặc định** = 30 render dự án thật của TTT, tên file `ttt-*`; comment "Thư viện ảnh TTT: 30 render thật" | **Chuyển thành seed / thay ảnh** — đổi tên file trung tính (`wall-01..30`) và thay bằng ảnh có quyền dùng công khai, hoặc bỏ hẳn khỏi repo và để backdrop mặc định là CSS gradient (đã có nhánh `kind: 'css'`). Ảnh render khách không được là mặc định của sản phẩm bán ra. |
| `components/ProjectSelect.tsx:115` + `public/covers/render_00/04/05/10/03.jpeg` (**5 file**) | `COVERS = ['/covers/render_00.jpeg', …]` — ảnh bìa **mặc định + fallback hash-cover** cho mọi flow ở màn chọn dự án (màn app thật sau đăng nhập). Ảnh là render Detech thật (`lib/present-demo.ts:30` xác nhận nguồn) | **Thay bằng cover trung tính** (placeholder hình học/gradient sinh từ hash id) hoặc ảnh stock có license. |
| `components/intro/TitleSequence.tsx:37-65,179` | 7 ảnh `/detech/lobby-water.png`, `tower-dusk.png`, `apt-1.png`, `wellness.png`, `pool-zen.png`, `lounge-green.png`, `meditation.jpg` — comment ghi rõ "ảnh render THẬT trong public/detech". Chạy ở route sống `/intro` (`app/intro/page.tsx` → `components/intro/IntroSequence.tsx`) | **Thay ảnh** trung tính/stock, hoặc chuyển `/intro` thành khu demo tường minh. Intro là mặt tiền sản phẩm. |
| `public/detech/*` (**18 file**: `apt-1..4`, `enso-circle`, `enso-garden`, `iki-banner`, `lobby-water`, `lounge-green`, `mat-moodboard`, `mat-palette`, `mat-travertine`, `mat-walnut`, `meditation`, `pool-zen`, `tower-dusk`, `tower-night`, `wellness`) | Ảnh dự án thật Detech + banner IKI, **serve công khai** tại `https://<host>/detech/…` — ai biết đường dẫn đều tải được, kể cả khi UI không hiện | **Xoá khỏi `public/`**; nếu cần cho demo thì đưa vào seed ngoài repo hoặc thay bộ ảnh demo trung tính (`public/demo/` đã có sẵn 9 ảnh generic). |
| `app/library/ingest/page.tsx:163` | placeholder textarea: `"Đề bài / input dự án Detech: khách là ai, mục tiêu…"` | **Đổi trung tính**: "Đề bài / input dự án: khách là ai, mục tiêu…". Chuỗi này còn đang hardcode ngoài `i18n.ts` (vi phạm CONTENT-RULES §2). |
| `lib/present-editor/akh-sample.ts:116-118` | Deck **mặc định** của `/present-editor` (`makeSampleDeck()` → `makeAkhIkiDeck()`): `project: 'IKI Village — Show Unit Moodboard'` (tên dự án thật). `brand: 'ATELIER NORD · LUMEN VILLA'` là hư cấu — OK | **Đổi tên dự án sang hư cấu** (vd `'Lumen Villa — Show Unit Moodboard'`) để deck mẫu 100% generic. Ảnh của deck này đã dùng `/demo/*` — đúng. |
| `app/present/page.tsx:22` + `lib/present-demo.ts:59-136` | Route sống `/present` trình chiếu `DEMO_DECK`: `brand: 'DETECH · NỘI THẤT'`, `project: 'Detech — Không gian trưng bày'`, kicker `'DETECH — CONCEPT 2026'`, `'DETECH · 2026'`, ảnh `/covers/*` | **Đổi trung tính** (dùng deck hư cấu như AKH-sample sau khi đổi tên) hoặc **xoá route** — CONTENT-RULES §3 cho phép route showcase nhưng §4 cấm nội dung khách trong sản phẩm. |
| `app/demo-amanoi/page.tsx` (toàn file, ~15 chỗ) | Route sống `/demo-amanoi`: `brand: 'AMANOI · NÚI CHÚA'`, `project: 'Amanoi — Pavilion Phòng Ngủ'` / `'Amanoi — Sảnh Central Pavilion'`, kicker `'AMANOI — NÚI CHÚA, NINH THUẬN'`, **tên file xuất** `Amanoi-<scene>-present.pdf` / `Amanoi-<scene>-moodboard.png` | **Đổi trung tính** (dự án hư cấu vùng biển/núi) hoặc **xoá route**. Tên khách rò cả vào tên file tải về. |
| `lib/demos/present.ts:52-65` | Flow demo "Present" nạp 4 ảnh `/detech/mat-palette.png`, `lobby-water.png`, `lounge-green.png`, `pool-zen.png` (nướng `done()` → hiện ngay khi user mở demo) | **Trỏ sang `/demo/*`** (mood1..4, hero, sketch đã có) — sửa 4 dòng, không đổi kiến trúc. |
| `components/present-editor/PresentEditor.tsx:288-292` | comment "Nền CỐ ĐỊNH beige ấm TTT" + `background: '#F1ECE3'` hardcode làm nền canvas dàn trang của **nội dung user** | **Đổi**: đọc từ theme/Brand Kit của dự án, fallback là neutral do user chọn. Vi phạm LUẬT NỀN TẢNG §3 (áp ngôn ngữ thiết kế lên nội dung user). Ít nhất phải bỏ chữ "TTT" và đưa hằng số vào theme. |
| `package.json:6,47,65,67` | `"author": "TTT Architects"` · `"appId": "com.ttt.interiorflow"` · `"copyright": "TTT Architects"` · `licenseNotes`: *"InteriorFlow is an INTERNAL TOOL for TTT Architects (auth locked to @ttt.vn, no public registration) — NOT distributed/sold externally"* | **Đổi trung tính**: author/copyright → `InteriorFlow`, appId → `com.interiorflow.app`. `licenseNotes` phải **viết lại** — nó vừa gắn thương hiệu vừa mô tả sai bản chất sản phẩm, **và** đang là căn cứ pháp lý duy nhất bào chữa cho việc dùng `@mlightcad/libredwg-web` (GPL-3.0). Đổi luật nền tảng → **phải soát lại license GPL trước khi phát hành**. Các giá trị này hiện ở Properties của app Windows/macOS đã đóng gói. |
| `installers/windows-electron-builder.json:18,30,65` | `"certificateSubjectName": "TTT Architects Co., Ltd"` (2 chỗ) + dòng kỳ vọng verify | **Đổi trung tính** hoặc để trống + đọc từ biến môi trường CI. Tên này hiện trên hộp thoại UAC khi cài. |
| `app/globals.css:106,719` · `app/layout.tsx:10` | Font hệ thống và bảng màu app khai báo là "Font hệ thống **TTT**" / "cam **TTT** #F06020 / navy **TTT** #002850" | **Đổi tên gọi, giữ giá trị**: UI app có nhận diện riêng là ĐÚNG (LUẬT §4) — nhưng phải gọi là nhận diện **InteriorFlow**, không phải TTT. Chỉ sửa chữ, không đổi hex. |

## 🟡 Mức vàng — trong repo nhưng không hiện UI

| File | Chuỗi dính | Đề xuất |
|---|---|---|
| `knowledge/project-references/` (**4 PDF, 121 MB**) — `2600403_Sungroup-Beach-Club-Phu-Quoc_ConceptID_REV01.pdf`, `260320_Sungroup-Beach-Club-Phu-Quoc_mood.pdf`, `260623_8324_HV_Office.pdf`, `260710_DETECH_COMPLEX__1_.pdf` | Hồ sơ concept **dự án khách thật** (Sun Group Beach Club Phú Quốc, HV Office, Detech Complex) commit vào repo sản phẩm | **Xoá khỏi repo** (`git rm` + cân nhắc rewrite history vì 121 MB đã nằm trong object store). Đây là tài liệu bảo mật của khách — không được nằm trong repo sẽ phát hành/bán. Giữ ngoài repo ở `~/Downloads/knowledge/`. |
| `knowledge/ttt-brand/` (**5 HTML**, 180 KB) — `TTT_Brand_Guideline_v2*.html` (gốc/print/VI/VI-Dark/VI-EN) | Brand guideline nội bộ TTT | **Xoá khỏi repo** — `CLAUDE.md` đã ghi "chỉ dùng khi làm tài liệu CHO TTT, KHÔNG áp vào sản phẩm"; vậy nó không thuộc repo sản phẩm. Giữ ngoài repo. |
| `knowledge/ttt-design-system/` (**3 file**: `SKILL.md`, `readme.md`, `styles.css`) | Design system TTT (cam/navy/Archivo, class `.ttt-architects`/`.ttt-corporation`) | **Xoá khỏi repo** cùng lý do trên. Nếu muốn giữ 1 design system cho IF thì **fork thành `knowledge/if-design-system/`** đã bỏ tên/màu TTT. |
| `san pham dau ra/tham khao/**` (**51 file**: `bo cuc dan trang/`, `moodboard/`, `view render/`, `không cần tham khảo/`) | Ảnh/video tham khảo nội bộ (tên file dạng `z8013092465386_…` = ảnh Zalo tải về), thư mục đặt tên tiếng Việt không dấu | **Xoá khỏi repo** — không phải asset sản phẩm, không có license rõ, và tên thư mục tiếng Việt trong repo global là mùi. Chuyển ra `~/Downloads/prototype/`. |
| `2407-Test/` (**untracked**, 48 MB, 27 file) — `TTTA_8436_Riviera-Residences-Resort_Tang 2_CO CHU THICH.pdf`, `260626_riviera_congcong.pdf`, `MHA_RV3-Sheet - A02-02 - MẶT BẰNG KIẾN TRÚC TẦNG 1/8*.pdf`, `Mb bố trí tầng 2_ Phong ngu Master.dwg`, 2× PDF Sungroup Beach Club, ảnh `santorini-*` | ✅ **Đã gitignore đúng** (`.gitignore:"2407-Test/"`, `git ls-files` không có file nào) — nhưng vẫn nằm cạnh repo trên đĩa, chứa **bản vẽ CAD + hồ sơ khách thật** (Riviera Residences Resort, Sungroup) | **Để nguyên gitignore** + **chuyển ra ngoài thư mục repo** (vd `~/Downloads/if-test-input/`). Lý do: agent/tool nào scan thư mục dự án đều đọc được, và một `git add -f` vô tình là lộ hồ sơ khách. |
| `prisma/dev.db` + 2 bản `.bak` (**untracked**, 3 × 143 MB) | ✅ gitignore đúng (`prisma/dev.db*`). Nội dung: **email + tên thật** — `hoa@ttt.vn` (Hoà), `thong@ttt.vn` (Thông), `hoa.tt@ttt.vn`, `tranthaihoa.tth95@gmail.com`, `integrator@ttt.vn`, `render-test@ttt.vn`, `qa-bot@ttt.test`; `LibraryAsset` chứa hash ảnh Zalo dự án thật | **Để nguyên gitignore**; **xoá 2 file `.bak` cũ** (19/07, 22/07 — 286 MB rác) và cân nhắc reset DB dev trước khi làm bản demo/phát hành. Tên bảng `Project` chỉ còn 3 record test ("Dự án verify inline input", "Enter test 2", "__nb:untitled-flow") — sạch. |
| `lib/present-editor/detech-sample.ts` (toàn file, ~25 chỗ) | Deck khách hoàn chỉnh: `'DETECH COMPLEX'`, `brand: 'DETECH · ENSŌ'`, `project: 'Detech Complex — Khối căn hộ dịch vụ 5 sao'`, `DETECH_IMG` map 18 ảnh, palette dự án. Không phải deck mặc định (`sample.ts:28-31` đã chuyển sang AKH) nhưng vẫn export | **Xoá khỏi `lib/` sản phẩm** — CONTENT-RULES §4: "không nhét vào `lib/` của app". Nếu cần đối chiếu chất lượng dàn trang thì lưu thành fixture `.json` ngoài repo, hoặc viết lại thành deck hư cấu tương đương. |
| `lib/present-editor/akh-sample.ts:2,21,41,51,116` · `lib/present-editor/sample.ts:4-35` · `lib/present-editor/templates.ts:990,998,1069,1111,1174` · `lib/moodboard-collage.ts:2` · `scripts/moodboard/build-collage.mjs:3` | Tên dự án khách `AKH-IKI` dùng làm nhãn nguồn gốc của 5 template + collage engine (comment + tên hằng `AKH_PALETTE`) | **Đổi tên trung tính** (`EDITORIAL_PALETTE`, comment ghi "trích từ moodboard editorial tham khảo"). Kỹ thuật thì giữ — chỉ bỏ tên khách. |
| `components/IntroSequence.tsx:21` | comment: `Test hoa@ttt.vn/matkhau123` — **mật khẩu thật của tài khoản test** trong repo | **Xoá dòng này** + đổi mật khẩu tài khoản đó. Credential không được nằm trong source, kể cả tài khoản test. |
| `components/entry/cardFaces.tsx:25-28,119,170,218` · `components/StageSelect.tsx:24` · `components/LoginScreen.tsx:10` | comment "mặt thẻ ẢNH RENDER THẬT (**Detech**)" + 3 `CoverPhoto src="/covers/render_*.jpeg"`. **Dead code**: app thật dùng `components/entry/LoginScreen.tsx` (không import cardFaces); `StageSelect` đã bị `ProjectSelect` thay | **Xoá dead code** `components/LoginScreen.tsx` + `components/StageSelect.tsx` + `cardFaces.tsx` nếu xác nhận không còn dùng — dọn luôn 1 ổ ảnh khách. Nếu giữ thì đổi ảnh trung tính. |
| `installers/android-bubblewrap.md:8,27,70,89,97` · `installers/ios-pwa-vs-capacitor.md:62` · `installers/mac-notarize.md:56` | `com.tttarchitects.interiorflow`, `com.ttt.interiorflow`, `app.tttarchitects.vn`, "email list nội bộ TTT" | **Đổi trung tính** — và làm **trước khi publish store**: package name không đổi được sau khi publish (doc tự ghi rõ). Đây là quyết định khoá vĩnh viễn thương hiệu vào sản phẩm. |
| `installers/windows/HUONG-DAN-CAI.md:3,48` | "dùng nội bộ **TTT Architects**", máy đích "PC-TTTA-008 · i7-10700 · 32GB · GTX 1660", "Phân phối NỘI BỘ TTT" | **Đổi trung tính** — bỏ hostname máy nội bộ (thông tin hạ tầng) và câu "nội bộ TTT". |
| `public/__testcases/present.json:24,42,32-33` | `"brand": "TTT ARCHITECTS"`, `"body":"TTT Architects"`, `"styleref": "/detech/mat-palette.png"`, `"hero": "/detech/lobby-water.png"` — file **serve công khai** dưới `/public` | **Đổi trung tính** (brand hư cấu, ảnh `/demo/*`). Test case nằm trong `public/` nên vẫn tải được từ web. |
| `lib/present-editor/brand-kit.test.ts:31,138` · `edgecase-stress.test.ts:40,104,147` · `reflow.test.ts:158` · `theme-roles.test.ts:64` | fixture `brand: 'TTT'` / `name: 'TTT'` (7 chỗ) | **Đổi trung tính** (`brand: 'ACME'` / `'Studio X'`) — dễ, không rủi ro, giữ test pass. |
| `lib/server/stress-auth.test.ts` (16 chỗ) · `lib/server/auth-policy.test.ts` (9) · `lib/server/edgecase-concurrency.test.ts` (3) · `lib/server/auth-policy.ts:6-23` · `lib/server/auth.ts:211` · `app/api/auth/*/route.ts` · `scripts/seed-admin.ts:9,52` | domain `@ttt.vn` dùng làm domain mẫu trong ~35 assertion + ví dụ CLI (`SEED_ADMIN_EMAIL=admin@ttt.vn SEED_ADMIN_PASSWORD=matkhau6+`) | **Đổi trung tính** → `@company.example`. Riêng `scripts/seed-admin.ts:9` còn kèm **mật khẩu mẫu `matkhau6+`** — đổi thành `<mật-khẩu-của-bạn>`. Không phải rò rỉ nặng (domain công khai) nhưng là dấu vết "tool nội bộ". |
| `scripts/seed-specs.ts:103` | `vendor: 'Xưởng nội thất TTT'` trong seed ProductSpec | **Đổi trung tính** (`'Xưởng nội thất địa phương'`). |
| `lib/cad/commands.ts:209,279` · `lib/cad/cad-core-b1.test.ts:21,89` · `lib/cad/standards-report.ts:15,61` · `lib/cad/standards-report.test.ts:9,22,25,30-32` | alias backward-compat `titleBlockTTT = titleBlockPro` / `TitleBlockInfoTTT` — nội dung khung tên **đã trung tính** (test `cad-core-b1.test.ts:84` khẳng định "KHÔNG hardcode thương hiệu nào") | **Bỏ alias** khi tiện (đổi tên symbol + cập nhật 8 chỗ tham chiếu). Không gấp: chỉ là tên hàm, không lộ ra bản vẽ. |
| `lib/present-editor/text-fx.ts:165` | preset gradient chữ `from: '#002850'` (navy TTT) | **Để nguyên + lý do**: đây là 1 preset trong danh sách user chọn được, không phải mặc định ép buộc. Chỉ nên bỏ chữ "TTT" ở comment nếu có. |
| `lib/avatar.ts:40-44` · `components/avatar/AvatarBuilder.tsx` · `components/avatar/AvatarRenderer.tsx` · `app/settings/avatar/page.tsx` (≈15 chỗ hex) | Palette avatar dùng cam `#F06020` / navy `#002850` / beige `#F1ECE3` | **Để nguyên + lý do**: avatar là **UI của app**, LUẬT §4 cho phép app có nhận diện riêng. Chỉ cần khai báo đây là palette **InteriorFlow** (một lần, trong theme) thay vì gọi là màu TTT, và tập trung hex vào 1 nguồn thay vì rải 15 chỗ. |
| `components/intro/IntroSequence.tsx:47-49,83` · `components/intro/svgs/index.tsx:6-11,222` · `app/intro/page.tsx:28` | `BEIGE/NAVY/ORANGE` + `fontFamily: 'Archivo'` hardcode trong intro | **Để nguyên màu (UI app), gộp vào theme**. Nhưng `'Archivo'` **không được load** ở `app/layout.tsx` (app load Be Vietnam Pro) → thực tế đang fallback system-ui: **nợ kỹ thuật**, sửa hoặc bỏ. |
| `docs/CATALOG-4-STAGES/poster.html:2,25-26,168,226` + `docs/CATALOG-4-STAGES/img/42-demo-amanoi.png` | `<html class="ttt-architects">`, CSS `.ttt-architects/.ttt-corporation`, "khung tên **TTT**", "Brand kit **TTT**", ảnh chụp route `/demo-amanoi` | **Xoá hoặc chuyển ra ngoài repo** — đây là poster giới thiệu sản phẩm, dùng design system TTT và ảnh dự án khách. Nếu là tài liệu cho TTT thì đặt ngoài repo như `docs/BAO-CAO-BGD-GD1.html` (đã gitignore đúng). |
| `prisma/schema.prisma:289` | comment `// song ngữ TTT: "Volumen Lounge Chair"` | ⚪-borderline; **đổi thành** `// song ngữ VI·EN`. |

## ⚪ Mức trắng — chỉ comment / tài liệu

| File | Chuỗi dính | Đề xuất |
|---|---|---|
| `components/cad/CadEditor.tsx:777` · `components/cad/ZonePanel.tsx:10` · `components/studio/StageSwitcher.tsx:12` · `components/studio/VitalsIcon.tsx:6,7,43,54` · `components/ui/IOMenu.tsx:16` · `components/notebook/NotebookSourcesSidebar.tsx:12-13` · `components/present-editor/Inspector.tsx:642` · `app/projects/[id]/notebook/page.tsx:7` · `lib/adaptive-contrast.ts:22,59` · `lib/cad/legend.ts:19,121` · `lib/cad/model.ts:93` · `lib/cad/render.ts:565` · `lib/cad/schedule.ts:154` · `lib/present-editor/model.ts:274` (**≈20 comment**) | "gu TTT", "palette TTT", "khung tên TTT", "song ngữ TTT", "nền beige TTT", "TTT Design System", ví dụ `"Logo TTT"` | **Đổi từ ngữ hàng loạt**: "TTT" → "hệ thiết kế InteriorFlow" / "song ngữ VI·EN". Không đổi code. Làm 1 lượt sed có review — rẻ và dọn sạch tín hiệu "tool nội bộ" cho bất kỳ ai đọc source. |
| `lib/adaptive-contrast.ts:351,450,480` · `lib/adaptive-contrast.test.ts:198` · `components/entry/LoginScreen.tsx:50` | tham chiếu số đo trên ảnh `ttt-05` / `ttt-06` (`raw 0.1901 → 0.152`) | **Sửa cùng lúc với đổi tên wallpaper** (`wall-05`) để comment vẫn khớp file thật. Test `adaptive-contrast.test.ts:198` có tên chứa `ttt-05` → cần đổi kèm. |
| `docs/LICENSE-NOTES.md` (3 chỗ) | Lập luận GPL-3.0 dựa trên "internal tool for TTT Architects" | ⚪ về mặt lộ UI, nhưng **quan trọng nhất về rủi ro**: xem mục 🔴 `package.json`. Phải viết lại cùng lúc. |
| `docs/MASTERPLAN-IF-ARCHINOTE.md` (12) · `docs/RESEARCH-INSTALLER-4-PLATFORMS.md` (8) · `docs/PROPOSAL-LEGEND-SYSTEM.md` (7) · `docs/RESEARCH-CHAT-FULL.md` (5) · `docs/RESEARCH-OFFICE-FILE-INTEROP.md` (5) · `docs/RESEARCH-TECHNICAL-DRAWING-PIPELINE.md` (5) · `docs/HUONG-DAN-SU-DUNG.md` (4) + ~15 doc khác (1-3 hit) | Tên TTT trong tài liệu nghiên cứu/kế hoạch nội bộ | **Để nguyên + lý do**: là ghi chép quá trình, không phải sản phẩm. Nhưng nếu repo sẽ open-source / giao khách thì cả `docs/` phải soát lại — ghi vào Nợ kỹ thuật. |
| `docs/CONTENT-RULES.md` (4) · `CLAUDE.md` (9+3) · `STATUS.md` (6+2) · `CHANGELOG.md` (4+2) · `ARCHITECT-REVIEW.md` (7) · `DEPLOY-CHECKLIST.md` (3+4) · `QUALITY-LOG.md`, `RESUME.md`, `DIAGNOSIS.md`, `FINAL_ARCHITECTURE_REPORT.md`, `TECHNICAL_GLOSSARY.md`, `AUDIT-2026-07-15.md` | Tên TTT/Detech/Amanoi trong luật, lịch sử, checklist — **cố ý** (đây chính là chỗ ghi luật cấm) | **Để nguyên** — đúng chỗ. |
| `lib/report-deck.ts:10` | comment "tách khỏi deck khách (Detech)" | **Để nguyên** — comment giải thích đúng luật. |
| `lib/present-editor/templates.ts:1296` | `'hello@studio.vn · +84 · studio.vn'` | **Để nguyên** — placeholder hư cấu, `+84` không kèm số. Đúng chuẩn. |
| `components/cad/CadEditor.tsx:881` | placeholder `"VD: Nguyễn Văn A"` | **Để nguyên** — tên placeholder generic tiếng Việt, không phải người thật. |

---

## Tổng kết

| Mức | Số dòng bảng | Số file/asset thực tế bị ảnh hưởng |
|---|---|---|
| 🔴 lộ ra UI/output | **14** | ~22 file code + **53 file ảnh** (30 wallpaper + 18 detech + 5 cover) |
| 🟡 trong repo, không hiện UI | **22** | ~45 file code/test + **63 file tài liệu-asset** (4 PDF khách 121 MB, 5 HTML brand, 3 design-system, 51 ảnh "san pham dau ra") + 2 ổ untracked (48 MB + 429 MB DB) |
| ⚪ comment/docs | **8** | ~45 file (≈20 comment code + ~25 doc) |

Điểm sáng — đã đúng: `lib/i18n.ts` sạch 100% · brand mặc định của node/deck đã là chuỗi rỗng (`lib/nodes/registry.ts:775`, `lib/present-editor/model.ts:318`) · khung tên CAD đã trung tính và **có test khoá** (`cad-core-b1.test.ts:84`) · Present mode trong app rỗng khi chưa có deck thật (`PresentDeck.tsx:94-95`) · tên file export mặc định trung tính (`interiorflow-deck.pdf`, `layout.pdf`, `bao-cao-quy-chuan.pdf`) · không seed thương hiệu nào vào localStorage · `2407-Test/` + `prisma/dev.db*` + `docs/BAO-CAO-BGD-GD1.html` + `docs/DEMO-CLAY-2407/` đã gitignore đúng · không có số điện thoại/địa chỉ/email cá nhân thật trong code track.

### Top 5 việc nên sửa trước

1. **`lib/present-editor/content-deck.ts:113` — bỏ `kicker: 'DETECH · CONCEPT'`.** Sửa 1 dòng, chặn ngay việc in tên khách hàng lên deck do người dùng khác tự sinh. Rẻ nhất, nghiêm trọng nhất.
2. **Dọn ảnh dự án thật khỏi `public/` và các màn mặc định** — `public/detech/` (18), `public/wallpapers/ttt-*` (30), `public/covers/` (5) + 4 chỗ tham chiếu (`LoginBackdrop`, `ProjectSelect.COVERS`, `TitleSequence`, `lib/demos/present.ts`). Đây là toàn bộ mặt tiền sản phẩm (login → chọn dự án → intro) đang chạy bằng render của khách.
3. **`package.json` + `docs/LICENSE-NOTES.md`** — đổi author/copyright/appId sang trung tính và **viết lại `licenseNotes`**. Kèm theo: soát lại nghĩa vụ GPL-3.0 của `@mlightcad/libredwg-web`, vì lập luận miễn trừ hiện tại ("internal tool, not distributed") đã hết hiệu lực khi IF là sản phẩm phát hành. Đây là hạng mục có rủi ro pháp lý, không chỉ thương hiệu.
4. **Xoá `knowledge/project-references/` (4 PDF hồ sơ khách, 121 MB) + `knowledge/ttt-brand/` + `san pham dau ra/` (51 file) khỏi repo**, và di chuyển `2407-Test/` ra ngoài thư mục dự án. Vừa gỡ tài liệu bảo mật của khách khỏi repo sẽ phát hành, vừa giảm ~170 MB. Lưu ý cần rewrite history nếu muốn xoá thật khỏi git object store.
5. **Trung tính hoá 3 route/deck mẫu công khai**: `/present` (deck Detech), `/demo-amanoi` (tên khách cả trong file xuất), và tên dự án `IKI Village` ở deck mặc định của `/present-editor`; đồng thời **xoá `lib/present-editor/detech-sample.ts`** khỏi `lib/` sản phẩm theo `CONTENT-RULES.md` §4.

Việc nên gộp làm 1 lượt sau đó (rẻ, ít rủi ro): đổi ~35 fixture `@ttt.vn` → `@company.example`, 7 fixture `brand: 'TTT'`, ~20 comment "gu/palette TTT" → "InteriorFlow", xoá dòng credential `hoa@ttt.vn/matkhau123` ở `components/IntroSequence.tsx:21`, xoá dead code `components/LoginScreen.tsx` + `components/StageSelect.tsx` + `cardFaces.tsx`.
