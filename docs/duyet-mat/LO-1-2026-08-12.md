# 👁 LÔ DUYỆT MẮT #1 — 30 mục xong-MÁY chờ mắt Hoà (12/08/2026)

> Cơ chế: HOP-DONG-PHOI-HOP-T §8.1 — T soạn lô, Hoà soi ~20 phút, tick từng ô;
> T flip registry `xong → xong-mat` theo tick. Mục nào Hoà thấy CHƯA ĐẠT thì ghi 1 dòng
> lý do — T mở lại entry, KHÔNG flip.
>
> **App:** dev server duy nhất còn chạy ở **http://localhost:3002** (T đã tắt 2 server
> thừa 3000/3001 cùng thư mục — bệnh §0aa). Đăng nhập sẵn.
> Cách tick: sửa trực tiếp file này `[ ]` → `[x]`, hoặc nhắn T "trạm 3 đạt hết, mục X chưa".

---

## TRẠM 1 · HOME — Tổng quan dự án (mở http://localhost:3002/)

- [ ] **home-overview-card** — card dự án hiện: tên · ngày bắt đầu (card "Nháp" → "Bắt đầu từ 09/08/2026") · thiếu dữ liệu thì tự ẩn, không placeholder. Click card → nhảy đúng CHẶNG ĐANG DỞ (lastStage). ⚠️ Lưu ý đổi hành vi: dự án chưa có lastStage giờ mặc định vào **Thiết kế 2D** (trước là 3D) — Hoà gật hay lật?
- [ ] **presence-avatar-row** — dãy avatar trên card + thanh presence: người online = avatar MÀU + chấm; offline = trắng-đen. (Hiện chỉ có 1 user Hoà — cần thấy đúng 1 avatar màu + chấm xanh.)
- *T pre-check:* card + avatar + start date hiện đúng trên browser. Click card body trong browser máy của T không điều hướng (có thể do pane) — **Hoà click thử bằng tay**.

## TRẠM 2 · ＋ DỰ ÁN MỚI — Bảng khởi tạo

- [ ] **project-profile** — form khởi tạo có mục QUY MÔ/LOẠI HÌNH dự án (model ProjectProfile), lưu xong card Home hiện quy mô.
- [ ] **project-init-board** — bảng khởi tạo 3 mảnh **PLAN · TASK · TIMELINE** hiện đủ.
- [ ] **scaffolder** — chọn LOẠI HÌNH (vd căn hộ/văn phòng) → máy gợi ý khung việc **kèm căn cứ** từng dòng; KHÔNG chọn loại hình → máy im, không đoán bừa. Việc gieo ra mang chip chặng (TaskContext).
- *T pre-check:* ⚠️ T bấm thẻ ＋ trên Home thì rơi thẳng vào canvas 2D flow trống, KHÔNG qua Bảng khởi tạo (có thể T bấm nhầm nút ＋ khác). **Hoà kiểm đúng đường: thẻ "Dự án mới" (viền đứt) → phải ra Bảng khởi tạo.** Nếu đúng thẻ đó mà vẫn rơi canvas = LỆCH, T mở lại entry. (T lỡ tạo 1 "Untitled flow" rác lúc thử — Hoà xoá giúp hoặc để T dọn.)

## TRẠM 3 · THIẾT KẾ 2D — mở flow "Dự án mẫu" (có sẵn đối tượng)

- [ ] **pie-menu-2d** — chuột phải / chạm-giữ trên canvas → đĩa lệnh tròn (RadialToolMenu).
- [ ] **tao-viec-tu-day** — chuột phải lên MỘT ĐỐI TƯỢNG (tường/phòng) → menu "Tạo việc từ đây" mang TÊN ĐỐI TƯỢNG THẬT; flow chưa gắn dự án → nút mờ + lý do.
- [ ] **material-impact-ui** — chọn vài đối tượng → áp vật liệu từ Thư viện → bảng **Impact preview hỏi trước khi áp** (liệt kê nơi bị ảnh hưởng, số thật) → áp xong ⌘Z lùi được.
- [ ] **focus-entity-doc** — vào Bảng việc (/tasks), bấm CHIP CHẶNG trên một việc có ngữ cảnh 2D → nhảy về đúng bản vẽ, đối tượng được SELECT + camera bay tới.
- [ ] **task-context** — việc tạo từ trạm này mang đủ chip chặng/đối tượng, bấm chip nhảy ngược lại đúng chỗ (dây 2 chiều).

## TRẠM 4 · XUẤT PDF 2D — từ flow "Dự án mẫu" bấm Xuất

- [ ] **light-arc** — lúc đang xuất: CUNG SÁNG tiến độ (không phải spinner giả — indeterminate thật).
- [ ] **chuan-dau-ra-gate** — bản vẽ có lỗi chuẩn (tỷ lệ lẻ/nhãn đè) → dialog CHẶN kèm nút sửa; sạch mới cho xuất.
- [ ] **ty-le-chuan** — PDF ra tỷ lệ BẮT NẤC (1:50, 1:100…), không bao giờ "1:47".
- [ ] **khung-ten-sach** — khung tên 9 ô: đủ ô, KHÔNG jargon nội bộ, không bịa giá trị (ô thiếu để trống).
- [ ] **label-ne-hinh** — mở PDF: nhãn phòng KHÔNG đè lên đồ (PHÒNG NGỦ hết đè giường), nhãn chật chỗ có leader 8 hướng.
- [ ] **label-ne-hinh-v2** — dim (kích thước) nằm NGOÀI hình 2 lớp thẳng hàng, nhãn nằm trong biên phòng.
- *File đã soi vòng máy:* `~/Downloads/layout.pdf` (12/08 00:27) — Hoà có thể mở file này soi luôn thay vì xuất lại.

## TRẠM 5 · THIẾT KẾ 3D — cùng flow, chuyển chặng ⌘2, gạt mode "Vẽ 3D"

- [ ] **tool-state-3d** — dock có nút SỐNG (Tường/Floor/Ceiling…), bấm Tường 2 điểm → **Tool3DBar** hiện, NHẬP SỐ được (dài/cao/dày).
- [ ] **dock-3d-that** — dock chỉ hiện nút làm được thật, không nút giả (12 nút chờ → còn nút sống + disabled có lý do).
- [ ] **card-kinh-gradient** — node ĐANG render/sinh ảnh: viền card kính chạy gradient màu; đứng yên: KHÔNG chạy (ánh sáng = trạng thái, không trang trí).

## TRẠM 6 · TRÌNH CHIẾU — ⌘3

- [ ] **h4-picker** — cửa vào hiện màn chọn **6 LOẠI HỒ SƠ**; loại chưa làm được khoá kèm LÝ DO NĂNG LỰC, không CTA giả.
- [ ] **story-set** — thẻ **Story Set** đứng ĐẦU gallery hồ sơ → tạo ra deck 8 trang 6 chương (bìa editorial → DNA board → câu chuyện → ảnh → vật liệu → phụ lục), ảnh mẫu hiện đủ (đã tải offline).
- [ ] **material-a3** — Bảng vật liệu A3 mở vào EDITOR THẬT, sửa được, lưu `.idfp`.
- [ ] **tao-viec-tu-day (Trình chiếu)** — Inspector trang có nút tạo việc; thiếu projectId → nút mờ kèm lý do.

## TRẠM 7 · THƯ VIỆN + FILES + VITALS

- [ ] **library-data-that** — Thư viện đọc KHO THẬT: 17 mục seed (ảnh offline), SỐ ĐẾM THẬT trên kệ, kệ trống có empty-state + nút Nhập (hết mock).
- [ ] **fm-data-that** — /files: cây 5 root đọc ĐĨA THẬT, dung lượng thật (0 byte hiện 0 — hết "2,1GB" bịa).
- [ ] **think-dial** — Vitals có 4 NẤC SUY NGHĨ (Trả nhanh/Cân bằng/Nghĩ sâu/Nghiên cứu); nấc Nghiên cứu chạy RAG notebook thật.
- [ ] **vitals-doccontext** — đang mở bản vẽ, hỏi Vitals về bản vẽ → trả lời có NGỮ CẢNH THẬT (số phòng/lỗi chuẩn), không nói chung chung.

## TRẠM 8 · MÁY — T đã chạy hộ, Hoà chỉ liếc số

- [ ] **backup-offsite** — script sqlite `.backup` + integrity trên BẢN SAO + rsync `--link-dest` + rotation 7 + manifest. (Đợt 3 T đã chạy thử ra đích riêng OK. Hoà chỉ cần gật cơ chế.)
- [ ] **chong-lech-dinh-nghia** — `npm run soi:tu-dien`: từ 81 chỗ lệch → còn **77** (toàn trong docs/mocks cũ; nhãn hiển thị UI đã sửa; luật "sửa khi chạm").
- [ ] **hinh-hoc-dong-tam** — máy soi `npm run soi:hinh-hoc` sống: 252 file · 997 khai báo radius · bảng top file lệch.
- [ ] **hinh-hoc-ap-thang** — thang bo 6/10/14/20 + `--r-full` vào globals.css, top-10 chỗ dùng nhiều đã migrate: 498 → **442** ngoài thang (v2 là hàng đợi đã khai, không phải nợ giấu).
- [ ] **project-profile API + scaffolder engine** — đã có test + tsc pass (V đợt 3 kiểm độc lập, 1 lệch duy nhất đã vá tại commit e7abac5/a0317ad).

---

## Ghi chú T (đọc 30 giây)

1. **2 nghi vấn cần tay Hoà:** ① đường "＋ Dự án mới → Bảng khởi tạo" (trạm 2); ② click card Home nhảy lastStage (trạm 1). Browser pane của T click không ăn ở 2 chỗ này — có thể là hạn chế pane, không dám kết luận lệch.
2. Đã dọn 3 dev server cùng thư mục → còn 1 (3002). Phiên nào mở thêm server phải preview_stop khi xong (luật §0aa).
3. Sau khi Hoà tick: nhắn T (hoặc để file này đã tick trong repo) — T flip loạt `xong → xong-mat` vào frontier-registry, Cửa chỉ đóng khi nợ mắt = 0.
