# LÔ DUYỆT MẮT #1 — 47 mục xong-MÁY, gộp nghiệm thu MỘT LƯỢT (~20 phút)

> Cơ chế HOP-DONG §8.1: Hoà đi từng TRẠM theo thứ tự, mỗi trạm 2-5 phút trên app thật;
> phản hồi chỉ cần **"Trạm N ok"** hoặc **"Trạm N: <mục lệch> vì <gì>"** — T flip loạt
> `xong → xong-mat`, mục lệch mở lại entry. Không cần đọc từng dòng đủ 47.
>
> **Trục soi = 7 cấm kỵ [Đ5]:** ①lỗi giao diện ②xài không ra chất lượng ③lỗi thao tác
> ④cảm giác GIẢ ⑤gò ép không tuỳ chỉnh ⑥không phân loại ⑦thẩm mỹ kém.
>
> Server: dùng server đang chạy (kiểm cổng 3000/3001), KHÔNG mở thêm.

## TRẠM 0 · MIỄN MẮT — đề xuất flip thẳng, gật 1 lần cho cả nhóm (0-1')
Nền/máy/docs, không có mặt thị giác riêng (mặt thị giác của chúng nằm trong các trạm dưới):
`triet-ly-if` · `nhip-van-hanh-p2` · `he-luat-thao-tac` · `chong-lech-dinh-nghia` ·
`backup-offsite` · `hieu-nang-do` · `hinh-hoc-dong-tam` (luật+token, mắt soi ở Trạm 1) ·
`tu-dien-mocks-sach` · `fix-f2-node-render` (verify tay 12/12 + bộ render đã giao tay Hoà).
**→ Hoà chỉ cần gật "Trạm 0 ok" = 9 mục flip.**

## TRẠM 1 · HOME (mở app, cả theme TỐI + SÁNG) (~3')
- `home-dong-studio` — bento co giãn theo độ dày dữ liệu, 1 màn không cuộn, seed KHÔNG lên Home, cung mặt trời theo giờ; **theme SÁNG có đẹp không** (điểm treo từ đợt trước).
- `home-overview-card` — card dự án: tên+quy mô+avatar; click nhảy đúng chặng đang dở (⚠️ mặc định concept — cố ý, xem có thuận tay không).
- `presence-avatar-row` — avatar online MÀU / offline TRẮNG-ĐEN, đọc được ngay.
- `mat-do-con-tro` + `hinh-hoc-v2` + `hinh-hoc-ap-thang` — cảm nhận chung: mật độ, bo góc đồng tâm có "một nhà" không (tội ⑦).

## TRẠM 2 · CHẶNG 2D + XUẤT PDF (~5')
- `pie-menu-2d` — đĩa lệnh hiện đúng chỗ, không che tay.
- `vitals-doccontext` — Vitals nói đúng ngữ cảnh bản vẽ đang mở (tội ④: không bịa).
- `focus-entity-doc` + `tao-viec-tu-day` — chuột phải đối tượng → Tạo việc từ đây (tên đối tượng THẬT); bấm việc → nhảy đúng đối tượng.
- **Bấm Xuất PDF** rồi MỞ FILE: `label-ne-hinh` + `label-ne-hinh-v2` (0 nhãn đè hình, dim ra ngoài thẳng hàng) · `ty-le-chuan` (tỷ lệ chẵn 1:50, không "1:47") · `khung-ten-sach` (9 ô, 0 jargon) · `chuan-dau-ra-gate` (thử xuất bản thiếu — máy phải CHẶN kèm lý do) · `light-arc` (cung sáng tiến độ lúc xuất, không phải spinner giả).

## TRẠM 3 · CHẶNG DỰNG / 3D (~4')
- `tool-state-3d` + `dock-3d-that` — dock chỉ nút THẬT (5 nút, không nút giả — tội ④), nhập số sau thao tác.
- `build-recipe` — tab Sửa: ngăn xếp công thức, tắt/bật từng bước, undo giữ nguyên.
- `card-kinh-gradient` — viền gradient CHỈ chạy khi đang render (ánh sáng có nghĩa, không trang trí tĩnh).
- `grounded-render` — node "Phiếu đọc tham khảo" + "Render bám ý (mảng)": phiếu 4 cấp hiện ra SỬA ĐƯỢC, chưa duyệt thì nút chạy mảng phải từ chối kèm lý do (tội ③⑤).

## TRẠM 4 · TRÌNH CHIẾU (~4')
- `present-task-first` — mở chặng: 3 lối theo VIỆC (không phải rừng công cụ), toolbar 6 nút + nhóm ⋯.
- `h4-picker` — màn chọn 6 loại hồ sơ, thẻ ＋ Tạo trống ở cuối, tạo ngay không form.
- `smart-convert-pdf` — Mở tệp PDF → deck có LỚP CHỮ THẬT (bôi đen được chữ, dấu tiếng Việt chuẩn).
- `material-a3` + `editor-bang-bieu-mau` — Bảng vật liệu A3 + "Bảng thống kê" cửa/phòng: sửa ô tay rồi re-sync KHÔNG bị đè (tội ③).
- `story-set` — template 6 chương trung tính, đọc như tạp chí (tội ⑦ soi kỹ).

## TRẠM 5 · THƯ VIỆN · FILES · GALLERY (~3')
- `library-data-that` + `fm-data-that` — số đếm THẬT từ kho/đĩa, không mock (tội ④).
- `gallery-lien-nganh` — /library/gallery: nhóm ngành, bộ sưu tập CÓ NGUỒN.
- `material-impact-ui` — đổi vật liệu: preview impact TRƯỚC khi áp, undo được.
- `panel-handle-chung` — tay cầm ‹ › thu/mở panel: vị trí đoán được, thu rồi vẫn thấy tay cầm.

## TRẠM 6 · DỰ ÁN MỚI · TRÍ TUỆ DỰ ÁN (~3')
- `project-profile` + `project-init-board` — ＋ Dự án mới: bảng khởi tạo 3 mảnh PLAN·TASK·TIMELINE.
- `scaffolder` — khung việc gợi ý KÈM CĂN CỨ; thiếu loại hình thì IM (không đoán bừa — tội ④).
- `dna-card` — Thẻ DNA 8 lớp của dự án: nguồn + cờ suy/đo rõ ràng.
- `task-context` — chip chặng trên việc, bấm nhảy đúng workspace.
- `think-dial` — 4 nấc suy nghĩ Vitals đổi được, nấc Nghiên cứu trả lời có trích nguồn.

---
**Đếm:** Trạm 0 = 9 · T1 = 6 · T2 = 10 · T3 = 6 · T4 = 6 · T5 = 5 · T6 = 5 → **47/47.**
Sau khi Hoà phản hồi: T flip loạt trong `scripts/frontier-registry.mjs` (thêm trạng thái
`xong-mat` theo cơ chế §8.1), mục lệch mở lại entry + ghi tội danh theo [Đ5].
