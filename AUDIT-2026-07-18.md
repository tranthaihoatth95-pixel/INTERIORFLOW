# Chẩn đoán toàn diện — InteriorFlow (18/07)

Tổng hợp từ toàn bộ audit đã làm trong phiên này (code thật + test thật + thao tác trình duyệt thật, không chép từ tài liệu). `main`/`origin/main` hiện ở `1ce8674`, khớp `feat/present-layout-ml-p1`.

## Chặng 1 — Layout CAD: vững, sản xuất được

Đã verify: 41 shape nội thất, checker TCVN/QCVN (data-driven, có cờ `verified:true/false` minh bạch), modify tools kiểu AutoCAD đầy đủ (trim/extend/fillet/chamfer/array/stretch), DWG import cô lập GPL, MEP sơ cấp, export PDF/.idf/DXF, multi-sheet, copy-paste/undo-redo. **21/21 file test PASS, tsc 0.**

Cần biết:
- "AI-assist" mới là rule-based (regex+từ khoá), chưa có LLM thật — tránh gọi là "AI" trước khách nếu dễ hiểu lầm.
- `docs/CAD-ROADMAP.md` đã lỗi thời (nói trim/fillet/chamfer "cố tình chưa làm" nhưng thực ra đã làm ở Sprint 9-10), nên gắn nhãn cũ hoặc cập nhật.
- Hatch xuất DXF chỉ là đường bao, không phải hatch-pattern chuẩn CAD.
- 4 file stress test CAD đã mất, chưa viết lại.

## Chặng 2 — Render: vững, nhưng 2 điểm dễ hiểu lầm

Node canvas + grouping (bug Cmd+G **đã sửa và merge**), 7 node render-v2 kiến trúc 2 tầng thật (AI có key → lõi tất định), adapter NVIDIA/fal/ComfyUI/SD-portable/Ollama đều network call thật (không mock). CAD→3D qua Blender pipeline thật. Handoff CAD↔Render↔Present đều thật. **tsc 0, 7 file test trọng tâm PASS 100%.**

Cần biết:
- WebGPU local chỉ là stub (luôn throw `WEBGPU_PENDING`), dropdown chọn được nhưng không sinh ảnh thật.
- ~28 node "v1" cũ (sketch2render, moodboard, styletransfer...) vẫn dùng mock SVG placeholder khi thiếu key — khác hẳn 7 node render-v2 mới, dễ tưởng cả node đều "không mock".
- fal hiện hết balance, chờ nạp credit — không verify được network thật (code đúng, hạ tầng đang chặn).
- Chưa có test end-to-end cho logic chọn tầng AI/lõi của node render-v2.

## Chặng 3 — Present: nền tảng mạnh, còn vài lỗ hổng trải nghiệm thật

**Đã merge vào main tuần này** (5 nhánh, tsc 0, 49/49 test PASS trên `main` trước khi push):

- Bug GroupOverlay vô hình (Render canvas Cmd+G) — sửa xong (`ViewportPortal` + bỏ `zIndex:-1`).
- **PS-2** — Lưu slide này thành template → nhóm "Của tôi" trong picker, persist localStorage.
- **PS-3** — Round-trip `/photo-editor` ↔ slide (truyền ảnh vào/composite ra/ghi về) + tài sản liên kết (assetId dùng chung nhiều slide) — **đã xác nhận hoạt động đúng bằng mắt** qua audit UX thật (sửa 1 nơi, 2 slide cùng cập nhật).
- **PS-4** — Đa khổ A4/A3 ngang-dọc ngoài 16:9, gộp 1 nguồn sự thật khổ sân khấu, reflow tự động, export đúng khổ (PPTX cố ý giữ 16:9). UI có nhãn bắt buộc "khổ trình bày màn hình/chiếu — KHÔNG phải in 300dpi".
- **PS-7** — Phím tắt kiểu Photoshop trong `/photo-editor` (⌘/Ctrl+Z/Y, V/B/E/S/J/M/L chọn tool, `[`/`]` cỡ cọ), LayersPanel rõ hơn adjustment-vs-raster.

**Phát hiện MỚI từ audit UX thật (đóng vai người dùng thật, chưa fix):**

1. **Bug thật, tái lập được nhiều lần**: nhãn slider trong tab "Chỉnh màu" (photo-editor) bị cắt mất ký tự đầu ("Sáng 100" → "áng 100", "Tương phản 100" → "rơng phản 100"...) — khớp đúng ảnh chụp màn hình ban đầu. Không xảy ra ở tab "Cắt"/"Thay ảnh".
2. **PS-2 "Của tôi" không lộ ra UI** trong lần audit UX — cần kiểm tra lại vì sao (có thể do agent chưa tạo template nào nên mục ẩn theo thiết kế, cần xác minh thêm chứ chưa chắc là bug).
3. Đổi Khổ trình bày (PS-4) qua lại nhiều lần (16:9 → A4 dọc → A3 ngang → 16:9) → tiêu đề kẹt sai vị trí (top-center thay vì bottom-center gốc), không phục hồi khi quay về khổ ban đầu.
4. Brand Kit "Áp lại theme cho cả deck" không có phản hồi thị giác nào quan sát được (modal không tự đóng, không toast, màu slide có sẵn không đổi theo).
5. Không có color picker đầy đủ cho màu chữ trong TextToolbar — chỉ 1 nút toggle đen/trắng.
6. Ảnh nền slide không click/double-click/right-click được (không có object handle) — dễ khiến người dùng tưởng ảnh "chết".
7. Không có xác nhận sau khi Export (không toast/progress).
8. Không có phím tắt tool đơn ký tự (T/V...) ở `/present-editor` như đã có ở `/photo-editor` — không nhất quán giữa 2 chặng.

**Cụm chức năng còn thiếu so với PowerPoint/Photoshop:**
- Template gallery để duyệt/click-áp dụng (Mẫu hiện chỉ mở panel AI-generate, không preview trước).
- Smart guide / căn chỉnh-phân bố nhiều object khi kéo-thả.
- Slide Sorter dạng lưới toàn deck.
- Animation Pane theo từng object (Motion hiện chỉ set cấp-slide).
- Format Painter (sao chép định dạng).
- Bảng màu chữ/style preset nhanh.

*(Điểm "mất dữ liệu khi quay lại từ Chỉnh ảnh nâng cao" mà agent audit UX báo cáo ban đầu — đã tự tay kiểm chứng lại bằng code + thao tác trình duyệt thật: là artifact do agent bị lẫn tab lúc test (window.open mở tab MỚI, tab gốc không hề bị ảnh hưởng), KHÔNG phải bug thật của app.)*

**Điểm làm tốt, đáng ghi nhận:**
- Multi-sheet giới hạn 5 xử lý rõ ràng (disabled + tooltip khi chạm giới hạn).
- Tài sản liên kết (PS-3) hoạt động đúng thật — đã xác nhận bằng mắt.
- Engine chỉnh ảnh non-destructive (adjustment layer, blend mode, preset "Quiet-luxury grade", Levels, White Balance) — chiều sâu chuyên môn áp sát Photoshop.
- Export menu có mô tả 1 dòng dễ hiểu cho người dùng phổ thông.
- Crop tool có preset tỉ lệ + lưới rule-of-thirds.

## Hạ tầng chung

- **Bug môi trường nghiêm trọng đã sửa**: `DATABASE_URL="file:./dev.db"` trong `.env` resolve theo `process.cwd()` (root) thay vì `prisma/`, khiến app đọc nhầm database rỗng → MỌI đăng nhập (Google + email/password) trả lỗi 500. Đã đổi sang đường dẫn tuyệt đối, xoá file rỗng, restart dev server — verify login trả đúng 401 (sai mật khẩu) thay vì 500. Lỗi này có từ trước phiên này (`.env` mtime 03/07), không phải do phiên này gây ra.
- Git hygiene: 0 worktree rác, `main`/`origin/main` đồng bộ ở `1ce8674`.

## Đang treo, chưa merge / chưa xong

- **`feat/smart-tooltips`** — chú thích hover thông minh (component `Tooltip.tsx` dùng chung) áp vào 4 toolbar chính (Present/Photo-editor/CAD/StudioBar). Đã audit độc lập: tsc 0, test mới 6/6 pass, diff 9 file gọn gàng. Phát hiện đáng chú ý: tự sửa 1 bug CSS overflow-clip thật khi verify (CadToolbar cắt mất tag do `overflow-x:auto` kẹp `overflow-y`). Chờ duyệt merge.
- **Đại tu UI/motion qua Figma** — bị chặn: kết nối Figma MCP hiện tại chỉ có quyền đọc (Dev Mode), tài khoản chỉ có seat "View". Đã khảo sát hiện trạng thật (chụp đầy đủ các màn hình) + viết đề xuất thiết kế lại (gộp 3 modal thành property panel thống nhất, custom slider chống bug, hệ motion nhất quán) nhưng chưa dựng được file Figma thật. Đang chờ chủ dự án nâng quyền Figma (Editor seat + kết nối loại có tool ghi).

## Tổng kết mức độ sẵn sàng

App có kỷ luật kỹ thuật tốt: test thật (không chỉ đếm file), ghi nhận giới hạn trung thực trong code/docs thay vì phóng đại (verified:false flags, non-goal ghi rõ lý do). CAD dùng được cho sản xuất thật. Render vững phần lõi, cẩn thận khi trình bày "AI" ở phần node cũ còn mock. Present đủ sâu về mặt kỹ thuật ảnh (ngang Photoshop ở phần color-grade) nhưng cần vá bug slider + xác minh PS-2 UI trước khi cho khách dùng thật, và còn khoảng cách rõ với PowerPoint ở khâu "trình bày" (template gallery/animation/align/slide-sorter).

**Việc cần quyết định tiếp:** ưu tiên sửa bug slider "Chỉnh màu" + kiểm tra PS-2 "Của tôi", hay merge tooltip trước, hay việc khác.
